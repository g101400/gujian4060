# 版本变更记录 v3.7.12（古建景点打卡 · 内部版 · 三端同批）

- 版本：**v3.7.12**｜构建日期：2026-09-13
- 同批版本：水利 **v3.74**／感知 **v1.50**
- 归档：`APK归档/四端安装包_20260913_v374_int`
- 回退备份：`backup_2026-09-13_before_v374`
- 提版脚本：`bump_version_v374_int.py`
- 公开派生：无（古建为单通道，公开数据不另派生）

> 古建核心元数据保持：5 级景区组织（景区→园区→子景点→打卡位→标签，key=spot/area/sub/point/tag）
> + 17 类古建类型 + 快捷常用 ⭐ + 历史关键词可折叠 + 周边搜索 nbtype + AI 文物助手分支。

## 一、本批实际改动

### 口令硬化（O1，跟随同批）
- 去除源码明文「开发者分机号」，改 AES-GCM(PBKDF2) 凭证校验能力（门闸**保持关闭**，仅借用校验，不对古建启用访问口令）。
- 明文 `3305` 计数 = 0（已 grep 核验）。
- 古建无局/管理处字段，**不涉及数据化（O5）**。

## 二、门禁与验证（全绿）

- `sync_all_mirrors.py --check`：11 镜像 + 跨产品 7 模块 md5 一致（EXIT=0）
- `verify_pass.py <归档> int`：21/21（古建 v3.7.12 数据标记 + 版本）
- `verify_pkgs_smartquery.py`：pub+int 52/52
- `node --check`：canonical `travel/android/assets/app.js` + 各镜像全过
- 口令硬化回归：明文 `3305` = 0

## 三、四端产物

见 `APK归档/四端安装包_20260913_v374_int/`：古建 APK / 4 架构 deb（无内部加密变体）/ Win MSI+EXE / iOS PWA zip。

## 四、发布与分发

- GitHub Release `gujian-travel5090@v3.7.12`（资产下载正常；内部私有仓无 Pages 422 边界，仅走 Release）。
- 百度网盘 `yitu-release/四端全量_v3.74_1.50_3.7.12_20260913/` 四平台 26 文件。
- 交付须知：须重新安装新 APK 方能看到口令硬化改动生效。
