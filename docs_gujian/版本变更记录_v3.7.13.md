# 版本变更记录 v3.7.13（古建景点打卡 · 内部版 · 三端同批）

- 版本：**v3.7.13**（内部偶数通道）｜构建日期：2026-09-16
- 同批版本：水利 **v3.76**／感知 **v1.52**
- 归档：`APK归档/四端安装包_20260916_v376_int`（24 个文件）
- 公开派生：古建 **v3.7.14**（奇数通道，GitHub Pages，由 `derive_public_v377.py` 派生）

> 本轮为**三端同批同步发布批**：古建侧自 **v3.2** 起无独立新需求（古建智能化升级/知识库/AI 查询融合已在 v3.2 落码并保持），
> 本轮核心是把水利 v3.76 的 **AI·知识库增强** 经共享模块回灌到古建端，并完成四平台（Android/Win11/统信UOS/iOS PWA）同步打包。

## 一、本批实际改动

### 1. 共享 AI·知识库增强（经共享模块落古建端）
共享模块 `kb_rag.js` / `kb_vector.js` / `ai_module.js` 已随同步在古建 webroot 内置，古建端获得：
| # | 项 | 要点 |
|---|---|---|
| D-1 | 智能推荐复用 `kbHybridSearch`/`KBRag` | 替代孤立文本匹配；混合权重 向量 0.45 / BM25 0.35 / 模糊 0.20（权重可学习）；修复「晚 3」复制条件 bug |
| D-2 | KB 属性结构化索引 | `kb_vector.js` 数值等值/范围查询与向量召回融合 |
| D-3 | RAG 引用溯源 | chunk 原文高亮 + 置信度 + 混合权重可学习 |
| D-4 | 向量索引增量构建 | 切片签名缓存 + `ts` diff，加速启动 |
| D-5 | KB 多源冲突裁决 | OCR / 网页 / Hermes 来源置信度 + 人工修订优先 + 冲突提示 |
| D-6 | query→推荐→报告→KB 闭环 | 报告导出 + 结果写回知识库 |

### 2. 统信运行慢优化（共享模块同步）
首屏优先渲染 + 启动耗时 profiling + 延迟非关键初始化（AI 菜单 / CtxMenu / cleanInbox）。

### 3. 保持项（不删不减，回归保持）
- 口令硬化（v3.7.12）：源码无明文开发者分机号，AES-GCM(PBKDF2) 凭证校验。
- 数据：1032 条古建知识库种子（含属地/朝代/级别/简介/特点）随包内置。
- 古建专属能力（5 级景区组织、17 类 CANONICAL_TYPES、快捷常用 ⭐ 隔离、ZIP 三级匹配古建版、文物助手 AI 系统提示词）保持。
- 四平台（Android/Win11/UOS/iOS PWA）webroot 全量同步。

## 二、验证（夜班门禁）
- `sync_all_mirrors.py --check` EXIT=0（11 镜像 + 跨产品 7 公共模块 md5 一致，v3.7.13）
- `verify_pass.py int`（四端安装包_20260916_v376_int）21/21 PASS；`verify_pkgs_smartquery.py` 含古建 4 架构 deb 全 PASS
- `sync_weapp_version.py --check` ✅ 小程序基线已对齐 v3.76（2026-09-16）

## 三、说明
- 古建侧独立特性（v3.2 智能化升级、知识库、AI 查询融合、文物助手）保持，本轮不新增、不删减。
- 公开派生 v3.7.14 由奇数通道脚本派生，公开 PWA 经脱敏（不含真实 sk-or-v1 密钥，门禁已扫 `公开PWA_20260916_v377_pub` 干净）。
