# 古建景点打卡 v3.7 — 版本变更记录（同步水利一张图 v3.49）

> 生成日期：2026-09-02
> 升级来源：水利工程基础信息一张图 v3.49（「三图联动」规范：母版升级自动移植到派生应用）
> 包名：com.shuili.gujian · versionCode 37 · versionName 3.7
> 说明：古建此前无独立文档目录（`docs_gujian/` 首份变更记录），与 `docs_shuili_v3/版本变更记录_v3.49.md` 配套阅读。本篇记 v3.7 文档与诊断增强。

## 一、本次变更范围（与水利 v3.49 同源，文档与诊断增强）

1. **全平台启动诊断 logRuntime（版本号自动跟随）**：`travel/android/assets/app.js`（canonical）新增 `logRuntime()`，启动时把「产品名 + 版本号（读 `APP_VERSION`） + 构建日期 + 平台」写 `console.log` 与 `localStorage["yitu_runtime_log"]`；版本一升日志自动跟随。UOS 端 `server.py._startup_log()` 兜底（铁律-UOS5~9 已落地，本版保留）。
2. **子菜单「帮助」→「功能介绍」改名 + 正文补全**：`showHelp()` 标题改「功能介绍」，正文扩充古建语义（⑨ 快捷常用 ⑩ 智能AI ⑪ 知识库 ⑫ 统计/版本变更 ⑬ 错误日志排查等）；菜单 `{ico:"❓", t:"功能介绍"}` 同步改名，旧「帮助」残留清零。
3. **版本变更补到最新**：`openChangelog()` 在 1.8 前插 `cv:"3.7"` / `cd:"2026-09-02"` 条目（四端功能对照单同 v3.49 同步）。
4. **版本统一提升**：古建 v3.6→**v3.7**，构建日期 2026-09-02；`version.json` + app.js + 全部 webroot（canonical `travel/android/assets` + 镜像 `gujian-v31/assets` + win/uos webroot）+ 构建脚本 + 小程序基线全量同步。

## 二、关键实现点

| 点 | 说明 |
|---|---|
| canonical 指针 | 古建 canonical = `travel/android/assets`（与 `sync_all_mirrors.py` / `bump_version_v349.py` 一致，`gujian-v31/assets` 为镜像之一，见铁律-多副本2 / T-044） |
| 版本同步 | `bump_version_v349.py`：version.json(真相源) + app.js(`3.7`，无 v 前缀) + webroots + make_deb/gen_win_msi/build_ios_zip 版本串；小程序 `sync_weapp_version.py DEFAULTS.gujian=3.7` |
| 古建独有 | 5 级景区组织（spot/area/sub/point/tag）、17 类古建类型、AI 默认联网（公开文物可核实）、data.js 取 `window.SCENERY`（1032 条） |
| 启动诊断 | `logRuntime()` 三端同源注入，版本号读 `APP_VERSION`（随 version.json 自动更新） |

## 三、验证

- `node --check`：app.js 全过；古建 `APP_VERSION` = `3.7`（无 v 前缀，铁律-版本 T-006）。
- UOS deb：`verify_myml_ar.py` 通过（`gujian-map_3.7.20260902_<arch>.deb`，四架构 ×1）。
- iOS：DEPLOY_README 版本 `3.7_20260902` 正确。
- 同步门禁：`sync_all_mirrors.py --check` EXIT=0；`sync_weapp_version.py --check` 基线 v3.7。

## 四、交付与回退

- 交付：`APK归档/四端安装包_20260902_v349/`（古建 APK / gujian-map_3.7.20260902_* deb×4 / 古建景点打卡 Setup.msi+exe(构建机产) / 古建景点打卡_iOS_3.7_20260902_可托管.zip）。
- 回退：`backup_2026-09-02_before_v349/`（bump 前版本文件，含 travel/android assets）。
- 已知限制：iOS PWA 无原生文件桥（导出降级浏览器下载）；桌面端照片压缩仅安卓完整。
