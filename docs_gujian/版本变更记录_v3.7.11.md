# 版本变更记录 v3.7.11（古建景点打卡）

- 版本：**v3.7.11**（versionCode 39）｜构建日期：2026-09-11
- 同批版本：水利 **v3.72** ／ 感知 **v1.48**（重出）
- 归档：`APK归档/测试包_20260911_v9_int`
- 回退备份：`backup_2026-09-11_before_v372`

## 一、本轮改动

**无功能改动，随批重出。**

本轮三端同批发布中，实际改动集中在感知 App（新增内置「水工建筑物底图种子」）；
古建景点打卡的代码、数据与 v3.7.10 **完全一致**，仅同步提升版本号并重出四端安装包，
以保证三端版本号与发行节奏一致（铁律：三端四平台同步）。

## 二、门禁与验证

- `node --check` 古建 canonical + 各镜像 webroot `app.js` 全通过
- `sync_all_mirrors.py --check`：镜像一致 + 跨产品 7 个公共模块 md5 一致
- APK 签名 `apksigner` verify 通过（versionCode 39 / versionName 3.7.11）
- UOS deb 4 架构 ar 头门禁 `verify_myml_ar.py` 全过
- Windows MSI / NSIS EXE 构建版本串已同步为 3.7.11

## 三、四端产物

见 `APK归档/测试包_20260911_v9_int/`：Android APK、UOS deb ×4 架构、Windows MSI + NSIS EXE、iOS 可托管 PWA zip。

## 四、备注

古建**不加口令锁**（公开受众向），本批未启用口令门；GitHub 发布通道为 `g101400/gujian-travel5090`。
