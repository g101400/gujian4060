# 古建景点打卡 v3.7.3 — 版本变更记录（同步水利一张图 v3.55）

> 生成日期：2026-09-05
> 升级来源：水利工程基础信息一张图 v3.55（三图联动）；感知 v1.31 同步
> 包名：com.gujian.travel · versionCode 32 · versionName 3.7.3
> 性质：代码级更新（先修复不打包），镜像/文档已就绪，安装包下次发版统一构建。
> 改动前备份：`backup_2026-09-05_before_kbslice_20260905_140441/`（travel/android/assets + travel/webroot）

## 一、本次变更范围（与水利 v3.55 / 感知 v1.31 同源，古建核心不变）

1. **知识库切片保持语句相对完整**：整句为最小打包粒度（段落→句末标点→逗号级→极端硬切兜底），重叠按整句回溯并预留预算保证不超长；切片绝不在句中截断。
2. **保存前先切片再落盘**：kbAdd/导入/网页入库/OCR入库/Hermes沉淀/查询保存统一经 `kbSave` 写入口先切片（chunks+签名缓存）随条目保存；向量索引优先复用已存切片。
3. **RAG 智能检索四端同步**：混合检索（向量+BM25+模糊）、内容反查条目、模糊/智能查询、智能生成提示词、大模型智能问答（引用标注，失败降级抽取式答案）、记忆管理 + Hermes 自我学习；保留古建语义（5 级景区组织 / 17 类古建类型 / AI 文物助手分支）。
4. **OCR 外部文档入库同步**：pdf/docx/xlsx/csv/txt/md 智能转 Markdown（本地化 Tesseract + pdf.js，assets/ocr/ 随包）。
5. **升级备份增强同步**：导出自定义文件夹/文件名（默认 `古建景点打卡备份_YYYYMMDD.bak`），导入「请确认这是最新备份，导入会覆盖程序中的全部数据」预检 + 合并/覆盖双模式；升级检测接入网盘托管清单。
6. **信息与帮助更新（本版重点）**：
   - `openChangelog` 头插 **3.7.3** 块（含 3.7.1/3.7.2 以来的全部增量）；
   - **新增「四端功能对照单」菜单项**（信息与帮助 → 版本变更 之后）与 `openPlatformCompare()`——此前古建无此菜单，现与水利/感知对齐（8 项功能 × 安卓/Win11/统信UOS/iOS PWA）；
   - 功能介绍新增 ⑭ RAG/入库、⑮ 升级备份与四端对照单。

## 二、四平台同步

- canonical = `travel/android/assets`（version.json 真相源 3.7.3/code 32/2026-09-05）。
- 镜像：gujian-v31/assets、**travel/webroot（本次补入镜像清单）**、win-gujian-webview2/webroot、uos-gujian-pyqt6/webroot —— 核心文件 md5 全一致，`node --check` 通过。
- 小程序基线同步 3.7.3（jingyin）。

## 三、测试

| 套件 | 结果 |
|---|---|
| kb_slice_test.js | 11/11 |
| kb_rag_test.js | 22/22 |
| kb_test.js | 10/10 |
| up_test.js | 14/14 |
| kb_ocr_test.js | 7/7 |
| help_menu_test.js（古建帮助子菜单运行时，含新对照单） | 6/6 |
| load_test.js 古建启动 | OK |

## 四、下次发版

古建 3.7.3（versionCode 32），APK/MSI+EXE/UOS deb/iOS PWA 统一出包；UOS deb 按 `verify_myml_ar.py` 门禁（mips64el/loongarch64/arm64/amd64 架构对照不变）。
