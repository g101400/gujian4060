# 古建景点打卡 v3.7.6 — 版本变更记录（同步水利 v3.62 / 感知 v1.38）

> 生成日期：2026-09-08
> 包名：com.gujian.travel · versionName 3.7.6
> 性质：公开版（古建不分内外通道）。用户复检五条问题修复 + 隐藏/收藏移植。
> 改动前备份：`sync_all_mirrors.py` 自动备份；canonical git 可回退。

## 一、本次需求

同水利 v3.62 / 感知 v1.38 五条（隐藏/收藏二次确认、照片灯箱空白、导入照片落盘、deb 筛选导出照片、deb 图标），古建范围：

## 二、核心改动

### E1 隐藏/收藏整体移植（古建专属适配）

- 古建无菜单 key，改用 `menuKeyOf(it)="t:"+it.t` 稳定键；组级/项级双层过滤。
- 收藏组注入跳过已隐藏项（古建收藏键是 gi:ii，隐藏会错位）。
- 收藏 ask 二次确认 + 图标改内联 SVG。

### E2 照片灯箱空白根治

- `photoFullSrc` 大缩略图多级兜底（v3.7.6 注释：安卓走「大缩略图」避免 readPhoto 回传整图 base64 超 JS↔Java 桥上限导致空白；浏览器端按本地 HTTP 服务 origin 解析相对路径）；appLightbox `img.onerror` 自动退回缩略图。

### E3 导入照片落盘归位

- escapeJson 单引号回归修复（LargeFileManager）；`linkPhotoToBuilding` 原生桥把导入照片归位 `photos/<条目id>/pic_N.jpg`。

### E4 deb 筛选后导出照片 + 内联 SVG 图标

- 导出收集 `file || data` + 筛选遍历 + 统计条；菜单图标内联 SVG（系统缺 emoji 字体不再发虚）。

## 三、验证

- 三平台同步（Android / Win / UOS / iOS PWA）；`node --check` 全过。
