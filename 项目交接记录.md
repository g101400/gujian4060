# 项目交接记录 · 三图一张图离线地图 App 家族

> **交接日期**：2026-08-31
> **项目维护人**：炎冰（业务方）/ 小七（AI 万能助理，开发执行）
> **当前状态**：三产品 × 四端（Android / UOS / Win / iOS PWA）+ 微信小程序 全部可构建、已出包至 `APK归档/四端安装包_20260831_v346/`
> **最新版本**：水利 v3.46 · 感知 v1.22 · 古建 v3.4（baseline 2026-08-31）
> **适用读者**：接手本项目的开发/运维工程师

---

## 1. 项目概述

一套**离线优先**的「一张图」地图 App 家族，基于同一套离线 WebView 技能（`shuili-yitu-app`）派生出三个业务产品，每个产品出四端安装包（外加一个微信小程序）。

| 产品 | 包名 | 核心实体 | 网络策略 | 定位 |
|------|------|----------|----------|------|
| **水利工程基础信息一张图**（母版） | `com.shuili.yitu` | 水工建筑物 | 本地辅助（不联网编造） | 内部水利资料 |
| **水利感知项目一张图**（派生） | `com.shuili.ganzhi` | 感知设施设备 | 本地辅助（不联网编造） | 内部监测设备资料 |
| **古建景点打卡**（派生） | `com.gujian.travel` | 古建景点 | 联网（核实公开文物） | 公共受众 / 旅游 |

**四端形态**：
- **Android APK** —— `aapt2+d8+zipalign+apksigner`（JDK17，零 Gradle）
- **统信 UOS DEB** —— `mips64el`（龙芯 3A4000 / UOS 20 专业版 1050），PyQt6+QWebEngine 壳
- **Windows 11** —— WebView2（EXE/NSIS 自解压 + WiX7 MSI）安装器
- **iOS PWA** —— 可托管 ZIP（Apple 真机"添加到主屏幕"）
- **微信小程序** `jingyin` —— 独立工程，baseline 对齐到 v3.46（功能未全量移植，见 §10）

**统一技术栈**：Leaflet 离线底图（天地图 WGS84）+ 离线 WebView 承载壳；网页逻辑 `app.js / data.js / ai_module.js / ovobj_bridge.js / ctx_menu.js` 四端完全复用，仅替换承载壳与原生桥。

---

## 2. 当前版本状态（2026-08-31）

| 产品 | 版本 | versionCode | buildDate | 数据种子 | canonical 目录 |
|------|------|-------------|-----------|----------|----------------|
| 水利一张图 | **v3.46** | 47 | 2026-08-31 | 557 条建筑 | `android-build/shuili-v329/assets` |
| 水利感知 | **v1.22** | 27 | 2026-08-31 | 1125 条设施 | `android-build/perc-v13/assets` |
| 古建打卡 | **v3.4** | 27 | 2026-08-31 | 1032 条古建 | `android-build/gujian-v31/assets` |
| 微信小程序 | baseline v3.46 | — | 2026-08-31 | — | `jingyin/` |

> 版本号遵循「八处同步」：`version.json`（真相源）→ `app.js(APP_VERSION)` → `AndroidManifest.xml` → 6 个 webroot → `make_deb.py`/`gen_win_msi_wix7.py`/`build_ios_zip.py` 硬编码串，由 `bump_version*.py` 一次对齐。

---

## 3. 工程目录地图（关键路径）

```
D:\Users\Claw\
├── android-build/                 # ★ Android 工程 + 版本唯一真相源 assets
│   ├── shuili-v329/assets/        # 水利 canonical（改动从这里出发）
│   ├── perc-v13/assets/           # 感知 canonical
│   ├── gujian-v31/assets/         # 古建 canonical
│   ├── water-v329/assets/         # 水利 staging 镜像（随 shuili 同步）
│   └── <各产品>/build_apk.sh      # APK 构建脚本 + 版本门禁
├── aowwei_app/webroot_shuili/     # 水利母版镜像（历史"母版"，现作为镜像之一）
├── travel/android/                # 古建 Android 真源（app-release.apk 出处）
├── native-shell/                  # ★ 原生壳 + deb/msi 构建器
│   ├── win-water-webview2/webroot/        # 水利 Win 源
│   ├── uos-water-pyqt6/webroot/           # 水利 UOS 源
│   ├── win-webview2/webroot/              # 感知 Win 源
│   ├── win-webview2/publish_win_perc/webroot/
│   ├── uos-pyqt6/webroot/                 # 感知 UOS 源
│   ├── win-gujian-webview2/webroot/       # 古建 Win 源
│   ├── uos-gujian-pyqt6/webroot/          # 古建 UOS 源
│   ├── make_deb.py / build_uos_deb.py     # ★ UOS deb 构建器（ar 头铁律）
│   ├── verify_myml_ar.py                  # ★ deb 安装门禁（必跑）
│   ├── build_win_exe_nsis.py / gen_win_msi_wix7.py / build_win_msi.ps1
│   └── README.md                  # 原生壳构建说明
├── jingyin/                       # 微信小程序工程（独立，见 §10）
├── docs_shuili_v3/                # 水利软件文档（需求/设计/变更/版本历史）
├── docs_perc/                     # 感知变更文档
├── APK/                           # 空（临时）
├── APK归档/                       # ★ 各版本四端产物归档（按日期+版本命名）
├── backup_2026-08-*/              # 各版本升级前全量快照（回退点）
│
├── bump_version.py                # ★ 版本八处同步（最新范式，含自动 sync）
├── bump_version_v331~v346.py      # 历史 bump 脚本（参考，勿手写新版本）
├── sync_all_mirrors.py            # ★ 三产品 webroot 多副本对齐工具（防分叉）
├── build_four_ends.sh             # 三产品四端打包驱动（调 multi-platform-app-pack）
├── release_v331.sh                # 早期三产品四端发布脚本
├── _apply_fixes_v347.py           # 下版本(v3.47)预热补丁脚本（进行中）
└── 三工程问题技术分析汇总_2026-08-23.md / V3.15_to_V3.24_三端同步分析报告.md / 工程回顾与自我总结_2026-08-19.md
```

**关键技能（位于 `C:/Users/admin/.workbuddy/skills/`）**：
- `shuili-yitu-app` —— 离线 APP 构建脚手架 + `references/pitfalls.md`（22 条坑，唯一真相源）
- `multi-platform-app-pack` —— 四端一键构建（`build_apk.sh`/`build_deb.py`/`build_win_nsis.py`/`build_apple_zip.py`/`verify_pkg.py`/`gen_compare.py`）
- `uos-deb-build-verify` —— UOS deb 构建 + `verify_myml_ar` 门禁封装
- `win-webview-map-app` —— Win WebView2 安装器

---

## 4. 核心架构概念（接手必读）

### 4.1 版本唯一真相源（canonical）
每个产品的 **`android-build/<产品>/assets/`** 是真相源，`version.json` 是版本号唯一真相源。`build_apk.sh` 由 `version.json` 同步 `AndroidManifest` 的 `versionCode/versionName`。**改版本号只需改 `version.json` + `app.js` 的 `APP_VERSION`**，再跑 bump/sync 工具扩散。

### 4.2 多副本镜像（最易出事的地方）
每个产品的 web 源码存在多份副本（canonical + 各端 webroot + 母版镜像）。历史上已因 bump 脚本只同步"手挑副本"**分叉 6 次**（v3.30.3/v3.31/v3.33/v3.38/v3.41/v3.45）。**彻底根治工具 = `sync_all_mirrors.py`**，固化三产品「canonical → 全部镜像」映射：
- 仅 add/update（**绝不删镜像自带 leaflet**）
- 自动备份 + 核心文件 md5 + `node --check app.js`
- 支持 `--check`（只校验）/ `--dry`（只报告）

**三产品镜像映射**（来自 `sync_all_mirrors.py`）：
```
shuili  canonical = android-build/shuili-v329/assets
        mirrors   = aowwei_app/webroot_shuili, android-build/water-v329/assets,
                     native-shell/win-water-webview2/webroot, native-shell/uos-water-pyqt6/webroot
perc    canonical = android-build/perc-v13/assets
        mirrors   = native-shell/uos-pyqt6/webroot,
                     native-shell/win-webview2/publish_win_perc/webroot, native-shell/win-webview2/webroot
gujian  canonical = android-build/gujian-v31/assets
        mirrors   = native-shell/win-gujian-webview2/webroot, native-shell/uos-gujian-pyqt6/webroot
```
> 排除规则：`.bak/.ctxbak/.menu1bak` 后缀、`ai_seed.js`（本地密钥注入，不进镜像）、文件名含 `bak`。

### 4.3 三图核心差异（绝不能 1:1 复制代码）
- **组织层级 key 命名**：水利 `ju/guanchu/suo/zhan/duan`（局/管理处/所/站/段，默认 京密引水管理处）；古建 `spot/area/sub/point/tag`（景区/园区/子景点/打卡位/标签）。
- **核心实体 + 类型表**：水利=水工建筑物（含泵站/溢洪道）；感知=感知设施设备（独有 `subsystem` 子系统 + `equip_type` 设备类型，四类子系统：视频监控/雨水情/大坝安全/地下水源）；古建=古建景点（17 类：古塔/寺庙/城墙/古桥/园林/陵墓/书院/会馆/故居/古街/楼阁/亭台/坛庙/宫殿/石窟/衙署/其他）。
- **AI 系统提示词 / ZIP 匹配词表 / CANONICAL_TYPES 必须按核心实体差异化**，古建智能查询联网核实公开文物（优先文物局/UNESCO/世界遗产委员会），水利/感知保持本地辅助。
- **管理所归一化**：水利 `normOffice()` 统一 9 所（地下水源所/温泉所/龙山所/史山所/埝头所/水库所/北台上所/西田各庄所/潮河所）；`潮河总干渠→潮河所`、`怀柔水库所→水库所`。

---

## 5. 标准构建与发版流程

> 以下为发一个新版本（例：v3.46 → v3.47）的标准动作。任何新 `bump_version_vXXX.py` 必须**克隆自 `bump_version.py`**（已含自动 sync），不得手写窄清单。

**Step 1 — 改源码**：在 `android-build/<产品>/assets/` 改 `app.js/index.html/ovobj_bridge.js/ai_module.js/ctx_menu.js` 等。
```bash
node --check app.js          # 改完必跑，语法零错误
```

**Step 2 — 版本八处同步**：
```bash
python3 D:/Users/Claw/bump_version_v347.py     # 改 version.json + app.js + 6 webroot + deb/msi/ios 硬编码串，自动 subprocess 调 sync
```

**Step 3 — 镜像一致性门禁（铁律，退出码 0 才可继续）**：
```bash
python3 D:/Users/Claw/sync_all_mirrors.py --check   # 必须退出 0（覆盖三产品所有镜像）
```

**Step 4 — 构建 Android APK**（每产品）：
```bash
cd D:/Users/Claw/android-build/shuili-v329 && bash build_apk.sh   # 含版本门禁 + badging + apksigner
# 感知/古建同理（perc-v13 / gujian-v31）
```

**Step 5 — 构建 UOS deb + 门禁**（需在统信 UOS 真机或等效构建链）：
```bash
python3 D:/Users/Claw/native-shell/make_deb.py ...
python3 D:/Users/Claw/native-shell/verify_myml_ar.py <包.deb>   # 必须 3/3 PASS
```

**Step 6 — 四端打包驱动**（APK 已签名后）：
```bash
bash D:/Users/Claw/build_four_ends.sh "D:/Users/Claw/APK归档/四端安装包_<日期>_v347"
```

**Step 7 — 归档 + 文档**：产物入 `APK归档/四端安装包_<日期>_vXXX/`；更新 `docs_shuili_v3/版本变更记录_v3.XX.md` 与 `05_版本历史与备份.md`。

**Step 8 — 小程序对齐**（如适用）：`jingyin/` 三处 baseline 同步到新版本号。

---

## 6. 铁律与致命坑（避坑清单 —— 踩过 6 次分叉 + 多版 deb 失败）

1. **镜像分叉是头号杀手**：交付/归档任何产物前，**必须** `python3 sync_all_mirrors.py --check` 退出 0。任何新 `bump_version_vXXX.py` 的镜像清单须与 `sync_all_mirrors.py` 映射一致。**不要手写窄清单**（已因此分叉 6 次）。
2. **UOS deb ar 头铁律（致命）**：成员名**空格填充** `name.ljust(16," ")`（绝不 NUL）；mode 必须 `100644`（含 `100` 前缀）；tar 用 **xz**（`lzma.FORMAT_XZ`+`CHECK_CRC64`+`tarfile.GNU_FORMAT`，避 PAX 头）；`make_deb.py` 须显式写父目录条目（`collect_dirs`），否则 dpkg `.dpkg-new` ENOENT 报"请检查deb是否损坏"；`Depends: python3` 即可，绝不硬依赖 `python3-pyqt6.qtwebengine`（UOS 仓库无此包名），运行时 try/except 回退浏览器壳。交付前 `verify_myml_ar.py` 必过。
3. **cp 失误是最大隐性 bug**：跨产品同步时曾把水利 `app.js` cp 到感知/古建，导致装个空架子或 build_apk.sh FATAL。sync 后**必须加 cp-error 校验**（检测 `APPNAME/LS_KEY/domain/fieldSchema/data.js` 是否与工程名匹配）。
4. **AI 联网策略不可错配**：水利/感知=本地辅助（内部资料，禁止联网编造）；古建=联网核实公开文物。`ai_module.js` placeholder 自动按 `CFG.appName.indexOf("古建"/"感知")` 适配。
5. **密钥纪律**：天地图 Token、OpenRouter Key **绝不硬编码进共享源码**。`ai_module.js` 通过本地 `ai_seed.js` 注入个人 Key（该文件排除在镜像同步外）。天地图内置默认仅存 `TOKEN_BROWSER_DEFAULT` 不展示明文。
6. **用户自换密钥入口**：设置菜单「🗝️ 修改/添加天地图密钥」三层回退 `localStorage → window.TIANDITU_TOKENS → 内置默认`，`refreshTdtTileLayer()` 立即生效；32 位十六进制校验；复制当前密钥需密码 `3305`。
7. **前端兜底**：改完 `node --check app.js`；挂 `window.onerror` + `unhandledrejection`（已植入 11 个活跃 webroot）；IIFE 局部函数用 `window.xxx=xxx` 暴露给内联 `onclick`，否则离线 WebView 报 `Script error @0:0`。
8. **zip 最长匹配**：关键词匹配必须**每层内按 token 长度倒序**（`arr.sort((a,b)=>b.length-a.length)`），否则"温泉管理所"先于"史山管理所"误命中"史山"前缀。案例：`史山.zip → 史山所 → IMG-6857.jpg → 三扬分水闸`。
9. **真源冲突**：`app.js` 的 `APP_VERSION` 与 `version.json` 必须一致，由 `build_apk.sh` 版本门禁兜底校验；travel/android 是古建真源，勿把水利 assets 当古建 canolical。

---

## 7. 密钥与环境变量

| 项 | 机制 | 位置 |
|----|------|------|
| 天地图 Token（浏览器端/服务端） | 设置菜单自换 + 三层回退 + 内置默认 | `index.html` 的 `window.TIANDITU_TOKENS` / `TOKEN_BROWSER_DEFAULT` |
| OpenRouter API Key | 本地 `ai_seed.js` 注入（不进共享源码），默认账号 `openclaw626` | `ai_seed.js`（各产品 webroot 本地，已被 sync 排除） |
| Android 签名 | `android-build/<产品>/keystore.jks`（同目录） | 随工程 |
| 复制密钥密码 | `3305`（水利/古建/感知统一） | 设置菜单逻辑 |

> ⚠️ 真实密钥值不写入本记录、不硬编码进 `ai_module.js`/`app.js`。接手人需向炎冰索取当前密钥或引导其在 App 内自助配置。

**构建环境依赖**：
- JDK17 + Android SDK `build-tools 34.0.0`（`C:/Users/admin/.workbuddy/android-sdk`，零 Gradle）
- Python3（bump/sync/deb 工具）
- Node.js（语法校验）
- NSIS（Win 安装器，可选，回退 7z SFX）
- 7z（可选）
- 统信 UOS 构建链（mips64el，需真机/等效容器；本 Windows 会话无 Loongson 链，未在此编译验证）
- .NET 8 SDK + WebView2 Runtime（Win 构建，需目标平台）

---

## 8. 工具脚本速查

| 脚本 | 用途 |
|------|------|
| `bump_version.py` | 版本八处同步（克隆模板，含自动 `sync` 调用） |
| `sync_all_mirrors.py` | 三产品 webroot 多副本对齐（`--check`/`--dry`/`--实际`） |
| `build_four_ends.sh` | 三产品四端打包驱动 |
| `android-build/<产品>/build_apk.sh` | APK 构建 + 版本门禁 |
| `native-shell/make_deb.py` / `build_uos_deb.py` | UOS deb 构建 |
| `native-shell/verify_myml_ar.py` | deb 安装门禁（必跑） |
| `native-shell/build_win_exe_nsis.py` / `gen_win_msi_wix7.py` | Win 安装器 |
| `multi-platform-app-pack/build_apple_zip.py` | iOS PWA ZIP |
| `gen_covers_pil.py` / `gen_gujian_covers.py` / `gen_v345_cover.py` | 封面图生成 |
| `_apply_fixes_v347.py` | 下版本(v3.47)预热补丁（进行中） |

---

## 9. 微信小程序（jingyin）

- 工程 `D:/Users/Claw/jingyin/`，从 Android V3.15→V3.24 迁移，baseline 对齐到 **v3.46**（仅版本号+样式基线，功能未全量移植）。
- 已完成：核心框架（`app.json/app.js/config/config.js`）、地图页（`pages/map/`）、查询/筛选圈选逻辑、规范类型清单。
- **未完成（待接手）**：`utils/dialogHandler.js`（离线 `confirm→wx.showModal` 封装，P0）、筛选实时计数 badge、详情弹窗、`pages/index`（列表视图）、离线存储模拟、v3.7 圆形图标风格。
- 关键文档：`jingyin/README_微信小程序.md`、`jingyin/migration_report_V315_to_V324_微信小程序.md`、`jingyin/experience_summary_v3.15_to_v3.24.md`。
- AppID 未提交/未配置（需炎冰提供小程序账号）。

---

## 10. 待办与已知问题

- [ ] **v3.47 预热中**：`_apply_fixes_v347.py` + `_phaseB_pre_patch_20260824_v347.zip` 已落地，下版本可能进入 v3.47，待确认需求后走 §5 流程。
- [ ] 微信小程序功能未全量对齐（见 §9），dialogHandler 封装为最高优先级。
- [ ] `bump_version.py` 实际仅 `copy2 version.json+app.js` 到部分镜像（窄清单），**真正对齐靠末尾的 `subprocess sync_all_mirrors.py`**；新脚本务必沿用此"bump 后强制全量 sync"范式。
- [ ] Windows / UOS 原生壳**未在本环境编译验证**（无 .NET SDK / Loongson 链），需在目标平台首构建并验证。
- [ ] 历史分叉根因已通过 `sync_all_mirrors.py` 双保险（脚本内置 + 事后兜底）杜绝，仍建议每次发版前手动跑一次 `--check` 复核。

---

## 11. 交接清单

### 环境 / 账号
- [ ] WorkBuddy 连接器：`agent-mail`（智能体邮箱 `qclawXXXX@agent.qq.com`，已连接）、`github`（已连接）；其余按需连接。
- [ ] Android 签名 `keystore.jks`（位于各 `android-build/<产品>/`，勿外泄）。
- [ ] 天地图 Token / OpenRouter Key：向炎冰索取或在 App 内自助配置（**不硬编码**）。
- [ ] 微信小程序 AppID：向炎冰索取（jingyin 未配置）。

### 文档 / 知识库
- [ ] `docs_shuili_v3/`（需求/设计/变更/版本历史 13 份）
- [ ] `C:/Users/admin/.workbuddy/skills/shuili-yitu-app/references/pitfalls.md`（22 条坑，唯一真相源）
- [ ] `C:/Users/admin/.workbuddy/MEMORY.md`（三图联动升级规范 / 出包铁律 / 分叉复盘）
- [ ] 本文件 `项目交接记录.md`

### 首构建验证（建议接手后立即执行一次）
- [ ] `python3 sync_all_mirrors.py --check` 退出 0
- [ ] `node --check android-build/shuili-v329/assets/app.js`（及 perc/gujian）
- [ ] `bash android-build/shuili-v329/build_apk.sh` 出包成功 + `apksigner verify` 通过
- [ ] `python3 native-shell/verify_myml_ar.py <水利 deb>` 3/3 PASS

---

*本文档由 小七（AI 万能助理）根据项目实际结构与 `MEMORY.md` / 各版本变更文档整理，供项目交接使用。最后更新：2026-08-31。*
