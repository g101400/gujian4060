# 双通道奇偶发版与信息脱敏策略（水利 / 感知，古建不变）

> 背景：2026-09-03 用户提出「为防止重要信息泄密，水利和感知分两个情况发版，古建不变」。
> 本文件是**分析结论 + 同步规则 + 构建流程**的单一真相源，配套 T-049 台账与 `strip_internal.py` / `propagate_ai_seed.py` / `bump_version_v350.py` 三件套脚本。

## 一、诉求拆解

| 版本 | 通道 | 数据策略 | 程序标注 | 用途 |
|---|---|---|---|---|
| **偶数版**（如 3.50 / 1.26） | 内部版 | 保留真实水利建筑物 / 感知设备信息 | 程序标明「内部版」 | 单位内部使用 |
| **单数版**（如 3.49 / 1.25） | 公开测试版 | 删除水利建筑物 / 感知设备信息 | 程序标明「公开测试版」 | 公开使用 / 测试 |
| 古建（恒 3.7） | — | 不变 | 不变 | 公共受众，不参与双通道 |

**铁律起点**：奇数版绝不能包含任何单位内部坐标、管理所、设计参数、私有 API Key。

## 二、敏感信息面分析（可能泄密的单位内部载体）

经全量梳理，三图家族里「单位内部信息」只集中在两个种子级载体，**其余全部级联派生**：

| 载体 | 是否泄密源 | 内容 | 单数版处理 |
|---|---|---|---|
| `kb_building_seed.js` | **核心泄密源** | 557 条真实水利建筑物 / 1125 条真实感知设备，含**精确 CGCS2000 坐标 + 管理所 + 设计参数**（桩号/设计流量/闸门型号/子系统） | 换为 `kb_building_seed.demo.js`（10 条虚构演示） |
| `ai_seed.js` | **密钥泄密源** | 真实 OpenRouter 私有 API Key（`sk-or-v1-…`） | 换为 `ai_seed.demo.js`（`apiKey: ""`） |
| `data.js` | **主渲染数据泄密源** | `window.SHUILI_DATA`/`window.PERCEPTION_DATA`，APP 主渲染数据集，含 407（水利）/1125（感知）条**真实 CGCS2000 坐标 + 真实建筑/设备名**（龚庄子进水闸/温泉所/怀柔水库/口头水文站/向阳闸/长副坝…）；条数最多、最易被漏判 | 换为 `data.demo.js`（557/1125 条虚构演示，坐标 ~116.0/39.8、名前缀「演示」，**保留条数避免计数守卫崩**） |
| 照片（种子 `photos` 字段） | 级联 | 依附于真实建筑/设备的现场照片 | 剥离种子即无照片，自动干净 |
| AI 组织上下文 `orgCtx` / `systemPrompt` | 级联 | `ai_module.js` 注入，基于 KB 本地数据作答；已含「禁止编造内部参数坐标」边界 | 单数版只换密钥，AI 边界不变（shuili/perc `allowOnlineQuery` 已为 false，内部资料不联网编造） |
| 导出 CSV / kmz | 级联 | 仅导出「当前已加载」的数据 | 剥离种子后加载即演示数据，导出自然无内部信息 |
| `version.json` desc / 关于页 / 功能介绍 | 非涉密 | 版本叙述、功能清单，不含坐标密钥 | 不变 |

**结论（修正·2026-09-03）**：单位内部信息集中在 **`kb_building_seed.js` + `ai_seed.js` + `data.js` 三个种子级载体**（原 T-049 分析漏判 `data.js`，本次修复）。三者均在单数版替换为演示/空值即一次性切断全部泄密路径。`data.js` 是 APP 主渲染数据集、条数最多（407/1125），剥离优先级最高——漏判它曾导致首轮公开版整包泄露真实坐标（已回溯重出）。strip_internal.py 的 TARGETS 现为三者齐全。

## 三、同步规则（单数版智能脱敏规则）

1. **奇偶通道由版本号自动判定**（运行期，非构建期硬编码）：
   `getReleaseChannel()` 解析 `APP_VERSION` 末段数字，`last % 2 === 0 → internal`，否则 `public`。
   故「单数版=公开测试版 / 偶数版=内部版」是**版本号奇偶的自然结果**，加版本即自动切换通道，无需人工记通道。
2. **UI 三重标注**（shuili / perc canonical app.js 已落地）：
   - 关于页新增「发布通道」行：`内部版（含完整单位数据）` / `公开测试版（不含单位内部数据）`；
   - 启动 `logRuntime()` 后：`public` 通道弹 toast「📢 公开测试版 · 不含单位内部数据」；
   - 功能介绍页末尾追加公开/内部说明段。
3. **构建期剥离规则**（见第五节）：单数版出包前 `strip_internal --public`，偶数版出包前 `--restore` 还原真实。
4. **级联干净规则**：单数版只换种子，照片/AI上下文/导出自动干净；禁止在单数版手动保留任何真实坐标/管理所/密钥。

## 四、奇偶通道机制落地（shuili v3.50 / perc v1.26，gujian 不变）

`android-build/shuili-v329/assets/app.js` 与 `android-build/perc-v13/assets/app.js` 注入：

```js
function getReleaseChannel() {
  try {
    var seg = String(APP_VERSION || "").replace(/^v/i, "").split(".");
    var last = parseInt(seg[seg.length - 1], 10);
    if (isNaN(last)) return "internal";
    return (last % 2 === 0) ? "internal" : "public";
  } catch (e) { return "internal"; }
}
var RELEASE_CHANNEL = getReleaseChannel();
window.SHUILI_RELEASE_CHANNEL = RELEASE_CHANNEL;   // perc: window.PERC_RELEASE_CHANNEL
```

- `node --check` 双端全过。
- 抽包核验：单数 APK `RELEASE_CHANNEL` 机制存在且 `channel=public`；偶数 APK `channel=internal`。

## 五、防泄密铁律（构建期，T-049 固化）

> 这些坑是本次真实踩过并修复的，任何双通道出包必须遵守。

1. **铁律-备份外置**：真实种子与密钥备份**必须放在 assets 目录之外**（`.strip_backup/<product>/`）。`sync_all_mirrors.py` 会把 assets 目录**全量**复制到所有 webroot/mirror，若把 `*.real.bak` 留在 assets 内，会被复制进 webroot 再打进包 → **泄密**。
2. **铁律-ai_seed 手动传播**：`sync_all_mirrors.py` 有 `EXCLUDE_NAMES={"ai_seed.js"}`（密钥按构建管理、不同步），故还原/剥离后必须手动跑 `propagate_ai_seed.py <shuili|perc>` 把（已还原/已剥离的）`ai_seed.js` 同步到 7 个 webroot，否则 webroot 残留旧密钥。
3. **铁律-发布子目录版本**：`publish_win_*/webroot` 不在 `sync_all_mirrors` 目标列表（sync 只覆盖根 webroot），Win MSI/EXE 构建门禁会校验 `publish_win_*/webroot/version.json` 与期望版本一致。若 sync 后发布子目录版本落后，需手动 `patch_versionjson` 或 `bump_version_*.py` 一并覆盖，否则**拒包**。
4. **铁律-出包前必抽包核验**：
   - 单数版：APK/deb/iOS 内 `data.js` 与 `kb_building_seed.js` **均**含「演示」标记、`龚庄子/温泉所/西田各庄/怀柔水库/口头水文站/向阳闸/长副坝` 等真实名**全 0**、`ai_seed` 无 `sk-or-v1`、`RELEASE_CHANNEL` 机制存在且 `channel=public`。
   - 偶数版：APK/deb 内真实名**存在**、`ai_seed` 含真实密钥、`channel=internal`。

## 六、双通道构建流程（四端全形态）

前置：canonical assets 为唯一真相源，构建脚本对 assets **原地操作**；`sync_all_mirrors.py` 把 canonical 全量复制到 9 个 webroot/mirror。

```
单数版（公开测试版）流程：
  strip_internal.py <canonical> --public      # 真实→demo，备份到 .strip_backup/<product>/(assets 外)
  bump_version_v349.py                        # 版本号提升 + sync --real/--check
  propagate_ai_seed.py shuili|perc            # 空密钥同步进 webroot
  → 出包（deb×4 + MSI + EXE + iOS zip + APK）
  strip_internal.py <canonical> --restore     # 还原真实，供下一轮偶数版

偶数版（内部版）流程：
  strip_internal.py <canonical> --restore      # 还原真实种子+密钥
  bump_version_v350.py                         # 3.49→3.50 / 1.25→1.26，gujian 3.7 不变；sync --real/--check
  propagate_ai_seed.py shuili|perc            # 真实密钥同步进 webroot
  → 出包（deb×4 + MSI + EXE + iOS zip + APK）
```

四端形态：安卓 APK（`build_apk.sh`）/ Win MSI（`gen_win_msi_wix7.py`）+ EXE（`build_win_exe_nsis.py`）/ UOS deb（`make_deb.py`，4 架构 mips64el/amd64/arm64/loongarch64）/ iOS PWA zip（`build_ios_zip.py`）。古建四平台公开版数据/版本不变、AI 凭证已置空（回溯重出，非原样复用含密钥旧包）。

## 七、交付物清单（2026-09-03）

| 通道 | 归档目录 | 件数 | 内容 |
|---|---|---|---|
| 单数（公开测试版） | `APK归档/四端安装包_20260903_v349_pub/` | 24 | 3 APK(3.49/1.25/3.7) + 12 deb + 3 MSI + 3 EXE + 3 iOS；全量抽包复扫**水利/感知零真实名·零真实密钥·含演示标记**（含 data.js 修正后回溯重出）；古建按「不变」原样复用（数据/版本不变），**其公开版 ai_seed 凭证已置空**：gujian 公开 APK/deb×4/MSI/EXE/iOS 全部回溯重出为 demo 空密钥，复扫零真实密钥，内部版源保留真实密钥 |
| 偶数（内部版） | `APK归档/四端安装包_20260903_v350_int/` | 24 | 3 APK(3.50/1.26/3.7) + 12 deb + 3 MSI + 3 EXE + 3 iOS；已抽包核验**有真实名/有真实密钥/通道=internal** |

**分发纪律**：单数版可公开/测试；偶数版仅限单位内部，不得外发。两版程序均已在关于页标明通道，运维/接收方一眼可辨。
