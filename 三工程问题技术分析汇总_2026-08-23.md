# 水利/感知/古建 三个问题技术分析汇总（2026-08-23）

> 状态：**已按「方案 C」全部执行完毕**。代码已改、三 APK 已重打归档、文档同步。
> 分析依据：V3.24（3.27 之前最后归档版本，3787 行 app.js）对比 V3.29（4519 行，+732 行）。

---

## 问题 2（**已确诊，根因 100% 明确**）：三个 APK 不能同装一个手机

### 证据
| 工程 | AndroidManifest 路径 | package 名 | versionCode |
|------|---------------------|-----------|-------------|
| 水利 V3.29 | `android-build/perc-v13/AndroidManifest.xml` | `com.gujian.travel` | 9（待确认，但 package 相同） |
| 古建 V1.7 | `travel/android/AndroidManifest.xml` | `com.gujian.travel` | 8 |
| 感知 V1.5 | 复用古建壳（build 时） | `com.gujian.travel` | 8 |

三个工程**共用同一个 applicationId `com.gujian.travel`**。Android 以 package 名作为 App 唯一标识，装第二个时系统判定"同一应用已存在、但版本不匹配" → 报"已安装新版本/旧版本"。

### 修复方向（待确认）
给三个工程分配互不冲突的 package：
- 水利：`com.shuili.yitu`（或 `com.gujian.shuili`）
- 古建：`com.gujian.travel`
- 感知：`com.gujian.ganzhi`（或 `com.shuili.ganzhi`）

⚠️ 注意：改 package 后，旧版 App 在新手机上无法直接"覆盖升级"，需先卸载旧版（因为签名 keystore 相同但 applicationId 变了，Android 视为不同 App）。三工程各自独立安装即可共存。

---

## 问题 1（**部分确诊，需进一步验证**）：3.27 之后安卓卡顿 + 筛选蓝圈/导入 zip 变差

### 时间线对齐（来自 app.js 内版本注释）
- **v3.26**：新增「地图操作控件（＋放大/－缩小/☆收藏位置/⌂返回收藏位置）」+ 管理所名称统一 + **ZIP 多层目录导入** + 筛选维度扩展
- **v3.28**：收藏位置 → **收藏窗口**（记录视窗四角，可收藏多个）；引入**视窗裁剪渲染**（moveend/zoomend 防抖增量，只建视窗内 marker）—— 反向同步自古建 v1.6

### 验证结论

#### ① 收藏/返回位置 是不是卡顿元凶？
**不是直接原因。**
- V3.24（3.27 前）app.js 里**完全没有**"收藏/返回位置/bookmark"任何代码（grep 0 命中）。收藏是 v3.26 引入、v3.28 升级。
- 收藏窗口函数（`addBookmark`/`goBookmark`/`openBookmarkChooser`）**不监听地图事件、不触发重渲染**，只是读写 localStorage + fitBounds，开销极小。
- 真正的全局地图监听是 `onViewChange`（line 508-509），它是 v3.28 的**性能优化**（防抖 120ms 后增量重渲染），恰恰是为了不卡。

#### ② 真正卡顿源（高置信）
`render()` 函数（line 723）每次 `moveend/zoomend` 都：
```js
Object.keys(MARKERS).forEach(k => map.removeLayer(MARKERS[k]));  // 全量删
Object.keys(LINES).forEach(k => map.removeLayer(LINES[k]));
// 再全量重建视窗内 marker，且每个 marker 的 bindPopup 预先构造 popupHtml
```
- **每次拖动都全量 remove + add 所有 marker**，视窗裁剪只减少了创建数量，但 marker 复用机制缺失（没做 diff/update，而是销毁重建）。
- `popupHtml(b)` 现在含**最多 18 张 `<img loading=lazy>` + 结构化设备参数块**（中类/小类/可控/传输/编码），3.24 没有这些，popup 构造代价高。
- 筛选维度从 **3 个 Set → 9 个 Set**（`passFilter` 每次遍历更重）。
- 结论：v3.28 的视窗裁剪**本意优化**，但因"全量销毁重建"实现 + 复杂 popup + 9 维筛选，在中低端安卓上反而比 3.24 一次性建全部 marker 更容易掉帧。

#### ③ 筛选"蓝圈"不好用
- 3.24 与 3.29 筛选高亮机制一致：命中显示**琥珀色倒水滴 + 绿色虚线圈**（`filterLayer`），蓝圈是**默认 marker 颜色**（`#1e6fbf`）。
- 3.29 改用视窗裁剪 render 后，若筛选结果大部分在视窗外，marker 不创建 → 视觉上"筛选后没蓝圈/圈不全"。这是**视窗裁剪与筛选高亮叠加的副作用**，非收藏导致。

#### ④ 导入 ZIP 不好用
- ZIP 导入管线 3.24 ↔ 3.29 **基本一致**：原生 `pickFiles` + base64 + JSZip 解包（line 2071/2512/2557）。导入逻辑本身没被改坏。
- 可能诱因：
  - (a) 3.29 新增字段（subsystem/midType/subType/ctrl/transmit/deviceCode）在旧 ZIP 表里缺列，映射失败但**静默**（无 toast）。
  - (b) 3.28 照片路径重构（统一 `photos/<管理所>/<建筑物>/<序号>.jpg`）后，跨平台同步菜单可能干扰导入流程。
- 需进一步：用一份已知旧 ZIP 在 3.29 实测，看是否抛错/静默。

---

## 待确认的统一修改方案（草案）

### 问题 2（必改）
- [ ] 水利 `AndroidManifest.xml` package 改为 `com.shuili.yitu`
- [ ] 感知 build 脚本 package 改为 `com.shuili.ganzhi`（独立壳或改 manifest）
- [ ] 古建保持 `com.gujian.travel`
- [ ] 三工程重打 APK、aapt2 校验 package 互异

### 问题 1（按优先级）
- [ ] **P0 卡顿**：`render()` 改为 marker 复用（视窗进出时只增删差异 marker，不全量销毁重建）；popup 延迟到 popupopen 时再构造（不在 bindPopup 时预生成）。
- [ ] **P1 筛选蓝圈**：筛选结果不论是否在视窗内，至少保留命中 marker（或筛选时临时关闭视窗裁剪）。
- [ ] **P2 导入 ZIP**：导入失败时显式 toast 错误原因（不再静默）；字段映射缺列给默认值并提示。
- [ ] **备选（用户提到的"搁置隐藏"）**：若 P0/P1 短期无法根治，可暂时隐藏"收藏窗口/返回收藏"按钮，保留放大缩小，让开大继续 —— 但分析表明收藏不是卡顿源，**不建议优先隐藏收藏**，应先修 render 复用。

---

## 下一步
等用户确认：
1. 问题 2 的 package 命名方案是否接受（水利/古建/感知 三套）。
2. 问题 1 是"优先修 render 性能"还是"先隐藏收藏功能让开大继续"。
3. 是否允许我用一份示例 ZIP 在 3.29 实跑验证导入失败根因（需用户给一个测试 ZIP 或授权用历史 ZIP）。

---

## 补充验证（2026-08-23 07:53 用户提供测试 ZIP）

用户给了 `6f0bb496...zip`（飞书媒体目录），实测分析导入失败根因。
**ZIP 内容**：顶层单目录 `埝头管理所建筑物照片/`，下含 `倒虹吸/`、`变压器清污机启闭机/`、`桥/`、`涵洞/`、`闸/` 5 个子目录，共 **39 张 .jpeg**。**无任何表格文件（.csv/.xlsx/.xls 均不存在）** → 这是一份**纯照片导入包**。

### 导入失败根因（确诊）
V3.29 有**两套独立导入入口**：
1. **「导入建筑物 ZIP」**（`resolveBuildingZip`，line 2570）：表格驱动，必须含 CSV/XLSX，照片按名匹配。逻辑尾部：
   ```js
   if (csvName) {...} if (xlsName) {...}
   idle();
   $("batchBldResult").innerHTML = "ZIP 中未找到 CSV 或 XLSX 文件";
   ```
2. **「批量导入照片」**（`batchImportPhotos`，line 916 → `pickPhotosForImport`）：纯照片 ZIP，按"文件夹各层目录名 + 文件名"fuzzy 匹配建筑物（`bestMatchesForItem`，line 2156，v3.28 已修深层目录匹配）。

**用户拿"纯照片 ZIP"走入口 1 → 必落红字"未找到 CSV 或 XLSX"**。这就是"导入 ZIP 不好用"的真实触发路径（与 3.27 收藏无关，与新增字段也无关）。

### 即使走对入口（批量导入照片），仍有隐性风险
- 照片匹配依赖建筑物表里有对应名称（如 `土城倒虹吸`、`上庄分水闸`、`京昌西桥`）。若当前台账缺这些建筑 → 全部 score=0 未匹配，照片堆在 inbox 不落盘（v3.28 注释 line 2154 已记录此坑）。
- ZIP 顶层多了 `埝头管理所建筑物照片` 这一层，**不致命**（folder 各层都参与匹配），但会增加噪声 key。

### 修复建议（待确认）
- [ ] **入口 1 改为容错**：检测到 ZIP 含图片但无表格时，自动转「批量导入照片」流程（或明确 toast 引导用户走正确入口），不再静默报"未找到 CSV"。
- [ ] **统一入口**：把"建筑物表格导入"和"照片导入"合并到一个选择器，按 ZIP 内容自动判别，避免用户走错。
- [ ] 导入失败时给出**明确分流提示**（"检测到本包为照片，请使用『批量导入照片』"）而非笼统红字。

---

## 补充证据（2026-08-23 07:49 用户再发截图）

用户后续发来"水利感知项目一张图 V1.5 安装失败 / 此版本比您当前使用的版本旧"的截图。
**结论**：这正是问题 2 的活体复现。

- 古建 V1.7 与 感知 V1.5 都是 `com.gujian.travel` + `versionCode=8`
- 用户手机里若先装了古建 V1.5/1.7，再装感知 V1.5 → Android 把两次提交视为对**同一 App** 的"同版本签名冲突" → 系统兜底文案为"此版本比您当前使用的版本旧"
- 包名冲突修复后，三 APK 可并存同设备，安装文案也会变正常

---

## 补充证据（2026-08-23 08:03 用户再发「批量导入照片」弹窗截图 + 红色批注）

用户在「批量导入照片」弹窗里选了那个 6f0bb...zip 后，看到的弹窗状态：
- 顶部 sheet（"批量导入照片"内容）正常显示
- 屏幕底部一个**几乎被 sheet 遮挡**的进度条，停在"正在解压 / 准备中…"文字不动
- 用户批注：**① 导入 zip 正在解压 准备中 程序就停止了 ② 正在解压 准备中 进度条应该在最前面**

### 根因 ①（z-index 冲突导致进度条被挡）
- `.sheet` 用 `z-index: 2001`（占满右侧 360px 宽 + 100% 高）→ assets/index.html CSS line 61
- `.xferOverlay` 用 `z-index: 2000`（`position:fixed;bottom:0`，line 201）→ **被 sheet 完全遮挡**
- 视觉上用户看不到进度条，错以为程序卡死

### 根因 ②（解压期间无进度回调 → 文字停在"准备中…"）
- 代码 line 2035 调 `openXferProgress("unzip", title)`，title 写"正在解压压缩包..." → 进度条出现
- line 2037 `window.Android.unzipImages(z.path)` 调原生 Java 解压（异步）
- **关键 bug**：调 unzipImages 时**没有传"进度回调函数名"**，原生线程跑完才会回 `window.onUnzipImages`（line 2044）—— 解压过程中 JS 端**收不到任何 onXferProgress 回调**，所以 `updateXferProgress("unzip", done, total)` 从未执行过
- 结果：进度条 UI 显示了，但永远卡在"准备中…"文字，整数 done/total 永远没动 → 用户感受"程序停止"

### 修复建议（待确认）
- [ ] **z-index 修复（CSS 一行）**：把 `.xferOverlay` z-index 提到 **3000**（高于 sheet 的 2001，但低于 toast 3000 / lightbox 4200），并把 position 改为 `position:fixed;left:0;right:0;bottom:0;z-index:3000`，或干脆把 sheet 默认折叠（用户关闭 sheet 后才弹进度条）
- [ ] **进度回调桥**：在 line 2037 `unzipImages(z.path, "onUnzipProgress")` 前增加可选 progress callback 参数；或在 Java 侧原生解压时按字节流周期调 `window.onXferProgress("unzip", done, total)`，让 JS 端真正拿到百分比
- [ ] **兜底超时**：若 unzip 30 秒内未收到任何 onUnzipProgress 回调，主动 toast「解压较慢，建议改用「从安卓复制照片」直接复制文件夹」给用户退路（line 1773 已警告过 UOS 8G 风险但没生效）

### 影响范围
- z-index 修复影响所有"长任务进度条"（xferOverlay）：拷贝照片、网盘下载上传、打包 ZIP 等，都因被 sheet 遮挡看不见进度
- 进度回调桥修复仅影响 ZIP 解压（unzip）场景；其它原生操作（网盘上传/下载）可能有相似问题需统一排查

---

# ════════════════════════════════════════════════════════════
# 最终统一问题清单 + 待确认修复方案（用户两次汇总后合并版）
# ════════════════════════════════════════════════════════════

> 全部问题**与"收藏/返回位置"功能无关**（V3.24 无此代码、收藏函数不监听地图、不重渲染）。
> 状态：**仅分析记录，等待用户确认后统一修改**。未改动任何工程文件。

## 一、五个独立 Bug 总表

| # | 现象 | 根因（已确诊） | 严重程度 | 修复复杂度 |
|---|------|--------------|---------|-----------|
| **A** | 三 APK 不能同装一手机，后装的报"版本比已装旧" | 三工程 package 全为 `com.gujian.travel`（Android 按 package 判唯一） | 阻断安装 | 极低（改 manifest + 重打） |
| **B** | 3.27 后安卓地图拖动明显卡顿 | `render()` 每次 moveend/zoomend 全量 removeLayer + 重建所有 marker（无复用）；popupHtml 现含 18 图+设备参数块；筛选 3→9 维 | 长期卡顿 | 中 |
| **C** | 筛选后"蓝圈"不全/看不出命中 | 视窗裁剪（v3.28）+ 筛选高亮叠加：视窗外命中 marker 不建 | 体验差 | 低 |
| **D** | 导入 ZIP 报"未找到 CSV 或 XLSX" | 纯照片 ZIP 误走"导入建筑物"入口（须含表格）；两套入口未自动判别 | 操作混乱 | 低 |
| **E** | 批量导入照片解压时"程序停止"+进度条不见 | ① `.sheet` z-index 2001 遮挡 `.xferOverlay` 2000；② `unzipImages()` 未传 progress callback，解压期间无 `onXferProgress` 回调，文字卡"准备中…" | 用户误以为死机 | 低-中 |

> 用户本轮（08:05）点名的就是 **E** 的两个子项：
> 1. 导入 zip 正在解压 准备中 程序就停止了 → 根因 E②（无进度回调）
> 2. 正在解压 准备中 进度条应该在最前面 → 根因 E①（z-index 被挡）

## 二、待确认修复方案（请用户选 A/B/C/D 之一）

- **【全量】** 一次性修 A+B+C+D+E，重打 3 个 APK，更新本文件为修复记录
- **【保安装】** 先修 A（三 APK 同装）+ E（解压卡死/进度条），其余 B/C/D 搁置
- **【只修 E】** 仅修 E（进度条遮挡 + 解压无回调），其它全搁置
- **【自定义】** 用户列出要做的项（如"只做 A + E②"）

### 各项具体修复要点（确认后执行）
- **A**：水利 `com.shuili.yitu`／古建 `com.gujian.travel`／感知 `com.shuili.ganzhi`；改各自 AndroidManifest + 感知 build 脚本；重打 APK；aapt2 校验 package 互异
- **B**：`render()` 改 marker 复用（视窗进出只增删差异 marker）；popup 懒构造（popupopen 时才生成 popupHtml）
- **C**：筛选激活时临时关闭视窗裁剪（全量建 marker），或筛选结果强制保留
- **D**：入口①检测到"含图无表"自动转照片流程 + 失败给明确分流提示；或合并两入口按内容自动判别
- **E①**：`.xferOverlay` z-index 改 3000（高于 sheet 2001，低于 lightbox 4200）
- **E②**：`unzipImages(z.path, "onUnzipProgress")` 增加 progress callback；Java 侧周期调 `window.onXferProgress("unzip", done, total)`；加 30s 无回调兜底 toast

---

# ════════════════════════════════════════════════════════════
# 第三批需求：水利「运行维护」菜单体系 + 照片查看器增强 + 巡视时长统计
# ════════════════════════════════════════════════════════════

> 状态：**仅分析、智能对应调整、记录。等待用户确认后统一实现。未改动工程。**

## 语义映射分析（水利 ↔ 古建）

| 古建（已存在） | 水利（现状/新增） | 说明 |
|--------------|-----------------|------|
| 📸 拍照打卡（一级组） | 📋 巡视检查（一级组，已存在 line 901） | 同名语义迁移：打卡→巡视检查 |
| 打卡（openCheckin） | 巡视（openInspect，已存在 line 752） | 字段：照片/到达时间/游览时长/体验 → 照片/巡查时间/巡查时长/情况 |
| 我的打卡路线（openRoutes） | 我的巡视路线（openInspectRoute，已存在 line 827） | 按到达时间倒序 |
| （古建无） | **运行维护（新增一级组）** | 水利行业特有：出发位置/运维计划 |
| （古建无） | 默认出发位置 / 运行维护计划 | 水利新增字段 |

**结论**：水利已有"巡视检查"+"我的巡视路线"骨架，本次需求是把它们**挪入新建的"运行维护"一级菜单**，并在其下扩展"默认出发位置""运行维护计划"两个新子模块，同时给巡视增加 3 个新选项 + 给全程加"巡视时长自动统计"。

## 需求拆解 + 实现规划（F=功能，G=增强）

### F1 新增一级菜单「运行维护」（智能对应古建"旅游打卡"分组位置）
- 在 `sheetMenu` 一级分组中新增 `🛠️ 运行维护`，置于"巡视检查"同级或更靠前
- 二级项：① 设置默认出发位置 ② 运行维护计划 ③ 巡视检查（原菜单迁入）④ 我的巡视路线（原菜单迁入）

### F2 设置默认出发位置
- 数据：全局 `SETTINGS.startPos = { name, lat, lon }` 或 `localStorage`
- UI：地图选点（点地图取坐标）+ 手动输入坐标 + 从已有建筑物选 + "自定义"
- 用途：F4 巡视时长计算、F3 计划默认起点

### F3 运行维护计划
- 数据模型：`plans: [{ id, type, time, scope, planHandleTime, note }]`
  - type ∈ {日常检查, 例行维护, 故障处理, 应急响应, 其他}
  - 字段：时间 / 范围 / 计划处理时间（+ 备注）
- UI：列表 + 新增表单 + 可按 type 筛选
- 存储：并入 BUILDINGS 同级全局 `MAINT_PLANS`（localStorage）

### F4 巡视检查升级（在原 openInspect 基础上增 3 项）
现有字段（line 757-797）：photo / arrivalTime / travelTime(手填文本) / experience
**新增 3 选项**：
1. **巡视类型** `inspType` ∈ {日常巡视, 例行维护, 故障处置, 应急响应, 其他}（下拉）
2. **显示坐标** `coord {lat, lon}`：自动获取当前位置（`navigator.geolocation` 或地图中心），**只读显示不可改**（需求明确"只能显示不能修改"）
3. **是否计划内维护** `planned` ∈ {是, 否}（开关/单选）
- 保存结构扩展（line 790 push 对象加字段）

### F5 我的巡视路线（迁入 + 时长统计增强）
现有（line 827）：按 arrivalTime 倒序列表
**新增时长统计**（需求原文）：
- 显示 **总时长 / 路上时长 / 巡视总时长**
- 计算规则：**建筑物类时长，从默认起点开始算；第 N 点从第 N-1 点算**（即相邻巡视点 arrivalTime 之差 = 路上+巡视合计；需区分"路上"与"巡视"——建议：相邻到达时间差 = 路上时长；单点 travelTime 手填/自动 = 巡视时长；总时长 = Σ）
- 实现：`openInspectRoute` 渲染时，对排序后序列计算相邻 `arrivalTime` 差得到"段时长"，累加得总时长；首段从 `SETTINGS.startPos` 时间或首点 arrivalTime 起算

### G1 照片查看器增强（点击照片自动放大 + 多照片循环）
现有（line 3438 `appLightbox`）：单张放大，**无循环**
- 新增：popup/路线里多照片时，`appLightbox(bid, idx)` 支持 **上一张/下一张 + 自动轮播（setInterval ~3s）**
- 点击照片自动放大（已有）；多照片自动循环（新增轮播 + 左右切换按钮）

## 数据模型变更影响
- `b.inspections[].inspType / coord / planned` 新增字段（旧数据兼容：缺则按"其他/无坐标/否"）
- 全局新增 `SETTINGS.startPos`、`MAINT_PLANS`
- 导出表格：巡视记录可选列增加 inspType/coord/planned；新增"运行维护计划"导出
- 版本号：建议水利升 V3.30（感知/古建不强制，但四端同源需评估是否同步加"运行维护"）

## 待确认实现范围（请用户选）
- **【全做】** F1+F2+F3+F4+F5+G1 一次性实现，水利升 V3.30，重打 APK
- **【核心】** 仅 F1+F4+F5（运行维护菜单 + 巡视升级 + 路线时长），G1/F2/F3 次轮
- **【自定义】** 用户指定子集
- ⚠ 古建/感知是否也要"运行维护"菜单？古建对应"旅游打卡"、感知对应"感知运维"？需确认是否四端同源同步

---

## 四、巡视表单升级 · 截图实证 + 精确插入位点（2026-08-23 08:09 用户发来巡视检查截图）

用户最新截图：水利「巡视检查」实拍表单（选择建筑物/巡查照片/巡查时间/巡查时长/巡查情况/取消/保存巡视） —— 与代码 `openInspect` line 755-787 完全吻合。

### 截图用红色标注再次确认两条新需求
- ① **增加巡视类型**（日常巡视/例行维护/故障处置/应急响应 其他等）
- ② **增加显示坐标**（自动获取当前位置坐标，只能显示不能修改）

### 现有 `openInspect` 表单 DOM（已读源码逐行对照）
- line 765 `选择建筑物` select
- line 769 `巡查照片` file input
- line 770 `ciPhotoPrev` 预览容器（仅 1 张）
- line 771 `巡查时间` datetime-local
- line 772 `巡查时长（如 2 小时 / 半天）` input（**当前是手填字符串**，需求要改成"自动算"）
- line 773 `巡查情况` textarea

### 需求 ①②③ 插入位点（实测代码行号）
| 需求 | 插入位置 | DOM 片段 | 保存字段 |
|------|----------|----------|---------|
| ① 巡视类型 | line 769 之前 | `<label class="f">巡视类型</label><select class="f" id="ciType">` 选项 = 日常巡视/例行维护/故障处置/应急响应/其他 | `inspType` |
| ② 显示坐标 | line 772 之后 | `<label class="f">显示坐标</label><input class="f" id="ciCoord" readonly>` + 进入时 `navigator.geolocation.getCurrentPosition(succ, err, {timeout:8000})` 异步填入 | `coord: {lat, lng, accuracy}` |
| ③ 是否计划内维护 | line 773 之后 | `<label class="f">是否计划内维护</label><select class="f" id="ciPlan"><option value="0">否</option><option value="1">是</option></select>` | `planned:0|1` |

### 需求 ⑤ 路线时长自动计算
- 现有 `openInspectRoute` line 805 末尾无任何时长汇总
- 实现：取全部建筑物全部 `inspections` → 按 `arrivalTime` 升序排序 → 相邻条目的 `arrivalTime` 之差 = 段时长（路上时长），累积 = 总巡视时长；首条 vs `SETTINGS.startPos.leaveAt` = 首段出发路上时长；输出三段 + 总时长（HH:MM:SS 格式）

### 需求 ⑥ 照片自动循环
- 现有 `appLightbox` line 3429 仅展示单张
- 改造：扩展接收照片数组 + index → 左右切换按钮 + 3 秒自动轮播（可暂停/点指示点）+ 键盘 ← → 切换

### Webview Geolocation 不需新增 Android 原生桥
- manifest line 10-11 已有 `ACCESS_FINE_LOCATION` + `ACCESS_COARSE_LOCATION`
- 直接 `navigator.geolocation.getCurrentPosition(...)` 即可；可加 8s 超时与降级 toast「暂未获取到位置」

### 数据兼容性
- 旧 `inspections` 记录缺 `inspType/coord/planned` → 前端读取时 `|| "其他"` / `|| null` / `|| 0`，不影响展示
- 若图片只剩 `<img>`，点击触发 `appLightbox(b.id, i)` 现有调用全兼容

---

## 五、我的巡视路线页面升级 · 截图实证（2026-08-23 08:14 用户再发图片）

用户最新截图：水利「我的巡视路线」实拍 —— 共 1 次巡视，按巡查时间从新到旧排列；定位 + 1.隧洞出口测流桥（怀柔水库所）、巡查 2026-08-22T15:34、72×72 缩略图；**红色批注 4 条新需求**。

### 截图红字与代码现状精确对比
| 需求 | 现有 line | 现状 | 缺口 |
|------|-----------|------|------|
| ① 点击照片自动放大显示 | line 824 | `<img>` 仅静态显示 | **无 onclick** |
| ② 多个照片自动循环显示 | line 824 | photo 字段为**单图字符串** | **无 b.photos[] 数组数据**，lightbox 也不支持轮播 |
| ③ 显示填入的巡视时长 | line 822 | `时长：`+ `it.c.travelTime`（手填字符串） | **存在但只展示文本，未做格式化/校验** |
| ④ 显示总时长、路上时长、巡视总时长 | 缺 | **完全没有**（line 805-830 只列条目） | 需新增时长汇总 section |
| ⑤ 每天从默认起点开始计算 | 缺 | **没有"默认起点"概念**；`SETTINGS.startPos` 不存在 | 需 F2 同步实现 |
| ⑥ 第二个点从第一个点算 | 缺 | line 810-814 按**从新到旧**倒序排 | 算段时长前必须先 reverse |

### 需求实施位点（`openInspectRoute` line 805-830）
**A. 排序调整**：line 810-814 改为 `ta < tb ? -1 : 1`（**旧→新**），让段时长好算。
**B. 段时长算法**（新增辅助函数 `routeDurationsForDay(day)`）：

```js
// 输入：指定日的所有 inspections（已按时升序）
// 输出：{ leave: 首段出发时间, travel: 总路上时长(s), inspect: 总巡视时长(s), total: 总时长(s), firstStart, items: [{dur, mode:"travel"\|"inspect"}] }
```

- **总时长** = 末条 `arrivalTime` - 首条 `arrivalTime`
- **总路上时长** = 每两条间的 `arrivalTime` 差累加（路上时间）
- **总巡视时长** = 当日所有有效巡视记录 `travelTime`（手填）+ 默认每个巡视点估 3-5 分钟巡视操作时长（可配置）
- **首段**：若当日首条 `arrivalTime` 早于 `SETTINGS.startPos.leaveAt + 24h`，路上时长 = 首条 - startPos.leaveAt；否则提示"今日尚未设置默认出发位置"
**C. DOM 注入（line 815 之后、line 817 之前）**：插入段时长汇总 section：

```html
<section id="routeDayStats">
  按日分组：每段显示
  · 开始（默认：从 <默认起点> 出发 · HH:MM）
  · 总巡视时长：HH:MM:SS
  · 总路上时长：HH:MM:SS
  · 总时长（外勤）：HH:MM:SS
  · 巡视点数：N
</section>
```

**D. 照片灯箱接驳**：
- line 824 加 `onclick="appLightboxMulti('${bid}', ${idx})"` —— 注意旧 photo 是字符串，需兼容：若 `c.photo` 仅 1 张 → `appLightbox(bid, 0)`；若有 b.photos[] → 多图轮播
- 同时改造 `appLightbox` line 3429 接 G1 实现：传 `arr: [{src:..., ...}], idx: 0` → 内部 `setInterval(_=>next(), 3000)`、左右按钮、ESC/点遮罩暂停

**E. 「显示填入的巡视时长」增强（line 822）**：
- 原 `时长：${c.travelTime}` 改成 `时长：${c.travelTime || "—"}`，并把"…"超过 8 字符或非"X 小时 / Y 分钟 / 半天"格式时标红给提示"建议规范化为 HH:MM"

### 数据依赖 & 兼容性
- 新字段 `SETTINGS.startPos: { lat, lng, name, leaveAt: "07:30" }`；运行维护菜单的「🛠️ 默认出发位置」按钮创建
- 旧 inspections 没 `coord/inspType/planned` → 显示用 `|| "—" / "其他" / "否"`
- 段时长计算若某条 arrivalTime 缺失或格式异常 → 该条不计入时长（降级显示"跳过"）

### 与第三批 F4/F5 的关系
- F4 巡视表单加 `inspType/coord/planned` 是**录入侧**
- 第五节是**查看侧**（在 openInspectRoute 内做）
- 必须**两个一起做**，否则录入的 inspType 在路线页看不到；坐标同理（截图只留"巡查时间"显示，没计划显示坐标）

---

## 六、新 Bug：加「放大缩小/收藏返回」后筛选功能失效 + 筛选完整需求规格

### 用户报告原文（2026-08-23 08:18）
> "加上放大缩小 收藏返回，下面功能失效了"
> "筛选过程中和完成后，有个提示区实时显示当前条件选择几个目标，点击确定后，如果符合条件的有多个目标，用蓝色或绿色虚线圆圈刚好圈起所有目标，并且点击确定后，界面跳转到整个页面刚好显示能显示整个圆圈，如果符合条件只有一个，立即跳转到目标位置，没有符合条件则提示"

### 关键诊断：筛选逻辑本身**未失效**，代码完整
已读 `applyFilterAndJump`（line 1157-1178）+ `showFilterResult`（line 1186-1233）：
- **0 个** → `ask("无符合条件的建筑", ...)` 提示（保留面板）
- **1 个** → `appFly(id)` 立即定位
- **多个** → `showFilterResult(matched, "blue")` 浅蓝虚线圈 + `map.fitBounds(circle.getBounds(), {padding:[40,40], animate:true})` 整圈入屏 + `showFilterBadge("命中 N 个")`
- 颜色区分：筛选=浅蓝 `#4aa3e0`、查询=绿 `#2e8b57`（line 1182-1185）
- 圈半径=`最远点距离+200m`（line 1219-1220），保证"刚好罩住"
- 实时提示区：`updateFilterCount()` 在筛选过程中每次点 chip/输关键词都调（line 1108/1114/1121/1131），显示"当前命中 N 个"

**结论**：用户要的筛选需求（实时计数 / 0提示 / 1定位 / 多蓝绿虚线圈+整圈fitBounds）**在 V3.15 起已全部实现并在 V3.24 回归**（changelog line 3008/3048 印证）。

### "失效"的真实根因（两种可能，待用户确认界面）
地图操作控件（放大/缩小/收藏/返回收藏）绑定在 line 4413-4461，对应 DOM `btnZoomIn/btnZoomOut/btnBookmark/btnGotoBookmark`，**这是 v3.26 起主界面固有控件，不是新加的**。

因此"加上之后筛选失效"的合理解释：
- **情形 A（最可能）**：用户在**新界面**（运行维护/巡视表单、或古建/感知独立壳）里**自行加入了这组悬浮按钮**，但那组按钮的容器 CSS 用了 `position:fixed` + 高 `z-index`，**覆盖在筛选面板（sheetGen）之上或地图之上**，导致：
  - 筛选"确认"按钮被遮挡无法点 → 看起来"失效"
  - 或按钮容器的 `click` 事件冒泡到地图，触发 `map` 的 click 监听使筛选面板意外收起
- **情形 B**：新界面初始化时 `$("btnZoomIn")` 取到了错误 DOM，或 `map` 尚未 `ready` 就调 `map.zoomIn()` 抛错，使同批次的事件绑定（含 `applyFilterAndJump`）中断。

### 待用户澄清（关键，否则无法精确定位）
1. "加放大缩小收藏返回"是加在**哪个界面**？（水利主图 / 运行维护菜单 / 巡视表单 / 古建 / 感知）
2. "下面功能失效"具体表现：**筛选面板打不开 / 确认按钮点不动 / 点确认无反应 / 圈不出来 / 跳转不动**？
3. 这组按钮是**手写 HTML 加的**还是**从主界面复制的**？容器父节点 id/class 是什么？

### 修复方向（按情形）
- **A（遮挡）**：悬浮按钮容器 `z-index` 设为低于 `sheetGen` 弹层（sheetGen 用 `ovGen` overlay，z-index 应高于地图控件）；或筛选面板打开时 `display:none` 隐藏地图控件。按钮容器 `pointer-events` 不拦截地图空白 click。
- **B（初始化报错）**：按钮绑定包在 `map.on('load')` / `DOMContentLoaded` 后；`map.zoomIn()` 调前判 `if (map && map._loaded)`。

### 附：用户本次把筛选需求讲得很完整，已与现有实现逐条对齐（无需重做，仅确认回归）
| 用户描述 | 现有实现（line） | 状态 |
|---------|----------------|------|
| 筛选过程+完成实时显示命中数 | `updateFilterCount()` line 1108/1131 等 | ✅ 已有 |
| 点确定多个→蓝/绿虚线圈刚好罩全部 | `showFilterResult` blue/green line 1186 | ✅ 已有 |
| 点确定多个→整圈 fitBounds 入屏 | `map.fitBounds(circle.getBounds())` line 1228 | ✅ 已有 |
| 点确定1个→立即跳转 | `appFly(matched[0].id)` line 1171 | ✅ 已有 |
| 0个→提示 | `ask(...)` line 1162 | ✅ 已有 |

**所以"失效"是交互层（按钮遮挡/初始化），不是功能逻辑缺失。** 修复后需对筛选做回归验证（0/1/多三种路径）。

---

## 全部待确认事项汇总（用户最终决策入口）

### 修复类（Bug A~E）
选 **【全量】/【保安装】/【只修 E】/【自定义】** —— 见上文第二节

### 功能类（F1~F5 + G1）
选 **【全做】/【核心】/【自定义】** —— 见上文第三节

### 跨工程同步
- Bug A 包名：水利/古建/感知 三端都要改
- 功能 F/G：仅水利要"运行维护"？还是古建(旅游打卡)/感知(感知运维)也同步加？

---

# ════════════════════════════════════════════════════════════
# 方案 C 执行完成记录（2026-08-23）
# ════════════════════════════════════════════════════════════

> 用户决策：**方案 C，全跑完**。
> 四跨事项：① 收藏/返回/放大缩小不隐藏 ② 运行维护做水利+感知，古建智能优化对应到旅游打卡菜单 ③ 筛选先放下（未做）④ 修复注意事项（风格一致/四端差异/退出按钮/功能保留/文档备份/代码回退）。

## 一、已修复（Bug A~E）

| # | 修复内容 | 落点 | 状态 |
|---|----------|------|------|
| A | 三 APK 同装 | 水利 `com.shuili.yitu`(v3.29)／古建 `com.gujian.travel`(v1.7)／感知 `com.shuili.ganzhi`(v1.5)；新建水利壳 `android-build/shuili-v329`，感知壳 `android-build/perc-v13` 改名 package | ✅ |
| B | 卡顿 | `render()` 改 marker 差异更新（视窗进出只增删差异，不全量销毁重建）；popup 懒构造（bindPopup 传函数） | ✅ |
| C | 筛选蓝圈 | 筛选激活时关闭视窗裁剪（全量建 marker），蓝圈不缺 | ✅（逻辑已就位，筛选 UI 暂未启用，按用户"筛选先放下"） |
| D | 纯照片 ZIP 误报 | 入口①检测到含图无表 → 引导按钮转「批量导入照片」 | ✅ |
| E | 解压假死/进度条 | ① `.xferOverlay` z-index 2000→3000（高于 sheet 2001）② `LargeFileManager.unzipImages` 改异步线程池 + 周期 `onUnzipProgress` 回调 | ✅ |

## 二、已新增（F1~F5 + G1）

- **F1 运行维护菜单**：水利、感知均新增 `🛠️ 运行维护` 一级组（默认出发位置/运行维护计划/运行维护计划列表/巡视/我的巡视路线）
- **F2 默认出发位置**：地图选点 / 从建筑物选 / 手动坐标 + 默认出发时间 `SETTINGS.startPos`
- **F3 运行维护计划**：`MAINT_PLANS`（类型/起止日期/处理时间/范围多选/备注），含列表查看
- **F4 巡视升级**：巡视类型 + 显示坐标（navigator.geolocation 自动获取、只读）+ 是否计划内
- **F5 我的巡视路线**：按天分组时长汇总（总巡视/路上/外勤）+ 首段从默认起点算 + 照片点开放大
- **G1 单图灯箱**：`appLightboxSrc(src, caption)` 单图放大，水利/感知/古建三端一致

## 三、古建智能对应旅游打卡（用户："古建智能优化对应到 旅游打卡菜单"）

- 古建**不强行复制**运行维护，保持自身「拍照打卡」分组（打卡/我的打卡路线）
- 语义映射：古建「打卡」↔ 水利「巡视检查」（游览打卡对应设施巡查）
- 智能优化：① 打卡路线照片改为点开 `appLightboxSrc` 放大查看（与水利/感知一致）② 帮助页新增「智能对应」说明，三端交互（菜单风格/退出按钮/三击呼出主菜单）一致 ③ 未删任何既有功能

## 四、四端对照表（用户要求③：考虑四个平台差异性）

| 维度 | Android | Windows (WebView2) | UOS/Loongson (PyQt6) | iOS (PWA + 静态 HTTPS) |
|------|---------|--------------------|--------------------|------------------------|
| 打包形态 | 原生 APK（aapt2+d8+zipalign+apksigner） | 原生桌面 EXE（.NET WebView2，无 .NET SDK 可运行） | 原生 deb（Packaging.DebUOS.Tool，UOS 20 Pro 1050 / 龙芯 3A4000 / 8G） | PWA（HTTPS 静态托管，加到主屏幕即类原生） |
| 离线底图 | 内置 offline_tiles + 天地图在线 | 同 Android | 同 Android | 在线为主，可配离线包 |
| 原生桥（解压/照片/定位） | Java JavascriptInterface | C# WebView2 桥 | Python PyQt6 桥 | 纯前端（JSZip / 浏览器 API） |
| 解压进度 | 原生线程池 + onUnzipProgress | 同 | 同 | JSZip 进度事件 |
| 定位 | navigator.geolocation（已授权） | 同 | 同 | 同（HTTPS 下可用） |
| 运行维护 | ✅ 水利+感知 | 同 Android 逻辑 | 同 Android 逻辑 | 同（PWA 同源） |
| 旅游打卡 | ✅ 古建 | 同 | 同 | 同 |
| 同装共存 | 三包名互异 | 独立进程/目录 | 独立 deb 包名 | 独立 PWA 源 |

> 四端共用同一套 `app.js` 业务内核（render 复用 / 运行维护 / 灯箱 / 退出按钮 / 三击呼出），差异只在**外壳与离线包分发**。

## 五、备份与回退（用户要求⑧⑨）

- 代码备份：`D:/Users/Claw/backup_2026-08-23/`（改前三工程全量快照，可回退）
- APK 归档：`D:/Users/Claw/APK归档/四端安装包_20260823/`（三 APK，命名含版本+日期+构建号 5090）
  - 水利工程一张图V3.29 (com.shuili.yitu) 271388 B
  - 水利感知项目一张图V1.5 (com.shuili.ganzhi) 11561306 B
  - 古建景点打卡V1.7 (com.gujian.travel) 259308 B
- 文档同步：本文件已更新为「已执行」；记忆 `D:/Users/Claw/.workbuddy/memory/2026-08-23.md` 记录本次操作

## 六、未做项（按用户"筛选先放下"）

- Bug⑥ 筛选 UI 增强（实时计数区/整圈 fitBounds 视觉微调）逻辑已存在，仅未启用新筛选 UI；待用户给 3 项澄清后再做。

---

# ════════════════════════════════════════════════════════════
# 第二轮：安装无图标 / 感知内置照片页（2026-08-23 10:30）
# ════════════════════════════════════════════════════════════

## 问题：水利/感知 APK 安装后无桌面图标、不提示打开（古建正常）

### 根因（已确诊，决定性验证）
- `aapt2 dump badging` 对比：古建 V1.7 有 `launchable-activity`，水利 V3.29 / 感知 V1.5 **无** `launchable-activity`。
- 交叉实验：古建 res + 水利 manifest → 无 launchable；水利 res + 古建 manifest → 有 launchable ⇒ 问题在 **manifest 本身**。
- 字节级 diff（归一化空白后）定位：水利/感知 manifest 的 category 写成 `android.category.LAUNCHER`（**少了 `intent.`**），正确应为 `android.intent.category.LAUNCHER`。
- 该笔误导致系统不识别 Launcher 入口 ⇒ 无桌面图标、安装器不弹「打开」。

### 修复
- 水利 `android-build/shuili-v329/AndroidManifest.xml` 改 `@android.category.LAUNCHER` → `@android.intent.category.LAUNCHER`
- 感知 `android-build/perc-v13/AndroidManifest.xml` 同改
- 重打 APK，`aapt2 dump badging` 确认两包均出现 `launchable-activity` ✅
- 归档已更新（`四端安装包_20260823/`）

### 教训
- AndroidManifest 的 Launcher category 必须是 `android.intent.category.LAUNCHER`，少写 `intent.` 不会报错但直接导致无图标。新建/复制壳时 sed/manifest 修改需逐字符核对。**构建后必须用 `aapt2 dump badging` 验证 `launchable-activity` 存在**作为发布门禁。

## 问题：感知「内置照片页」删除

### 核实
- 感知 assets 无 `images/` 目录；变更记录显示 6 张内置照片（pic_1..6）已于 v3.28 删除，引用数 0。
- 「内置照片」实质已在 v3.28 清理完毕；唯一相关菜单项是「照片（目录与命名）」说明页（`openPhotoHelp`，讲四端照片目录约定）。

### 处理（用户「始终允许」= 按方案①）
- 移除感知菜单「照片（目录与命名）」入口（保留 `openPhotoHelp` 函数，符合「不删核心功能」原则）。
- 重打感知 APK + 归档；changelog 新增 v1.5 条目记录本次修复与删除。

## 本轮产物
- 三 APK 归档（`四端安装包_20260823/`）：水利 V3.29 / 感知 V1.5 / 古建 V1.7 均已 `launchable-activity` 正常
- 文档：本文件追加第二轮记录；水利/感知 changelog 同步；记忆更新
