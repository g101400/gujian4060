# 三图家族项目交接台账（水利 / 感知 / 古建 一张图）

> **用途**：项目交接与长期维护的单一权威台账。覆盖三图同源产品 × 四平台（Android / UOS 龙芯 / Win11 / iOS-PWA）+ 微信小程序。
> **当前版本基线**：水利 v3.48(code 48) / 感知 v1.24(code 28) / 古建 v3.6(code 28)，buildDate 2026-09-01，归档 `APK归档/四端安装包_20260901_v348/`（24 件）。
> **配套文件**：`三图经验沉淀_铁律与配置.md`（耐久约定）、`问题台账_三图回归_2026-08.md`（完整 T-001~T-041 实战叙事）、本目录 `项目交接卡.md`（一页速查）。
> **核心原则**：每个问题单一真源、有根因/修复/验证/防复发铁律/状态；门禁必须嵌入发版脚本并**阻断**，不能只写文档。

---

## 一、项目概况与产品矩阵

| 产品 | APPNAME | 数据全局 | 核心实体 | canonical webroot（唯一权威源） | 当前版本 |
|------|---------|----------|----------|--------------------------------|----------|
| 水利一张图 | 水利工程基础信息一张图 | `SHUILI_DATA` | 水工建筑物(闸/坝/泵站/堤防) | `android-build/water-v329/assets/` | v3.48 |
| 水利感知项目一张图 | 水利感知项目一张图 | `PERCEPTION_DATA` | 感知设备(液位/渗压/雨量/摄像头) | `android-build/perc-v13/assets/` | v1.24 |
| 古建景点打卡 | 古建景点打卡 | `GUJIAN_DATA`/`SCENERY` | 古建景点(古塔/寺庙/城墙/古桥) | `travel/android/assets/` == `travel/webroot/` | v3.6 |
| 微信小程序（京引一张图） | — | — | 古建打卡延伸 | `jingyin/` | 基线 v3.48 |

**四平台产物（每产品）**：
- Android APK：`android-build/<key>/app-release.apk` → `APK归档/.../<名>V<ver>_YYYYMMDD_5090.apk`
- UOS deb：3 产品 × 4 架构(mips64el/loongarch64/arm64/amd64) = 12 包，浏览器壳方案
- Win：MSI(WiX7) + EXE(NSIS) = 6 件
- iOS PWA：`water-ios/apple-package/<名>_iOS_<ver>_可托管.zip` = 3 件

**⚠ 目录指纹铁律**：改前必 `grep APPNAME` + 数据全局变量确认归属；`jingyin/windows-pkg/webroot` 是感知 V1.2 误植污染源，**禁止作为任何四端源**。

---

## 二、构建体系（canonical → 镜像 → 产物）

### 2.1 多副本一致性（最高频坑，已治本 T-001~T-003）
- canonical webroot 改完，**必须用 `sync_all_mirrors.py --real` 同步到全部镜像**，`--check` EXIT=0 才交付。
- 活动 webroot 清单（water 产品示例，其余产品同构）：
  - `android-build/shuili-v329/assets/` `android-build/water-v329/assets/`
  - `native-shell/uos-water-pyqt6/webroot/`
  - `native-shell/win-water-webview2/webroot/`（iOS 源）
  - `native-shell/win-water-webview2/publish_win_water/webroot/`（MSI/EXE 源）
- **注意**：MSI/EXE 读 `publish_win_*` 目录、**不读源 webroot**；改数据后两个都要同步（T-034 教训）。

### 2.2 发版脚本清单
| 平台 | 命令 | 门禁 |
|------|------|------|
| 升版 | `python3 bump_version_v348.py` | 末尾强制 sync --check + 长按验证 + 小程序基线三道卡口（T-038） |
| Android | `bash android-build/shuili-v329/build_apk.sh`（perc-v13 / travel/android 同构） | 自带版本门禁 + `aapt2 dump badging` + `apksigner verify` |
| UOS deb | `python3 native-shell/make_deb.py shuili\|perc\|gujian` | `verify_myml_ar.py` EXIT=0（ar 头 + dir 齐备） |
| Win MSI | `python3 native-shell/gen_win_msi_wix7.py` | `verify_publish()`：exe 含 `GetWebView2DataFolder` + version.json 匹配 |
| Win EXE | `python3 native-shell/build_win_exe_nsis.py` | NSIS 表版本核对 |
| iOS PWA | `python3 native-shell/water-ios/build_ios_zip.py {shuili\|perc\|gujian}` | dict 版本核对 + 打印无「VV」双前缀 |
| 小程序 | `python3 sync_weapp_version.py [--check]` | 三处版本一致 |

### 2.3 标准出包顺序（T-004/T-005）
`node --check` → 改源码 → `build_apk.sh`×3 → `bump_version_v348.py`（自动 sync+卡口）→ MSI/EXE/deb/iOS → 归档。
**改 assets 必重编再归档**（release 不重编是历史致命坑）。

---

## 三、防复发铁律清单（每次发版必守，按优先级）

1. **多副本一致性**：交付前 `sync_all_mirrors.py --check` 必 EXIT=0（9 源 webroot 全绿）。
2. **门禁=卡口**：门禁必须嵌入发版脚本并 `sys.exit(非0)` 阻断，不能只写文档（T-038）。
3. **执行级验证**：宣称「已修 script error / 菜单不再崩」前，必须跑 `verify_threemap_harness.js` + `verify_fav_longpress.js`（实际调用函数 no throw）。`node --check` 仅查语法，查不出未定义引用/形参遮蔽/前向引用。
4. **出包顺序**：改 assets 必重编；release 不重编=旧包（T-004）。
5. **UOS 门禁**：`verify_myml_ar.py` EXIT=0（ar 头 3 条 + dir 条目齐备 + 浏览器壳 + control 四件套 + 独立端口）；`.desktop` 禁通用名/裸命令名；Depends 不得含 pyqt6（T-031）。
6. **Win 门禁**：跟「产物」走不跟「脚本」走，任何打包路径都校验 `GetWebView2DataFolder` 标记（T-032）；MSI 用 `<Files Include="payload\**">` 自动收割（T-014）。
7. **版本同步**：8 处 + gujian 无 `v` 前缀 + 小程序三处 + NSIS 表 + publish 目录名，升版前全核（T-006~T-009, T-034~T-037）。
8. **打包自带剪枝**：脚本必须自己排除 `webroot_bak_*`/`backup_*`/`bak` + `leaflet/leaflet` 嵌套，不能依赖源目录干净（T-033）。
9. **目录指纹**：改前 `grep APPNAME`+数据全局确认归属；cp 后 reset APP_VERSION + 同步 version.json（T-010）。
10. **Edit 走 Python**：JS 含正则/双引号用 Python `io.open`+`assert old in t`+replace，末 `node --check`（T-010b 前向引用）。
11. **交互功能必有执行级验证+回归自测**：新增交互（如长按收藏）必须证明门禁真能抓到未实现版本（T-039）。
12. **数据准确性**：业务坐标/属性变更须全 webroot 同步 + 一致性校验，单改源漏镜像=旧数据随包（T-041）。

---

## 四、问题追踪总表（T-001 ~ T-041）

### 4.1 已治本（结构性/工程纪律 T-001~T-010、T-038）
T-001~T-003 多副本分叉(6 次) → `sync_all_mirrors.py` 唯一对齐工具+卡口。
T-004 出包未跟源码 → 标准顺序 build_apk×3 再 release。
T-005 验证仅语法级 → `verify_threemap_harness.js` 执行级门禁。
T-006 gujian 无 v 前缀 / T-007~T-009 bump dict 漏改 / NSIS 表 / 小程序三处 → 升版前全核。
T-010 cp 污染 + T-010b 前向引用 → 目录指纹 + IIFE 内避免前向引用。

### 4.2 已治本（平台构建 T-011~T-015、T-031~T-037）
T-011 ar size 补齐 / T-012 tar 缺父目录 / T-013 无图标打不开 → `verify_myml_ar.py` 门禁。
T-014 MSI 图标全裂 → 自动收割。T-015 iOS dict 漏改。
T-031 UOS 3.40+ 全挂(四类根因) → 回归浏览器壳 + control 四件套 + 独立端口。
T-032 Win E_ACCESSDENIED 反复 → 门禁跟产物走。
T-033 备份/冗余混入包 → 脚本自带剪枝。
T-034 publish 目录漏同步 / T-035 deb 归档目录硬编码 / T-036 iOS V 前缀漏改 / T-037 小程序靠人工 / T-038 门禁是约定非卡口。

### 4.3 已治本（功能回归 T-016~T-024）
T-016 天地图密钥泄露 → 占位掩码。T-017 导出 zip 名不一致 → 统一 `kb_<domain>_<date>`。
T-018 知识库 `q is not a function`（形参遮蔽真源）/ T-019 `defaultOffice` 未定义（真源，此前只修表象）。
T-020 全子菜单兜底 window.onerror / T-021 CORS 代理回退 / T-022~T-024 导出/匹配/筛选。

### 4.4 新增功能（T-039）
T-039 子菜单长按添加到快捷常用菜单：三端 app.js 注入长按 600ms 收藏（touch+mouse 双通道、MutationObserver 重绑、长按吞 click 防误开、振动+toast），与 ☆ 并存。`verify_fav_longpress.js` 三端全 PASS + 回归自测（未注入版 EXIT=1 拦截）。

### 4.5 数据准确性（T-041，本轮新增）
T-041 **b103「水库进水闸测流桥」现场坐标与记录不符**：
- 旧 `lon 116.61319167, lat 40.30567789` → 新 `lon 116.62149293, lat 40.336104`（其余字段不变）。
- 根因：业务数据维护，非代码 bug；但**单改 canonical 漏镜像**=旧坐标随 MSI/EXE/iOS 包分发（T-041 教训）。
- 修复：5 个 water 活动 webroot 全部同步新坐标（shuili-v329 / water-v329 / uos-water / win-water / publish_win_water），一致性校验全 NEW。
- 验证：交付 `四端安装包_20260901_v348/` 全 24 件含新坐标。
- 防复发：见铁律 12（数据变更须全 webroot 同步+校验）。

### 4.6 已知遗留 OPEN（待人工/后续，未关账）
| ID | 问题 | 现状 | 下一步 |
|----|------|------|--------|
| T-025 | 古建三击空白呼主菜单 | **✅ 已治本** | click→pointerdown + 菜单已开防误计；`verify_guijian_triple_tap.js` 全过；古建四平台已重构建 |
| T-026 | 古建 zip 最佳匹配保存 | **✅ 已治本** | 导入项补 zipName + `PHOTO_MATCH_MEMORY` 持久化；`verify_guijian_zipmatch_persist.js` 全过；古建四平台已重构建 |
| T-027 | canonical assets 缺 `images/` | **关账(误报)** | `images/pic_1..6.jpg` 仅版本说明纯文本，非运行时加载；无需修复 |
| T-028 | 真机三端验证 | 待验 v3.48（24 件） | 重装后确认长按收藏/导入导出/知识库 |
| T-029 | UOS deb / Win MSI 真机装 | 待验 v3.48 | UOS 用 `fix_uos_v348.sh`；Win 验标准用户不再 E_ACCESSDENIED |
| T-030 | 小程序 jingyin 功能回归 | **✅ 审计完成** | 基线同步 v3.48；三击+收藏已对齐三端；已知 gap：小程序无 zip 照片导入（三端有），待产品决策 |
| T-040 | E: 盘每日备份静默失败 | **✅ 已治本** | 新增 `backup_claw.py`：首选不可达→回退 D: 本地 + 写 `_backup_STATUS.json`(ok/error) + 退出码非0 供外部告警；实测回退成功 |

---

## 五、质量门禁速查（每次构建必过）

| 检查项 | 命令 | 预期 |
|--------|------|------|
| JS 语法 | `node --check app.js` | 全过 |
| 执行级（菜单/收藏） | `node verify_threemap_harness.js` + `node verify_fav_longpress.js` | EXIT=0 |
| 多副本一致性 | `python3 sync_all_mirrors.py --check` | EXIT=0 |
| UOS deb | `python3 native-shell/verify_myml_ar.py <deb>` | EXIT=0 |
| 安卓版本/签名 | `aapt2 dump badging` + `apksigner verify` | versionName 对 / Verifies |
| 版本一致 | versionCode/Name ↔ filename ↔ version.json | 三者一致 |

---

## 六、交接 checklist（接手人第一天）

- [ ] 读 `三图经验沉淀_铁律与配置.md` + 本台账 + `项目交接卡.md`
- [ ] 环境：Android SDK(`C:/Users/admin/.workbuddy/android-sdk`)、WiX7(`wix`)、NSIS、Python3、dotnet
- [ ] 跑一次 `python3 bump_version_v348.py` 确认卡口全过（不动版本也可 `--check`）
- [ ] 确认 `四端安装包_20260901_v348/` 24 件齐全
- [ ] 真机验证 T-028/T-029（UOS 用 `fix_uos_v348.sh`）
- [ ] 处理 OPEN 项 T-025/T-026/T-027/T-030/T-040
