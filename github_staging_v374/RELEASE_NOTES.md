# 三图一张图 v3.74 / 1.50 / 3.7.12 发版说明（2026-09-13）

## 版本总览
| 通道 | 水利 | 感知 | 古建 |
|---|---|---|---|
| 内部版 | 3.74（code 74） | 1.50（code 50） | 3.7.12（code 40） |
| 公开版 | 3.75（iOS/PWA） | 1.51（iOS/PWA） | 单通道 |

## 本版重点
1. **口令硬化（安全）**：出货源码移除开发者分机号明文 `3305`，改为 AES-256-GCM（PBKDF2）凭证校验 + 运行时输入 + 构建期注入（`.sec_pass` 不随包）。
2. **局/管理处数据化（B 方案，水利+感知）**：编辑表单新增「局」「管理处」下拉字段；CSV / ovkmz 双向映射；AI 问答按 `ju/guanchu` 原始值回退口径。古建无此字段。
3. **感知集成策略**：维持「只集成水工建筑物底图数据层」范式（§8），内置 557 条种子。

## 四平台产物
- Android：APK（aapt2 badging + apksigner 校验通过）
- UOS / Linux：DEB × 4 架构（amd64 / arm64 / loongarch64 / mips64el）
- Windows：MSI + NSIS EXE
- iOS：PWA 可托管 zip（内部 + 内部加密；公开版经 Pages 分发）

## 安装注意
- 设备上的旧包（3.72 / 1.48 / 3.7.11）**不会自动更新**，须重新安装新 APK / DEB / EXE。
- 内部加密 iOS 需输入口令（由分发人单独提供）；公开版免密。

## 质量门禁
- `verify_pass int` 21/21、`sync_all_mirrors` 11 镜像 + 7 公共模块 md5 一致、小程序基线 3.74、`verify_pkgs_smartquery` 48/48，全绿。
