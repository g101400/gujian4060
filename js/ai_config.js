/* =====================================================================
 * ai_config.js —— 智能分析引擎配置文件（古建景点打卡，2026-09-16）
 * 说明：模型密钥等部署相关配置集中于此，便于按环境注入，
 *       亦避免密钥随源码/公开包外流。
 *       公开仓库与公开交付包中 openrouterKey 必须留空，
 *       由使用者在「智能分析设置」中自行填写自有密钥。
 * 加载顺序：必须在 js/ai.js 之前引入。
 * ===================================================================== */
window.SHUILI_AI_CONFIG = window.SHUILI_AI_CONFIG || {};
window.SHUILI_AI_CONFIG.openrouterKey = "";   // 公开源码/公开包一律置空（原内联密钥已因公开暴露作废）
