# 三图家族项目交接卡（一页速查）

**项目**：水利一张图 / 感知项目一张图 / 古建景点打卡（三图同源）+ 微信小程序「京引一张图」
**当前版本**：水利 v3.48 / 感知 v1.24 / 古建 v3.6（2026-09-01）
**归档**：`APK归档/四端安装包_20260901_v348/`（APK 3 + deb 12 + Win 6 + iOS 3 = 24 件）

## 版本速查
| 产品 | code | canonical webroot |
|------|------|-------------------|
| 水利 | v3.48 | `android-build/water-v329/assets/` |
| 感知 | v1.24 | `android-build/perc-v13/assets/` |
| 古建 | v3.6 | `travel/android/assets/` |

## 出包命令（标准顺序）
```
node --check app.js                     # 1 语法
bash android-build/shuili-v329/build_apk.sh   # 2 APK×3(perc/travel 同构)
python3 bump_version_v348.py            # 3 升版=自动 sync 卡口+长按验证+小程序基线
python3 native-shell/make_deb.py shuili      # 4 UOS deb×3 产品
python3 native-shell/gen_win_msi_wix7.py     # 5 Win MSI
python3 native-shell/build_win_exe_nsis.py   # 6 Win EXE
python3 native-shell/water-ios/build_ios_zip.py shuili  # 7 iOS×3
python3 sync_weapp_version.py --check        # 8 小程序基线
```

## 五条保命铁律
1. **门禁=卡口**：`sync --check` / `verify_*` 必须 EXIT=0，且嵌入发版脚本阻断，不止写文档。
2. **多副本一致**：改 webroot 必 `sync_all_mirrors.py --real` + `--check`（MSI/EXE 读 `publish_win_*` 目录，也要同步）。
3. **执行级验证**：菜单/收藏改完跑 `verify_threemap_harness.js` + `verify_fav_longpress.js`，`node --check` 不够。
4. **版本八处 + publish 目录名 + 小程序三处**，升版前全核；gujian 版本无 `v` 前缀。
5. **改 assets 必重编再归档**；数据变更须全 webroot 同步+校验（单改源漏镜像=旧数据随包）。

## 数据 fix 记录
- b103「水库进水闸测流桥」：旧 `116.61319167,40.30567789` → 新 `116.62149293,40.336104`（5 个 water webroot 已同步）。

## 已知 OPEN（待办）
T-025 古建三击空白 / T-026 zip 最佳匹配保存 / T-027 assets 缺 images / T-028 真机三端 / T-029 UOS/Win 真机 / T-030 小程序回归 / T-040 E盘备份静默失败。

## 关键路径
- 铁律与配置：`D:/Users/Claw/.workbuddy/memory/三图经验沉淀_铁律与配置.md`
- 完整台账：`D:/Users/Claw/.workbuddy/memory/问题台账_三图回归_2026-08.md`
- UOS 真机修复：`APK归档/四端安装包_20260901_v348/fix_uos_v348.sh`
