# 水利工程一张图 V3.15 → V3.24 三端同步分析报告

**生成时间**: 2026-08-19  
**分析范围**: Android APK V3.15 (2026-08-17) → V3.24 (2026-08-19)  
**目标平台**: iOS 18+ / Win11 x64 / 统信 UOS  

---

## 📊 一、版本跨度总览

| 项目 | 详情 |
|------|------|
| **分析周期** | V3.15 → V3.24 (2026-08-17 ~ 2026-08-19, 持续 2 天) |
| **Android versionCode** | 15 → 24 |
| **核心功能点数** | v3.15 基础功能集 + 新增/优化项共约 50+ |
| **关键修复数** | 6 (v3.21/v3.23/v3.24) |

---

## 🎯 二、核心功能变更清单（按优先级）

### 【P0】真机致命问题修复

#### v3.21 — IIFE 闭包陷阱修复
| 问题 | 根因 | v3.21 修复方案 | iOS/Win11/UOS 适配 |
|------|------|---------------|-------------------|
| `toggleList is not defined` | onclick 函数未暴露全局 | window.toggleList = toggleList | ✓ JS: 全局变量<br>✓ Electron: IPC Renderer-Main<br>✓ GTK: JS-GObject bridge |

#### v3.21 — localStorage 定位查看失效  
| 问题 | v3.21 修复方案 | 三端适配注意 |
|------|---------------|-------------|
| navigator.clipboard.writeText() 离线无安全上下文兜底 | textarea.execCommand('Paste') + fallback | iOS WKWebView: document.execScript<br>Win11 Electron: clipboard.writeText IPC<br>UOS GTK: gtk_clipboard_set_text |

#### v3.23 — 导入提示不弹（根因修复）  
**关键规律**: offline WebView confirm()/alert() **不显示且默认返回 false** → 导致：重复导入静默取消 / 同名建筑静默覆盖  

| 问题分支 | 影响 | V3.23 修复 |
|--------|------|-----------|
| kmz/ovkmz 二次导入 | 已存在→静默失败 | ask()自定义对话框✅ |
| 同名建筑物 | 覆盖更新→静默 | 三选一弹窗 (ask)✅ |

#### v3.24 — 查询圈选缺失  
**原状**:主界面「🔍查询」菜单仅聚焦搜索框，无确认动作（与筛选不一致）  
**修复**: window.doQueryConfirm() — 按钮/回车触发，0 条 ask() / 1 条 flyTo / 多绿圈 ✅

---

### 【P1】核心交互增强

#### v3.22 — 智能匹配确认体验升级
| 功能 | v3.21-前 | v3.22+ | iOS/Win11/UOS |
|------|----------|--------|--------------|
| **已选计数条** | 缺失 | "已选 X / N 张"实时刷新 | ✓ JS count<br>✓ Electron Renderer |
| **一键最匹配** | "全选"歧义 | 每张仅勾置信度最高 | ✓ UI + Enter/Esc键 |
| **长列表滚动条常驻** | 随内容消失 | sticky-actions position:sticky | ✓ CSS 兼容 |

#### v3.22 — 筛选圈选动画增强
| 效果 | 技术实现 | 三端适配 |
|------|---------|---------|
| 蓝色虚线圈（#1e6fbf）"行进蚂蚁"描边 | stroke-dashoffset transition | ✓ CSS animate<br>✓ Electron Chromium native |
| 倒水滴淡入缩放 (fd-anim 子元素) | 内层 transform key pit ✅ | ✓ CSS layer-isolation<br>⚠ iOS WKWebView 需测试 |

**经验教训**: Leaflet 标记根元素用 transform 定位，动画 transform 必须放在内层子元素（否则跳位）✅

---

### 【P1】类型规范化处理

| 问题根源 | V3.24 前动态 uniq(btype)混入乱字段 |
|----------|----------------------------------|
| **西田各庄段引渠**<br>技术指标<br>挖填方统计表 |
| **进水闸 部分缺少经纬度** | 

#### v3.24 — CANONICAL_TYPES (权威清单兜底)
```javascript
CANONICAL_TYPES = [  
  "进水闸/节制闸/分水闸/泄洪闸/扬水闸/补水闸",  
  "/泵站 /溢洪道/倒虹吸/跌水/过渠涵洞",  
  "/穿渠项目/交通桥/山洪桥/工作桥",  
  "/清污机/雨水口/道路/变压器"
];

ALWAYS_TYPES = ["泵站 /溢洪道"]; //始终展示即使暂无数据

canonicalTypes() → 图例/筛选/编辑下拉统一使用✅
```

**经验教训**: 脏类型建筑仅隐藏分类显示，不删除/静默改写；历史遗留建筑的原类型仍保留为可选项 ✅

---

### 【P2】UI/UX 体验优化（贯穿多版本）

| 版本 | 优化点 | iOS 适配 | Win11 适配 | UOS 适配 |
|------|--------|---------|-----------|---------|
| v3.7 | 🎚️筛选图标与🔍查询区分 | ✓ Unicode emoji | ✓✓ | ✓ |
| v3.7 | 菜单智能排序（高频功能靠前） | ✓ Web DOM | ✓ Electron shell | ✓ GTK menu |
| v3.20-21 | About 页三重环境展示 | ✓ WKWebView detect | ✓ Node 信息<br>✓ platform() | ✓ Python psutil |

---

## 🛠️ 三、经验教训沉淀（可复用模式）

### ✅ 已验证修复模式

#### 1. Offline WebView confirm/alert替代方案
```javascript
// ❌ 旧：离线不弹
confirm("是否覆盖更新？") 
alert("操作失败！")

// ✅ V3.21+ 新：自定义 ask()对话框（统一风格）
ask(
  "该文件与上次完全相同（SHA-256 比对）。\n" +
  "<b>点击"确定"继续导入；点击"取消"中止。</b>",  
  [{t:"确定"},{t:"取消"}]
) 
```

#### 2. Leaflet 动画 key pit ✅
```css
/* ❌ transform on marker root → jump position */
.l-marker .fd-anim {
  transition: transform 0.5s easOutCubic; /* WRONG */
}

/* ✅ V3.22+ transform on inner child */
.l-marker .fd-anim span {
  opacity: 0;
  transform: scale(0); 
  animation: fadeScaleIn 0.5s ease-out; /* CORRECT ✅ */
}
```

#### 3. IIFE onclick 全局暴露（闭包陷阱）
```javascript
// v3.21 前: onclick="toggleList()" 但函数在 IIFE 内找不到的问题
(function(){ 
  var toggleList = function() {...};  
})();

// ✅ V3.21+: window.toggleList = ... 全局暴露
(function(){ 
  var map, appGoToMap;  
  window.toggleList = function () { ... }; // EXPOSED ✅
})();
```

---

## 📁 四、三端技术路径对比

| 构建要素 | iOS (Apple Silicon) / ARM64 | Win11 (x64 / Chromium) | UOS (LoongArch/arm64 / GTK) |
|----------|------------------------------|-------------------------|-------------------------------|
| **WebView Core** | Apple WKWebView (WebKit WebCore) | Electron 78+ (Chromium 91+) | libwebkit2gtk-4.0 / Qt WebView 6.5+ |
| **JS Runtime** | JavaScriptCore (ES2021) | V8 Engine (ES2023 support) | Mozilla SpiderMonkey via bridge |
| **构建工具** | Xcode + Webpack + CocoaPods | electron-builder / forge | appimage-builder / Qt app bundle |
| **API 兼容性** | Clipboard API ✓<br>File System Access <br>(iOS17+) ✓ | Full ES6+ IPC bridge<br>Native dialogs ✓ | GTK clipboard wrapper<br>File dialog native |
| **离线存储** | localStorage file:// fallback<br>WKWebView data URL | app/Cache + node storage | ~/.local/share/app_cache |
| **打包输出** | .ipa (Xcode archive) | 64-bit .exe standalone | AppImage / UOS store bundle |

---

## 📦 五、交付物生成清单（自动化）

### 1️⃣ iOS 18+ (iOS 15+) — WebKit WebView 方案
```bash
# 输出目录：/d/Users/Claw/水利一张图_iOS_3.24_20260819/
├── dist/                           # Webpack 构建产物
│   ├── index.html                 # Single HTML file
│   ├── app.bundle.js              # Bundled JS (v3.24)
│   └── styles.css                 # Leaflet + app CSS
├── assets/leaflets/               # Leaflet maps (CSS)  
│   ├── leaflet.min.css
│   └── leaflet.awesome-markers.css
├── build_logs/                    # 构建日志
└── Info.plist                     # WKWebView 配置
```

**WKWebView 离线注意**: iOS WKWebView file:// 本地缓存 + localStorage持久化 fallback  
最小支持：iOS 15.0+ (Safari 同 WebKit 13+)

---

### 2️⃣ Win11 x64 — Electron v28+ 方案
```bash
# 输出目录：/d/Users/Claw/水利一张图_Win11_3.24_20260819/
├── dist/                          # Electon app bundle
│   ├── index.html
│   └── renderer.js (v3.24 bundled)
├── build_app/                     # Electron native 组件
│   ├── node_modules/              # IPC bridging libs
│   └── preload.js                 # Secure bridge script
├── package.json                   # electron-builder配置
└── appx_manifest.xml              # Windows Store (可选)
```

**Win11 原生功能**:  
- electron-dialogs for file picker / clipboard  
- context-menus native integration  
- shell.openExternal() 导航支持  

输出格式：.exe standalone + resources

---

### 3️⃣ UOS (统信 UOS) — GTK WebView 方案
```bash
# 输出目录：/d/Users/Claw/水利一张图_UOS_3.24_20260819/  
├── dist/                          # App bundle
│   ├── main.py                    # GTK app entry point
│   ├── webview_page.html          # WKWebView wrapper
│   └── js_bridge.js               # GTK-Node bridge
├── assets/                        # 静态资源
│   ├── leaflet.min.css
│   └── icons/                     # GTK native icons
└── .AppImage                      # 自包含二进制 (可选)
```

**UOS 特色功能**:  
- GNOME integration via app launcher  
- UOS native dialogs via GTK3 wrapper  
- ~/.config/gnome-session/integration  

输出格式：.deb bundle / AppImage（跨发行版兼容）

---

## 🔄 六、自动化执行流程（每日 05:00）

### 脚本架构
```bash
分析对比同步脚本.sh
├── extract (步骤 1)      # unzip V3.15 / V3.24 APK, 提取 META info
├── analyze (步骤 2)      # 基于文档生成 feature_change.txt  
├── prepare (步骤 3)      # 创建 iOS/Win11/UOS 目录结构
├── build (步骤 4-5)      # webpack/electron-builder/bundle
│   ├── build_ios.sh      # Webpack → SPA Bundle + Info.plist  
│   ├── build_win.sh      # electron-builder --win  
│   └── build_uos.sh      # python-pyinstaller or appimage-builder  
└── validate (步骤 6)      # checksum/签名验证
    ├── apksigner verify
    ├── webkit-wkverify
    └── sha256sum archive
```

---

## ✅ 七、已验证构建清单（待生成）

| 验证项 | Android (APK) | iOS (IPA) | Win11 (EXE) | UOS (Bundle) |
|--------|---------------|-----------|-------------|--------------|
| **签名验证** | ✓ `apksigner verify = Verifies` | ⏳ Entitlements check | ⏳ code-sign verification | ⏳ apt verify |
| **安全扫描** | ✓ dexdump -com.sun.net.httpserver=none | ⏳ Node audit 0 vulnerability | ⏳ npm audit secure | ⏳ pip-audit |
| **版本一致性** | ✓ version.json / Manifest.xml | ⏳ Info.plist CFBundleVersion | ⏳ exe manifest | ⏳ debian/copyright |

---

## 📝 八、详细变更日志（v3.15 → v3.24）

### [已记录在官方文档]
- `docs_shuili_v3/05_版本历史与备份.md` — V3.7 ~ V3.24 完整历史  
- `docs_shuili_v3/04_软件变更文档.md` — 核心需求/修复/优化项  
- `水利工程一张图_经验沉淀/04_APK命名与归档规范.md` — v3.15 ~ v3.24索引  

**关键经验教训条目**: v3.21 IIFE闭包陷阱、v3.23 confirm()离线失效、v3.24 类型规范脏数据过滤

---

## 📞 九、下一步操作建议

### ✅ 已完成
1. ✨ 自动化分析脚本模板已创建（步骤 1-3）  
2. ✨ 三端构建目录结构已准备  
3. ✨ version.json / Manifest.xml 元信息提取完成  

### 🔜 待执行（按优先级排序）
1. **iOS WKWebView 离线能力真机测试** (localStorage/file://兜底有效性)  
2. **Win11 Electron IPC bridge 性能评测** (500+标记渲染帧率)  
3. **UOS GTK WebView resize事件触发修复** (window.resize listener)  
4. **定时任务配置**: Windows Task Scheduler `(Daily 05:00)`  

### 📋 建议验证点
- [ ] 智能匹配"一键最匹配"在长列表下的响应 (<1s)  
- [ ] 倒水滴高亮动画在 iOS WKWebView 下的流畅度 (60fps)  
- [ ] UOS GTK native dialog vs WebView popup用户体验对比

---

**报告生成**: 小七 - AI 万能助理  
**原始依据**: `docs_shuili_v3/*.md`, `水利工程一张图_经验沉淀/*.md`  
**自动化脚本路径**: `/d/Users/Claw/分析对比同步脚本.sh`
