# 版本变更记录 v1.38（2026-09-08，内部测试包）

> 配套：水利 v3.62；古建 v3.7.6（同轮五条问题修复）。
> 性质：**用户复检五条问题修复 + 感知专属两项（隐藏/收藏整体移植、管理所九所统一）**。
> 产物：`APK归档/`（真机验证测试包）。

## 一、本次需求

同水利 v3.62 五条（隐藏/收藏二次确认、照片灯箱空白、导入照片落盘、deb 筛选导出照片、deb 图标），另加感知专属：

6. ① 感知补齐隐藏/收藏能力（从水利整体移植）。
7. ④ 感知管理所统一识别与确认（九所）。

## 二、核心改动

### E1 隐藏/收藏整体移植（感知专属）

- 从水利整体移植隐藏子菜单块：`menuTitleOf / PROTECTED_HIDDEN / getHiddenMenus / hideSubMenu(ask 确认) / restoreSubMenu / restoreAllHiddenMenus / openHiddenMenuList` + 设置 hideSet/hideList + 菜单索引注册。
- 收藏 ask 二次确认 + 图标改内联 SVG。

### E2 管理所九所统一（感知专属）

- `normOffice` 重写为 OFFICE_ALIAS 正则表（潮河总干渠管理所→潮河所、怀柔水库管理所→水库所、李家史山管理所→史山所、怀柔地下水源工程管理所→地下水源所……），先去「管理」再匹配。
- `levelOptions("suo")` 严格限于 CANONICAL_OFFICES 九所 + 用户自定义（数据里的「后勤服务中心/防汛物资仓库」不再进入所级下拉）。
- 设置新增「管理所统一确认（智能识别）」：列出全部原始所名+条数，下拉可改，确认后全库统一。

### E3~E5 同水利 v3.62

- 照片灯箱空白（photoFullSrc 大缩略图多级兜底 + onerror 退回缩略图）。
- 导入照片落盘归位（escapeJson 单引号回归修复 + linkPhotoToBuilding）。
- deb 筛选后导出照片（file||data + 筛选遍历 + 统计条）与内联 SVG 图标。

## 三、四平台同步与验证

- canonical = `android-build/perc-v13/assets`；三平台同步。
- 待真机确认项：管理所九所选项与统一确认；ovkmz 导入实测。
