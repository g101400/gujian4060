# 水利感知项目一张图 v1.17 — 版本变更记录（同步水利一张图 v3.41）

> 生成日期：2026-08-29
> 升级来源：水利工程基础信息一张图 v3.41（「三图联动」规范：母版升级自动移植到派生应用）
> 包名：com.shuili.ganzhi · versionCode 22 · versionName 1.17
> 说明：感知项目此前无独立文档目录，本文为 `docs_perc/` 首份变更记录，与 `docs_shuili_v3/版本变更记录_v3.41.md` 配套阅读。

## 一、本次变更范围

1. **发行前预生成知识库**：构建期生成 `assets/kb_building_seed.js`（`window.KB_BUILDING_SEED`，1125 条监测设施，含管理所/站/类型/渠道/子系统/参数/照片/坐标），随 APK/DEB/MSI/EXE/PWA 全平台安装包内置；`ai_module.js init()` 时 `mergeSeed()` 按 buildingId 去重合并进本地 KB，不覆盖用户后续增改。安装即有知识库，无需联网。
2. **智能AI × 知识库查询融合**：与水利/古建同引擎——先查本地 KB（结果标注 **🔒本地**），未命中且允许联网再走大模型（标注 **🌐在线**），未命中且离线给友好提示。全程 `Promise resolve` 不 reject、异常转友好结果，根治偶发「运行错误: script error」。
3. **知识库查询加确认按钮**：知识库管理中的查询需点击确认后执行，防误触。
4. **知识库网页入库**：支持读取普通网页 / 微信公众号文章 / 微博，抽取正文沉淀为 KB 条目；受目标站跨域限制失败时提示改用复制粘贴入库。
5. **智能更新补丁可编辑**：补丁预览由只读文本改为**可编辑输入框**，应用前可逐字段修改。
6. **四平台同步重建**：Android APK / Win(WebView2 MSI+EXE) / UOS(mips64el deb) / iOS PWA 全部重出 v1.17 包。

## 二、关键实现点

| 点 | 说明 |
|---|---|
| 种子生成 | 根目录 `gen_kb_seed_all.js`：VM 沙箱执行 `android-build/perc-v13/assets/data.js`，取 `window.PERCEPTION_DATA`（1125 条），保留 office/station/btype/chan/attrs/photos 并透传 `subsystem` |
| 正文构建 | `buildBuildingBody()` 扩展通用字段渲染：子系统（perc 独有；古建的属地/朝代等字段感知数据没有则自动跳过） |
| 查询融合 | `queryEngine`：本地 `kbSearch()` 优先 → `onlineEnabled()` 且未命中时走 `chat()` → 失败/离线 resolve 友好文案；两处查询入口共用 |
| 种子接线 | `index.html` 在 `data.js` 之后引入 `kb_building_seed.js`（perc-v13 assets、win-webview2、uos-pyqt6 三处） |
| 版本同步 | `bump_version_v341.py`：version.json(真相源) + app.js(`v1.17`) + webroots + make_deb/gen_win_msi/build_ios_zip 版本串；NSIS 脚本版本表手动补齐 |

## 三、验证

- `node --check`：app.js / ai_module.js / kb_building_seed.js 全过；3 处 ai_module.js 副本 md5 一致。
- APK 门禁：aapt2 badging versionCode=22 / versionName=1.17 + apksigner 签名校验通过。
- UOS deb：`verify_myml_ar.py` 通过（ar 成员名匹配 / tar.xz 可解压 / 关键文件齐全）。
- Win：`wix msi validate` 静默通过；NSIS EXE 生成正常。
- iOS：DEPLOY_README 版本 `1.17_20260829` 正确。

## 四、交付与回退

- 交付：`APK归档/四端安装包_20260829_v341/`（感知 APK / shuili-ganzhi_1.17.20260829_mips64el.deb / 感知 Setup.msi+exe / 感知 iOS zip）。
- 回退：`backup_2026-08-29_before_v341/`（bump 前版本文件）；旧包 `四端安装包_20260829_fixed/`（v1.16）保留。
- 已知限制：网页入库受 CORS 限制；iOS PWA 无原生文件桥（导出降级浏览器下载）。

---

# 水利感知项目一张图 v1.22 — 版本变更记录（同步水利一张图 v3.46）

> 生成日期：2026-08-31
> 升级来源：水利工程基础信息一张图 v3.46（「三图联动」规范：母版回归修复自动移植到派生应用）
> 包名：com.shuili.ganzhi · versionCode 27 · versionName 1.22
> 说明：v1.18 / v1.19 / v1.20 / v1.21 各版的逐版变更已记入 `docs_shuili_v3/04_软件变更文档.md`（第十~十三节）与 `05_版本历史与备份.md`，感知作为三图之一同步升级。本篇补 v1.22 总回归修复记录。

## 一、本次变更范围（9 项回归修复，纯修复版）

1. **F1 导出建筑物表格 script error 修复**：`doExportTable` 补齐字段兜底 + 单行 try/catch。
2. **F2 导出照片 script error 修复**：导出文件名组装走统一入口 + 空值保护。
3. **F3 全菜单回归验证**：全部菜单点击无 script error。
4. **F4 导入压缩包照片退回 v3.42 三级匹配**：`zipName→zipFolder→fname` 收敛 scope（感知 5 级组织，案例 `史山.zip → 三扬分水闸`）。
5. **F5 筛选自定义组织/类型「添加成功」真正生效**：设置→组织与类型管理增改回写持久化并同步建筑物（`getCustomOffices` ×6 含感知三维度）。
6. **F6 历史关键词上移**：查询关键词紧下方。
7. **F7 智能 AI 无 KB 条目时注入本域组织/类型数据增强**（含感知独有 `subsystem` 元数据）。
8. **F8 智能 AI 结果页「进一步操作」引导 + 关键词复制**（单击复制 / 双击复制并查询）。
9. **F9 设置密钥隐藏明文 + 复制需密码 3305**：不再明文显示当前密钥，复制须密码 3305 校验通过。

## 二、关键实现点

| 点 | 说明 |
|---|---|
| 版本同步 | `bump_version_v346.py`：version.json(真相源) + app.js(`v1.22`，修正此前错误残留 `v3.45`) + 6 webroot + make_deb/gen_win_msi/build_ios_zip 版本串；NSIS 脚本版本表手动补齐 |
| 感知独有 | 5 类分类方案（信息系统分类/感知子系统/中类/子类/信息传输方式）、fieldSchema 含 `subsystem`、data.js 取 `window.PERCEPTION_DATA` |
| 密钥 | 与水利/古建同款隐藏明文 + 密码 3305（`askPwd` ×7、`3305` 校验 ×5） |

## 三、验证

- `node --check`：app.js / ai_module.js 全过；perc `APP_VERSION` 由错误残留 `v3.45` 修正为 `v1.22`。
- APK 门禁：aapt2 badging versionCode=27 / versionName=1.22 + apksigner 签名校验通过。
- UOS deb：`verify_myml_ar.py` 通过（`shuili-ganzhi_1.22.20260831_mips64el.deb`）。
- Win：WiX7 MSI + NSIS EXE 生成正常。
- iOS：DEPLOY_README 版本 `1.22_20260831` 正确。

## 四、交付与回退

- 交付：`APK归档/四端安装包_20260831_v346/`（感知 APK / shuili-ganzhi_1.22.20260831_mips64el.deb / 水利感知项目一张图 Setup.msi+exe / 水利感知项目一张图_iOS_1.22_20260831_可托管.zip）。
- 回退：`backup_2026-08-31_before_v346/`（bump 前版本文件，含 perc-v13 assets）；旧全量 `backup_2026-08-30_before_v345/`（638 文件）可作全量回退。
- 已知限制：网页入库受 CORS 限制；iOS PWA 无原生文件桥（导出降级浏览器下载）；桌面端（Win/UOS/iOS）照片压缩仅安卓完整。
