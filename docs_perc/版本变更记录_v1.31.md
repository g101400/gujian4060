# 水利感知项目一张图 v1.31 — 版本变更记录（同步水利一张图 v3.55）

> 生成日期：2026-09-05
> 升级来源：水利工程基础信息一张图 v3.55（三图联动规范：母版升级自动移植到派生应用）
> 包名：com.shuili.ganzhi · versionCode 31 · versionName 1.31
> 性质：代码级更新（先修复不打包），镜像/文档已就绪，安装包下次发版统一构建。
> 改动前备份：`backup_2026-09-05_before_kbslice_20260905_140441/`

## 一、本次变更范围（与水利 v3.55 / 古建 v3.7.3 同源）

1. **知识库切片保持语句相对完整**：`kb_rag.js chunkText` + `ai_module.js kbSliceText` 双实现——整句为最小打包粒度（段落→句末标点→逗号级→极端硬切兜底），重叠按整句回溯并预留预算（`size - s.length - 1`）保证不超长，切片绝不在句中截断。
2. **保存前先切片再落盘**：`kbSave` 统一写入口——新增/导入/网页入库/OCR入库/Hermes沉淀/查询保存先按句切片（`chunks[id]` + 签名缓存，正文变更才重切）随条目保存；`kbRemove` 同步清理；向量索引优先复用已存切片。
3. **RAG 智能检索四端同步**：混合检索（向量 0.45 + BM25 0.35 + 模糊 0.20）、内容反查条目、模糊/智能查询、智能生成提示词、大模型智能问答（引用标注，失败降级抽取式答案）、记忆管理 + Hermes 自我学习；保留感知语义（subsystem / equip_type 元数据随条目入索引）。
4. **OCR 外部文档入库同步**：pdf/docx/xlsx/csv/txt/md 智能转 Markdown（本地化 Tesseract 5.1.1 + pdf.js 3.11，`assets/ocr/` 随包）。
5. **升级备份增强同步**：导出自定义文件夹/文件名（默认 `感知设备一张图_YYYYMMDD.bak`），导入「请确认这是最新备份，导入会覆盖程序中的全部数据」预检 + 合并/覆盖双模式；内部版（偶数通道）百度网盘自动升级。
6. **信息与帮助更新**：版本变更增补 v1.26/v1.27/v1.29/v1.31；四端功能对照单插入 v1.31 行；功能介绍新增 ⑰ RAG 检索、⑱ 入库与备份。

## 二、四平台同步

- canonical = `android-build/perc-v13/assets`（version.json 真相源 v1.31/code 31/2026-09-05）。
- 镜像：uos-pyqt6/webroot、win-webview2/webroot、win-webview2/publish_win_perc/webroot —— 17 文件级同步，核心 md5 全一致，`node --check` 通过。
- 小程序基线同步 v1.31（jingyin）。

## 三、测试

| 套件 | 结果 |
|---|---|
| kb_slice_test.js | 11/11 |
| kb_rag_test.js | 22/22 |
| up_test.js | 14/14 |
| kb_ocr_test.js | 7/7 |
| help_menu_test.js（感知帮助子菜单运行时） | 6/6 |
| load_test.js 感知启动 | OK |

## 四、下次发版

感知 公开 1.31 / 内部 1.32（versionCode 31/32），APK/MSI+EXE/UOS deb/iOS PWA 统一出包；UOS deb 按 `verify_myml_ar.py` 门禁。
