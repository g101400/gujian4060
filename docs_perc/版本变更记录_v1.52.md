# 版本变更记录 v1.52（水利感知项目一张图 · 内部版 · 三端同批）

- 版本：**v1.52**（内部偶数通道）｜构建日期：2026-09-16
- 同批版本：水利 **v3.76**／古建 **v3.7.13**
- 归档：`APK归档/四端安装包_20260916_v376_int`（24 个文件）
- 公开派生：感知 **v1.53**（奇数通道，GitHub Pages，由 `derive_public_v377.py` 派生）

> 本轮为**三端同批同步发布批**：感知侧自 **v1.20** 起无独立新需求（差异化修复/智能AI增强已在 v1.20 落码并保持），
> 本轮核心是把水利 v3.76 的 **AI·知识库增强** 经共享模块回灌到感知端，并完成四平台（Android/Win11/统信UOS/iOS PWA）同步打包。

## 一、本批实际改动

### 1. 共享 AI·知识库增强（经共享模块落感知端）
共享模块 `kb_rag.js` / `kb_vector.js` / `ai_module.js` 已随同步在感知 webroot 内置，感知端获得：
| # | 项 | 要点 |
|---|---|---|
| D-1 | 智能推荐复用 `kbHybridSearch`/`KBRag` | 替代孤立文本匹配；混合权重 向量 0.45 / BM25 0.35 / 模糊 0.20（权重可学习）；修复「晚 3」复制条件 bug |
| D-2 | KB 属性结构化索引 | `kb_vector.js` 数值等值/范围查询与向量召回融合 |
| D-3 | RAG 引用溯源 | chunk 原文高亮 + 置信度 + 混合权重可学习 |
| D-4 | 向量索引增量构建 | 切片签名缓存 + `ts` diff，加速启动 |
| D-5 | KB 多源冲突裁决 | OCR / 网页 / Hermes 来源置信度 + 人工修订优先 + 冲突提示 |
| D-6 | query→推荐→报告→KB 闭环 | 报告导出 + 结果写回知识库 |

### 2. 统信运行慢优化（共享模块同步）
首屏优先渲染 + 启动耗时 profiling + 延迟非关键初始化（AI 菜单 / CtxMenu / cleanInbox）。

### 3. 保持项（不删不减，回归保持）
- 口令硬化（v1.50）：源码无明文开发者分机号，AES-GCM(PBKDF2) 凭证校验 + 运行时输入 + 构建期注入密钥。
- 数据：1125 条监测设施知识库种子（含子系统/参数/照片/坐标）随包内置。
- 四平台（Android/Win11/UOS/iOS PWA）webroot 全量同步，UOS webroot 指向 `uos-pyqt6` 修正避免误用水利数据。

## 二、验证（夜班门禁）
- `sync_all_mirrors.py --check` EXIT=0（11 镜像 + 跨产品 7 公共模块 md5 一致，v1.52）
- `verify_pass.py int`（四端安装包_20260916_v376_int）21/21 PASS；`verify_pkgs_smartquery.py` 含感知 4 架构 deb 全 PASS
- `sync_weapp_version.py --check` ✅ 小程序基线已对齐 v3.76（2026-09-16）

## 三、说明
- 感知侧独立特性（v1.20 差异化修复、智能AI×知识库融合、联网核实等）保持，本轮不新增、不删减。
- 公开派生 v1.53 由奇数通道脚本派生，公开 PWA 经脱敏（不含真实 sk-or-v1 密钥，门禁已扫 `公开PWA_20260916_v377_pub` 干净）。
