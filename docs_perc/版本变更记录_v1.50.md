# 版本变更记录 v1.50（水利感知项目一张图 · 内部版 · 三端同批）

- 版本：**v1.50**（内部偶数通道）｜构建日期：2026-09-13
- 同批版本：水利 **v3.74**／古建 **v3.7.12**
- 归档：`APK归档/四端安装包_20260913_v374_int`
- 回退备份：`backup_2026-09-13_before_v374`
- 提版脚本：`bump_version_v374_int.py`
- 公开派生：感知 **v1.51**（奇数通道，GitHub Pages）

> 感知核心元数据保持：subsystem 子系统（视频监控/雨水情/大坝安全/地下水源四类）+ equip_type 设备类型。

## 一、本批实际改动

### 1. 口令硬化（O1，同水利）
- 去除源码明文「开发者分机号」，改 AES-GCM(PBKDF2) 凭证校验 + 运行时输入 + 构建期注入密钥（本地 `.sec_pass` 不随包）。
- `secure_boot.js` 门闸默认关闭，仅借用校验能力；公开版免密放行。
- 明文 `3305` 计数 = 0（已 grep 核验）。

### 2. 局/管理处数据化（O5，选 B 数据化）
- 编辑表单 `openEdit` 新增「局」「管理处」下拉（`levelOptions("ju")` / `guanchuOptions()`，默认回退 `getOrgDefaults`）。
- `appSave` 写 `b.ju` / `b.guanchu`。
- ovkmz 导出 `ExtendedData` 补写「局」「管理处」；CSV 导出 `baseCols` 加「局」列。
- CSV / ovkmz 导入与 AI `_orgContextForQuery` 经 `FIELD_ALIAS` 已覆盖局/管理处。

### 3. 水工建筑物底图种子（沿用 v1.48）
- `water_data.js`(557 条) 沿用 v1.48，本轮无变更。

## 二、门禁与验证（全绿）

- `sync_all_mirrors.py --check`：11 镜像 + 跨产品 7 模块 md5 一致（EXIT=0）
- `verify_pass.py <归档> int`：21/21（感知 v1.50 数据标记 + 版本）
- `verify_pkgs_smartquery.py`：pub+int 52/52
- `node --check`：canonical `perc-v13/assets/app.js` + 各镜像全过
- 口令硬化回归：明文 `3305` = 0

## 三、四端产物

见 `APK归档/四端安装包_20260913_v374_int/`：感知 APK / 4 架构 deb / Win MSI+EXE / iOS PWA zip（含内部加密版 + 公开派生 v1.51）。

## 四、发布与分发

- GitHub Release `ganzhi-yitu5090@v1.50`（资产下载正常）；GitHub Pages 公开版 `ganzhi-yitu-5090pub@v1.51` 已开启。
- 百度网盘 `yitu-release/四端全量_v3.74_1.50_3.7.12_20260913/` 四平台 26 文件。
- 交付须知：须重新安装新 APK 方能看到口令硬化 + 局/管理处字段。
