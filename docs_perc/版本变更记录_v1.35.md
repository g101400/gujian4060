# 版本变更记录 v1.35（2026-09-07，公开测试版）

> 配套：水利 v3.59；古建 v3.7.5（均奇数公开通道）；私有内部仓本轮不动（仍 v1.34）。
> 性质：**知识库导出交互升级 + 数据脱敏机制修复**（功能不删不减）。
> 改动前备份：`sync_all_mirrors.py` 自动备份（各镜像 `*_bak_v1.34_20260907_*`）；canonical 直接 git 可回退。

## 一、本次需求

1. 知识库管理导出 md/txt/html 允许**自定义或选择保存文件夹**，文件名默认「知识库+日期」，可改。
2. 导出 txt/html 与升级备份/其它导出按同一交互执行（自定义文件夹+文件名）。
3. 检查各子菜单不再出现「运行错误:script error」；智能AI 保存知识库先切片保持语句完整。
4. 三应用共享模块改一处同步三处；同步 Win/APK/苹果端及水利/古建全平台。
5. 信息与帮助（四端功能对照单 / 版本变更 / 功能介绍）更新到最新版本。
6. 数据脱敏：公开渠道内置演示数据，杜绝单位内部感知设备信息进入公开分发。

## 二、核心改动

### E1 知识库导出 md/txt/html 自定义文件名+文件夹（kbExport 重构，三应用同步）

- 文件名默认 `知识库_YYYYMMDD.fmt` 可改（自动补后缀）+ 可填保存文件夹。
- 安卓 `window.Android.saveBlobTo(dataUrl, folder, name)` 落 `Download/<folder>/`；Win/统信/网页回退系统下载目录。
- 共享模块 `ai_module.js`（kbExport 在共享模块，三应用 md5 一致）。

### E2 数据脱敏机制修复（本版安全重点）

- 问题：gh_sync.py 推公开仓仅排除 ai_seed.js，`data.js / kb_building_seed.js` 原样上传，2026-09-05 深夜把 v1.34（内部真实感知设备种子）带入公开仓 `ganzhi-yitu-5090pub` 历史。
- 修复：gh_sync.py pub 通道 demo_replace（data.js / kb_building_seed.js 一律用 *.demo.js 替换上传）；私有仓 int 保持真实数据全量。
- 本地：`strip_internal.py --public` 已把 perc-v13 assets 切演示种子（真实备份 `.strip_backup/perc/*.real.bak`）。
- 遗留风险：公开仓历史 commit 仍含真实数据，需用户决策（转私有/重建/联系 GitHub）。本轮发布仅更新公开仓；私有仓不动（v1.34 内部已发 Release 保持有效）。

### E3 信息与帮助更新至 v1.35

- `CHANGES` 头插 v1.35；`PLATFORM_COMPARE` 头插 v1.35（知识库导出自定义 / 数据脱敏 / GitHub 升级 / 备忘录 / 每页退出+三击 对照行）。
- 功能介绍追加 ⑲ 知识库导出与 GitHub 升级。
- `version.json`：version 1.35 / versionCode 35 / buildDate 2026-09-07 / desc 追加 v1.35 段。

## 三、四平台同步

- canonical = `android-build/perc-v13/assets`。
- 镜像：uos-pyqt6/webroot、win-webview2/publish_win_perc/webroot、win-webview2/webroot —— 核心文件 md5 全一致，node --check 全过。

## 四、测试

| 套件 | 结果 |
|---|---|
| node --check（app.js/ai_module.js/kb_rag.js/ctx_menu.js） | 全过 |
| sync_all_mirrors.py | 全部镜像与 canonical 一致 |
| strip 校验（公开仓种子=演示 md5） | 通过 |

## 五、待办

- 重出四平台安装包 → 覆盖公开仓 Release → 同步网盘。
- App 内升级提示/版本变更/对照单随 v1.35。
