# 水利感知项目一张图 v1.25 — 版本变更记录（同步水利一张图 v3.49）

> 生成日期：2026-09-02
> 升级来源：水利工程基础信息一张图 v3.49（「三图联动」规范：母版升级自动移植到派生应用）
> 包名：com.shuili.ganzhi · versionCode 28 · versionName 1.25
> 说明：v1.23 / v1.24 各版逐版变更已记入 `docs_shuili_v3/04_软件变更文档.md` 与 `版本变更记录_v3.48.md`（第十六节）；感知作为三图之一同步 v3.49 增强。本篇补 v1.25 总回归修复记录。

## 一、本次变更范围（与水利 v3.49 同源，文档与诊断增强）

1. **全平台启动诊断 logRuntime（版本号自动跟随）**：`perc-v13/assets/app.js` 新增 `logRuntime()`，启动时把「产品名 + 版本号（读 `APP_VERSION`） + 构建日期 + 平台」写 `console.log` 与 `localStorage["yitu_runtime_log"]`；版本一升日志自动跟随。UOS 端 `server.py._startup_log()` 兜底（铁律-UOS5~9 已落地，本版保留）。
2. **子菜单「帮助」→「功能介绍」改名 + 正文补全**：`showHelp()` 标题改「功能介绍」，正文扩充感知语义的 16 项功能（含感知独有「感知设施设备 / 子系统 / 设备类型」等）；主菜单与子菜单两处同步改名，旧「帮助」残留清零。
3. **四端功能对照单 + 版本变更补到最新**：`PLATFORM_COMPARE`/`CHANGES` 插入 v1.25（2026-09-02）条目。
4. **版本统一提升**：感知 v1.24→**v1.25**，构建日期 2026-09-02；`version.json` + app.js + 全部 webroot + 构建脚本 + 小程序基线全量同步。

## 二、关键实现点

| 点 | 说明 |
|---|---|
| 版本同步 | `bump_version_v349.py`：version.json(真相源) + app.js(`v1.25`) + 6 webroot + make_deb/gen_win_msi/build_ios_zip 版本串；小程序 `sync_weapp_version.py DEFAULTS.perc=1.25` |
| 感知独有 | 5 类分类方案（信息系统分类/感知子系统/中类/子类/信息传输方式）、fieldSchema 含 `subsystem`、data.js 取 `window.PERCEPTION_DATA`（1125 条） |
| 启动诊断 | `logRuntime()` 三端同源注入，版本号读 `APP_VERSION`（随 version.json 自动更新） |

## 三、验证

- `node --check`：app.js / ai_module.js 全过；perc `APP_VERSION` = `v1.25`。
- UOS deb：`verify_myml_ar.py` 通过（`shuili-ganzhi_1.25.20260902_<arch>.deb`，四架构 ×1）。
- iOS：DEPLOY_README 版本 `1.25_20260902` 正确。
- 同步门禁：`sync_all_mirrors.py --check` EXIT=0；`sync_weapp_version.py --check` 基线 v1.25。

## 四、交付与回退

- 交付：`APK归档/四端安装包_20260902_v349/`（感知 APK / shuili-ganzhi_1.25.20260902_* deb×4 / 水利感知项目一张图 Setup.msi+exe(构建机产) / 水利感知项目一张图_iOS_1.25_20260902_可托管.zip）。
- 回退：`backup_2026-09-02_before_v349/`（bump 前版本文件，含 perc-v13 assets）。
- 已知限制：网页入库受 CORS 限制；iOS PWA 无原生文件桥（导出降级浏览器下载）；桌面端照片压缩仅安卓完整。
