# 配置 / 密钥 / 密码 明文导出清单

- **导出时间**：2026-09-20
- **来源**：`aowei_win10` 三端四平台项目（已暂停开发）
- **操作性质**：仅复制，原 `aowei_win10` 与 `android_build` 的源码及密钥文件**均未改动**
- **目的**：将配置与凭证以明文形式迁移至 `new4060`，供新三端四平台复用

## 已导出文件（共 7 个，全部保持明文原样）

| 新路径（相对于 new4060/） | 大小 | 类型说明 |
|---|---|---|
| `aowei_win10/gujian_app/SECRETS.md` | 3164 B | 密钥 / 账号密码汇总（古建前端） |
| `aowei_win10/three_platforms/ios/gujian_pwa/SECRETS.md` | 3164 B | 同上（iOS PWA 端） |
| `aowei_win10/uos_gujian/jingmi_app/SECRETS.md` | 3164 B | 同上（统信 UOS 端） |
| `aowei_win10/win11_gujian/jingmi_app/SECRETS.md` | 3164 B | 同上（Win11 端） |
| `aowei_win10/gujian_app/secrets/config.js` | 485 B | 运行期密钥配置（古建前端） |
| `aowei_win10/gujian_app/secrets/config.local.js` | 597 B | 本地密钥配置（古建前端） |
| `android_build/keystore/release.keystore` | 2730 B | 安卓发布签名密钥库（Java keystore 二进制） |

## 说明

- 四份 `SECRETS.md` 内容一致（同一套密钥在四端复用），改一处需同步其余三处。
- keystore 的 store/key 密码、alias、各 API key、账号口令等明文记录在上述 `SECRETS.md` 与 `secrets/config*.js` 中，请在本清单同目录查看原文。
- 已**筛查并确认不含明文密钥、故未纳入**的文件：三个前端 app 的 `js/ai_config.js`、以及 `android_build` 各工程的 `gradle.properties` / `local.properties`（均为 `-`，即无密码字段）。

## ⚠️ 安全须知（重要）

1. `new4060` 现含明文密钥与安卓签名密钥库，等同于生产凭证。**请勿提交到公开代码仓库，勿随意外发**。
2. `release.keystore` 为安卓发布签名，一旦泄露或丢失将导致已发布应用无法更新或被冒用，请额外做离线备份妥善保管。
3. 新项目复用时，按上表"新路径"的镜像结构，将文件放回应端对应目录即可。

## 接收确认

- 导出人：__________
- 接收人：__________
- 日期：__________
