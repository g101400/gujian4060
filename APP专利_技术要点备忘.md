# APP 专利 · 技术要点备忘（交底书素材库）

> 长期任务 B 交付物（2026-09-15 启动）。开发中积累专利算法，季度性送代理做 FTO 检索 + 撰写。
> 本文件先沉淀**最稳的 2 个候选**（跨端共享、渲染优化）；其余 5 个后续补。
> 格式为"交底书要点"，非完整交底书——完整版由代理机构按此扩写。

---

## 候选 #2（优先申报）｜跨端/跨浏览器偏好共享的本地状态桥接方法

**技术领域**：离线 Web 应用 / 本地状态一致性。

**背景问题**：
- Web 应用将用户偏好（收藏菜单 favMenus、隐藏菜单 hiddenMenus、照片元数据）存入 `localStorage`，而 `localStorage` 按"浏览器 + 源"隔离——同一应用用 360 安全浏览器与统信浏览器分别打开，状态互相不可见。
- 通用浏览器无原生文件系统桥，照片只能以 base64 内联进 `localStorage`，体积暴涨并进一步拖慢启动。

**核心方案**（已落地于 UOS 原生壳）：
1. 原生壳注入 `window.fs` 文件桥，路径白名单限定在 `webroot/` 内。
2. 应用启动优先从 `webroot/state.json`（共享 JSON）加载偏好，缺失时回退 `localStorage`。
3. 写操作经桥**原子落盘**（写临时文件 → 重命名），保证多浏览器并发读一致。
4. 无桥的通用浏览器降级 `localStorage`，并提示"状态仅本浏览器生效"。

**有益效果**：多端/多浏览器偏好天然一致；照片走磁盘路径不再撑大 `localStorage`；跨端同步零服务器、纯本地。

**实施方式（伪代码）**：
```
init(): state = fs.readJSON('webroot/state.json') || localStorage.load()
save(key,val): state[key]=val; fs.atomicWrite('webroot/state.json', state)
```
**效果数据**：现状为 localStorage 隔离导致双浏览器状态分裂（已复现）；桥接后所有壳内浏览器读同一文件，一致性 100%。

---

## 候选 #3（优先申报）｜视窗裁剪 + popup 懒构造的离线地图海量点渲染优化方法

**技术领域**：离线地图 / 前端高性能渲染。

**背景问题**：离线地图一次性在 DOM 建全量海量点（水利设施成千上万），导致首屏卡顿、白屏、平移掉帧。

**核心方案**（已落地于 app.js 渲染层）：
1. **视窗裁剪**：仅对当前地图视窗（含 15% 缓冲带）内的点建 marker，视窗外不建 DOM。
2. **复用**：已存在的 marker 不重建；平移时增量增删，而非全量重绘。
3. **popup 懒构造**：marker 的 popup 内容在点击时才构造，不在建点时预生成。
4. **分帧（可选）**：`requestAnimationFrame` 分批建点，避免长任务阻塞主线程。

**有益效果**：DOM 节点数 ≈ 视窗内点数（远小于总量），首屏与平移流畅；直接缓解统信环境运行慢问题。

**实施方式（伪代码）**：
```
onMoveEnd(): view = map.getBounds().pad(0.15)
  for b in BUILDINGS: if inView(b,view): ensureMarker(b) else removeMarker(b)
onMarkerClick(b): popup = lazyBuild(b)   // 点击才构造
```
**效果数据**：渲染节点数由"全量 N"降为"视窗内 M（M≪N）"，首屏建点耗时随视窗缩放，与总量解耦。

---

## 待补候选（季度送检前补齐）

- #1 离线混合检索（向量0.45+BM25 0.35+模糊0.20）+ 属性结构化融合的建筑物智能推荐方法
- #4 建筑物类型反选补集筛选方法（`invertTypes` + `passFilter` 语义翻转）
- #5 口令硬化 + AES-GCM(PBKDF2) 本地隐私保护启动方法（已落地）
- #6 OCR + 网页入库 + Hermes 自学习的知识自动增长闭环方法
- #7 随包预生成建筑骨干 + 切片签名缓存的离线 KB 增量同步方法

---

## 推进节奏（A→B→C→D）

- **A 软著**：清单 + 源码定位已交付（`软著申请_材料清单与源码定位.md`），去 AI 化随 bump 增量。
- **B APP 专利**：本文件已沉淀 #2、#3；余 5 个待补。
- **C 产品需求 9 项**（待启动）：见 MEMORY.md 🅲。
- **D 智能 AI/知识库 6 项**（待启动）：见 MEMORY.md 🅳。
