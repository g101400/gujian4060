# 版本历史与备份

## 水利工程基础信息一张图 APP

| 项目 | 内容 |
|------|------|
| 文档编号 | VER-SHUILI |
| 更新日期 | 2026-08-25 |
| 维护人 | 炎冰 / 小七 |

---

## 1 版本历史

### v3.31（2026-08-25）— 当前版本

| 项 | 内容 |
|----|------|
| 版本号 | v3.31 |
| versionCode / versionName | 34 / 3.31 |
| 构建日期 | 2026-08-25 |
| 源码快照 | `D:/Users/Claw/android-build/shuili-v329/assets/`（canonical，四平台 webroot 由此同步） |
| 回退备份 | `D:/Users/Claw/backup_2026-08-26/`（源码 webroot ×9 + 已交付四平台包，2026-08-26 实际生成） |
| 交付文件 | `D:/Users/Claw/APK归档/四端安装包_20260825_v331/` |

**迭代主线（v3.30.3 → v3.31）：** ovobj/obj 导入导出全面可用 + 建筑与照片同存不丢 + 导出前压缩 + UOS 架构匹配 + 四平台源码一致性。

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | 导入 ovobj/obj 点了没反应、不知从哪个文件导入 | 仅 `pickFiles("*/*")` 直调系统选择器，无引导、无文件夹入口 | `ovobj_bridge.js` 增「选单个文件 / 选整个文件夹批量」对话框，明示可选后缀（.ovobj/.obj/.txt/.csv） |
| 2 | 运行错误：Script error. @0:0 | Java `escapeJson()` 未转义单引号，而 `evaluateJavascript("onPickFiles('"+json+"')")` 用单引号包裹 → 文件名含 `'` 直接崩 | 三产品 `MainActivity.java` 的 `escapeJson` 增 `.replace("'","\\'")` |
| 3 | 无法像 ovkmz 那样选文件夹 | 全端只有文件选择桥，无目录桥 | 新增 `pickFolder(exts)`：`ACTION_OPEN_DOCUMENT_TREE` + `listTreeFiles` 递归（≤6 层）+ 后缀过滤 + 复用 `onPickFiles` 路由 |
| 4 | 建筑物与照片同时添加 → 已填信息丢失 | 表单文字只在 `appSave` 时回写 `editing`，任何表单重建（+增加参数 / 原生选图返回）都丢 | `openEdit` 绑定 `oninput → flushEditFields()` 实时回写 |
| 5 | 导出 zip 照片体积过大 | 导出直接塞原图 | `addPhotoToZip` >1.5MB 走 `canvasCompress`；原生导出读压缩后文件 |
| 6 | DEB 安装提示架构不匹配 | `build_uos_deb.py` 硬编码 `Architecture: all`；`build_deb.sh` 取构建主机架构 | 固化架构对照表（3A3000/3A4000→mips64el、3A5000+→loongarch64、FT2000/鲲鹏→arm64、其余→amd64），新增 `--arch` / `ARCH=` 覆盖 |
| 7 | 四平台源码分叉 | UOS webroot 落后到 v3.30.1，`index.html` 未加载 `ovobj_bridge.js` | 三产品 × Win/UOS webroot 全量同步 canonical，补 `<script src="ovobj_bridge.js">` |

**版本号全链路 8 处（本次同步 27 条）：** `assets/version.json`（真相源）→ `assets/app.js`(APP_VERSION+APP_BUILD_DATE) → `AndroidManifest.xml`(versionCode/versionName) → 6 份 webroot(version.json/app.js/ovobj_bridge.js) → `build_msi.wxs`×3 → `build_deb.sh`×3 → `make_deb.py`/`make_msi.py`/`gen_win_msi.py`/`build_ios_zip.py` → `build_four_ends.sh` → 本文档 + `04_软件变更文档.md`。

**交付状态（2026-08-26 实际重新生成）：** 8/25 仅落地源码与文档，APK 未实际出包（停留在 v3.30.1），用户设备跑旧包复现全部 bug。2026-08-26 已用 `release_v331.sh` 实际重建三产品 × 四平台并归档 `APK归档/四端安装包_20260825_v331/`：水利 v3.31 / 感知 v1.7 / 古建 v1.9 各含 APK（`aapt2 badging` + `apksigner verify` 全过）+ 统信 UOS DEB（`Architecture: mips64el`，已验证 control）+ Win11 MSI+NSIS EXE + iOS PWA ZIP。回退备份见 `backup_2026-08-26/`。

### v3.30.3（2026-08-24）

| 项 | 内容 |
|----|------|
| 版本号 | v3.30.3 |
| versionCode / versionName | 33 / 3.30.3 |
| 构建日期 | 2026-08-24 |
| 源码快照 | `D:/Users/aowwei_app/webroot_shuili/`（2026-08-25 由 android-build/shuili-v329/assets 同步至 v3.30.3，消除多副本分叉） |
| 交付文件 | `D:/Users/Claw/APK归档/四端安装包_20260824_v33/水利工程一张图/`（4 端 + 对照单） |

**迭代主线（v3.30.1→v3.30.3）：** 照片导入/性能/老 WebView 兼容三大修复。
- v3.30.1：安卓 ZIP 照片导入「显示为 0」根因修复（中文条目名 GBK/GB18030 乱码 + 嵌套目录层级丢失，改用 `ZipFile(File,Charset)` + detectZipCharset + 完整相对目录）。
- v3.30.2：Bug A（选照片后对勾丢失，保留 DOM 而非重建 innerHTML）；Bug B（ambiguous 确认保存 0 张，finishPhotoMatch 早于 DOM 写入误调 maybeCleanInbox 删光临时目录，删除该调用）。
- v3.30.3：照片性能三件套（筛选弹窗懒构造 / ensureThumb 持久缩略图 / >1.5M 导入压缩默认 500K）+ ovobj·obj 奥维坐标导入导出 + 老 WebView ES6 polyfill（Array.find/from/Object.assign/TextDecoder 兜底，修复 Script error @0:0）。

**回归确认：** 全 App 风格一致，未删除任何已有功能与菜单；六 webroot（三产品源 + 三桌面 webroot）版本对齐；桌面端原生桥真实实现 unzipImages/linkPhoto/fileSha256/photoSha256/cleanInbox（此前 not supported 属残废）。详见 `04_软件变更文档.md` 的「2026-08-24」章节。

### v3.26（2026-08-21）— 历史版本

| 项 | 内容 |
|----|------|
| 版本号 | v3.26 |
| versionCode / versionName | 26 / 3.26 |
| 构建日期 | 2026-08-21 |
| APK 大小 | ~8.2 MB |
| 交付文件 | `D:/Users/Claw/APK归档/四端安装包_20260821/水利工程一张图V3.26_20260821_5090.apk`、统信 UOS DEB、Win11 x86_64 安装包、Apple HTTPS PWA 可托管 ZIP（同目录） |
| 源码快照 | `D:/Users/aowwei_app/webroot_shuili/`（app.js / index.html） |
| 备份路径 | `D:/Users/aowwei_app/backup/before_v326_20260821/`（回滚点，含 v3.25 旧版） |

**功能（地图操作控件 · 需求①）：** 页面右侧中部新增「＋ 放大 / － 缩小 / ☆ 收藏位置 / ⌂ 返回收藏位置」四个悬浮按钮，收藏存本地、一键跳回，离线可用。

**功能（管理所名称统一 · 需求③）：** 新增 `normOffice()` 统一处理——导入/导出去「管理」二字（温泉管理所→温泉所、埝头管理所→埝头所）、潮河管理所/潮河总干渠管理所→潮河所；查询/筛选忽略「管理」（史山所=史山管理所）；管理站为管理所下一级单位、独立字段不合并。覆盖 CSV/XLSX 导入、ovkmz 导入、表格导出、ovkmz 导出、筛选去重、查询匹配。

**功能（ZIP 多层目录导入 · 需求④）：** 导入表格/照片 ZIP 递归搜索全部层级目录。

**功能（四端功能对照表升级 · 需求⑤ + 架构对照表固化 · 需求7）：** App 内对照单新增 v3.26 行；`gen_compare.py` 新增国产平台架构对照表：3A3000/4000→mips64el、3A5000+→loongarch64、FT2000/鲲鹏→arm64、其余→amd64。

**修复（UOS DEB 龙芯安装失败 · 需求⑥）：** data/control.tar.gz 改用 GNU_FORMAT（无 PAX 头）+ data.tar.gz 显式写入目录条目（./opt/、./opt/shuili-map/ 及子目录）+ 路径前缀修复，解决龙芯 3A4000/内核 4.19 统信 1050 安装报「无法创建 /opt/shuili-map/app.js.dpkg-new: 没有那个文件或目录」及首轮「不支持的 PAX tar 头部类型 'x'」。

**回归确认：** 需求②（图标颜色+形状双维度 + 图例）、需求⑦（查询/筛选置顶 + 计数 + 圈选跳转）、导入同名三选一/SHA-256、智能匹配、每页退出 + 三击空白、执行中「请稍后」等已实现项无回退；全 App 风格一致，未删除任何已有功能与菜单。

**变更文件清单：**
| 文件 | 变更类型 |
|------|----------|
| app.js | 修改（新增 normOffice / appBookmark / appGotoBookmark / 地图操作控件绑定；导入导出查询筛选应用统一命名；CHANGES/PLATFORM_COMPARE 加 v3.26；版本 v3.26） |
| index.html | 修改（新增 `#mapCtrl` 控件组与 CSS） |
| AndroidManifest.xml | 版本号 26 / 3.26 |
| assets/version.json | 更新至 v3.26 |
| build_deb.py | 修改（GNU_FORMAT + 目录条目 + opt/shuili-map 前缀） |
| verify_pkg.py | 修改（新增目录条目 + 前缀校验） |
| gen_compare.py | 修改（v3.26 功能行 + 架构对照表 + NSIS 描述） |
| SKILL.md | 修改（DEB 三坑完整记录） |

### v3.25（2026-08-20）— 历史版本

| 项 | 内容 |
|----|------|
| 版本号 | v3.25 |
| versionCode / versionName | 25 / 3.25 |
| 构建日期 | 2026-08-20 |
| APK 大小 | ~8.2 MB |
| 交付文件 | `D:/Users/Claw/APK归档/四端安装包_20260820/水利工程一张图V3.25_20260820_5090.apk`、统信 UOS DEB、Win11 x86_64 安装包、Apple HTTPS PWA 可托管 ZIP（同目录） |
| 源码快照 | `D:/Users/aowwei_app/webroot_shuili/`（app.js / index.html） |
| 备份路径 | `D:/Users/aowwei_app/backup/before_v325_20260820/`（回滚点，含 v3.24 旧版） |

**功能（建筑物双维度区分 · 需求②）：** 不同建筑物类型用「颜色 + 形状」双重区分——标记倒水滴填充色取类型专属色（`colorForType`），中心白点内嵌该类型专属形状符号（`shapeForType`：● ■ ▲ ◆ ★ ⬢ ✚ ✖ 循环对应）。左下角图例同步升级：每个类型项展示「彩色形状符号 + 类型名称」，一眼可辨类型归属。

**回归确认（需求③④⑤⑥⑦及四端对照单）：** 经代码逐条核对，此前 v3.16–v3.24 已落地的导入同名建筑三选一 / 同文件 SHA-256 二次导入提示、智能模糊匹配、每页退出按钮 + 三击空白主菜单、执行中「请稍后」忙提示、查询/筛选双圈计数跳转、「四端功能对照单」子菜单等本轮回归确认无回退；全 App 风格、背景、色彩、文字一致，未删除任何已有功能与菜单。

**变更文件清单：**
| 文件 | 变更类型 |
|------|----------|
| app.js | 修改（新增 shapeForType / 重构 makeIcon(color, glyph) / 图例 buildLegend 双维度；CHANGES 加 v3.25；版本 v3.25） |
| index.html | 修改（新增 `#legend .dot` 样式支持形状符号居中） |
| AndroidManifest.xml | 版本号 25 / 3.25 |
| assets/version.json | 更新至 v3.25 |

### v3.24（2026-08-19）— 历史版本

| 项 | 内容 |
|----|------|
| 版本号 | v3.24 |
| versionCode / versionName | 24 / 3.24 |
| 构建日期 | 2026-08-19 |
| APK 大小 | ~8.2 MB |
| 交付文件 | `D:/Users/Claw/水利一张图V3.24_20260819_5090.apk`、`D:/Users/Claw/APK归档/水利一张图V3.24_20260819_5090.apk` |
| 源码快照 | `D:/Users/aowwei_app/webroot_shuili/`（app.js / index.html）、`versions/V1.9.0/` |
| 备份路径 | `D:/Users/aowwei_app/backup/v3.24_2026-08-19/` |

**功能（主界面查询圈选）：** 顶栏「🔍 查询」按钮与搜索框回车键 = 查询确认，与筛选同一套结果处理——0 个符合：弹窗提示并保留关键词；1 个符合：立即 flyTo 定位；多个符合：倒水滴高亮标记 + 绿色虚线圈（#2e8b57）刚好罩住全部 + 平滑飞入显示整圈。此前「查询」菜单只是聚焦搜索框、无圈选动作，本次补齐。

**功能（筛选圈改浅蓝）：** 筛选「确认」的多结果圈选改为浅蓝色虚线圈（#4aa3e0），与主界面查询的绿色圈区分开——查询=绿、筛选=浅蓝。

**功能（建筑物类型规范化）：** 主界面图例、筛选「建筑物类型」、编辑表单类型下拉统一改为规范类型清单（`CANONICAL_TYPES` + `ALWAYS_TYPES`，见 app.js）。移除被误当类型的字段：西田各庄段引渠 / 技术指标 / 挖填方统计表 / “进水闸 部分缺少经纬度” 等（仅影响分类显示，不删除任何建筑），新增「泵站」「溢洪道」。历史遗留的非规范类型在编辑该建筑时仍保留可选项，避免静默改写数据。

**变更文件清单：**
| 文件 | 变更类型 |
|------|----------|
| app.js | 修改（新增 CANONICAL_TYPES/canonicalTypes/doQueryConfirm/FILTER_CIRCLE_COLORS；图例/筛选/编辑类型改用规范清单；查询圈选=绿、筛选圈=浅蓝；版本 v3.24） |
| index.html | 无改动（动画 CSS 于 v3.22 已就绪） |
| AndroidManifest.xml | 版本号 24 / 3.24 |
| assets/version.json | 更新至 v3.24 |

### v3.23（2026-08-19）

| 项 | 内容 |
|----|------|
| 版本号 | v3.23 |
| versionCode / versionName | 23 / 3.23 |
| 构建日期 | 2026-08-19 |
| APK 大小 | ~8.2 MB |
| 交付文件 | `D:/Users/Claw/水利一张图V3.23_20260819_5090.apk`、`D:/Users/Claw/APK归档/水利一张图V3.23_20260819_5090.apk` |
| 源码快照 | `D:/Users/aowwei_app/webroot_shuili/`（app.js / index.html） |
| 备份路径 | `D:/Users/aowwei_app/backup/v3.23_2026-08-19/` |

**修复（导入提示真机不弹 · 根因）：** kmz/ovkmz/建筑物表格导入时的重复文件与同名建筑物提示此前误用原生 `confirm()`/`alert()`，离线 Android WebView 不显示且默认返回 false，导致静默取消或静默覆盖。已全部改为自定义 `ask()` 对话框，提示必现。

**修复（导入覆盖逻辑）：** 三处均弹窗——① 同名建筑物「覆盖更新 / 保留现有 / 取消导入」三选一；② 同一文件按 SHA-256 内容比对二次导入提示「已导入过，是否覆盖」；③ 解析不到任何建筑时给出原因提示。

**修复并调整（查询圈选）：** 筛选/查询「确定」后——0 个符合弹窗提示并保留面板；1 个符合立即跳转到该建筑物；多个符合倒水滴高亮 + 绿色虚线圈（accent #2e8b57）刚好罩住全部 + 平滑飞入显示整圈；无结果/导入异常提示统一改用 `ask()`。

**一致性：** 圈选虚线圈由蓝改绿，与 App 主题绿一致；所有弹窗走统一 `ask()` 与 CSS 变量，风格、背景、色彩、文字全 App 一致。

**变更文件清单：**
| 文件 | 变更类型 |
|------|----------|
| app.js | 修改（导入提示改用 ask()、查询绿圈、无结果 ask()、应用内版本变更加 v3.23） |
| index.html | 无改动（v3.22 已加动画 CSS） |
| AndroidManifest.xml | 版本号 23 / 3.23 |
| version.json | 版本号 v3.23 / 23 |

### v3.2（2026-08-13）— 历史版本（热修复）

| 项 | 内容 |
|----|------|
| 版本号 | v3.2 |
| 构建日期 | 2026-08-13 |
| APK 大小 | ~8.1 MB |
| 备份路径 | `backup/v3.2_2026-08-13/` |

**紧急修复（致命）：**
- **修复 v3.1 启动白屏/假死（app.js 语法错误）**：批量导入建筑物功能中错误提示字符串 `未找到"名称"列` 内含未转义的双引号，导致整个 `app.js` 解析失败、所有功能（地图/菜单/按钮）均不初始化。已转义为 `\"名称\"`，并用 `node --check` 验证通过。
- **新增全局错误兜底**：`window.onerror` 将运行期错误通过 toast 提示，避免再次出现"无提示白屏/假死"。

### v3.1（2026-08-13）— 已回退（存在致命语法错误，请勿安装）

| 项 | 内容 |
|----|------|
| 版本号 | v3.1 |
| 构建日期 | 2026-08-13 |
| APK 大小 | ~8.1 MB |
| 状态 | ⚠️ 含致命语法错误，启动即白屏假死 |

**新增功能：**
- 获取坐标：支持GPS定位、点选地图、搜索建筑物三种方式获取经纬度，可一键复制
- 批量导入照片：选择多张照片或ZIP压缩包，按文件名自动匹配建筑物
- 批量导入建筑物：支持CSV/Excel/ZIP格式，按列名解析，有经纬度自动定位，无经纬度按名称匹配
- 悬浮菜单按钮（FAB）：右下角始终可见，快速打开菜单
- 三击标题打开菜单：快捷操作，防止列表视图中找不到菜单

**优化改进：**
- 列表卡片"定位"改为"快速定位"，点击后立即跳转地图
- 建筑物弹窗增加"关闭"按钮，防止找不到主界面
- 测距提示条增加×关闭按钮
- Leaflet弹窗关闭按钮放大，更易点击
- 菜单新增"获取坐标"、"批量导入照片"、"批量导入建筑物"三项
- 帮助文档更新，涵盖所有新功能

**Bug 修复：**
- 列表视图中主菜单不可见问题（通过FAB+三击解决）
- 各模式互斥逻辑增强（坐标拾取/测距/添加三选一）

**变更文件清单：**
| 文件 | 变更类型 |
|------|----------|
| app.js | 修改（新增坐标获取、批量导入、FAB、三击菜单等） |
| index.html | 修改（新增CSS、测距关闭按钮） |
| MainActivity.java | 无变更 |
| AndroidManifest.xml | 无变更 |
| docs/ | 更新（变更文档+版本历史） |
| backup/v3.0_2026-08-13/ | 新增（v3.0 备份） |

---

### v3.0（2026-08-13）

| 项 | 内容 |
|----|------|
| 版本号 | v3.0 |
| 构建日期 | 2026-08-13 |
| APK 大小 | ~8.1 MB |
| 备份路径 | `backup/v2.0_2026-08-13/` (v2.0 快照) |

**新增功能：**
- 导航：建筑物弹窗「导航」按钮，唤起第三方导航 App
- 测距：菜单「测距」，多点折线测距，显示各段+总距离
- 周边搜索：菜单「周边搜索」，选中心建筑物+半径，列出范围内建筑物

**优化改进：**
- 标记图标改为蓝色水滴+白点（统一风格）
- 定位失败自动跳转系统设置（权限拒绝时）
- 菜单智能排序（高频功能靠前）
- 关于页增加建议使用环境+制作环境+当前运行环境
- 天地图 Token 规范化（区分浏览器端/服务器端）

**Bug 修复：**
- 测距模式与其他模式互斥
- UI 遮挡问题修复

**文档建设：**
- 新增软件需求说明书、概要设计、详细设计、变更文档、版本历史

**变更文件清单：**
| 文件 | 变更类型 |
|------|----------|
| app.js | 修改（核心逻辑） |
| index.html | 修改（CSS + HTML） |
| MainActivity.java | 无变更（v2.0 已完成） |
| AndroidManifest.xml | 无变更（v2.0 已完成） |
| docs/ | 新增（5 份文档） |
| backup/v2.0_2026-08-13/ | 新增（v2.0 备份） |

---

### v2.0（2026-08-13）

| 项 | 内容 |
|----|------|
| 版本号 | v2.0 |
| 构建日期 | 2026-08-13 |
| APK 大小 | ~8.1 MB |
| 备份路径 | `backup/v2.0_2026-08-13/` |

**新增功能：**
- 自定义底图切换器（替代 Leaflet 内置 layers control）
- 照片上传（WebChromeClient.onShowFileChooser）
- 导出文件保存到 Download 目录（DownloadListener + saveBlob）
- 关于页显示运行环境信息（OS/CPU/GPU/屏幕等）

**优化改进：**
- 列表视图「定位」支持线要素（取中点坐标）
- 切换视图后调用 map.invalidateSize()
- 顶栏 nowrap + 横向滚动
- 缩放控件移到右下角
- 侧滑面板 overflow:hidden

**Bug 修复：**
- 底图切换 radio 状态不同步
- 照片上传"选择文件"无反应
- 列表定位不跳转

**变更文件清单：**
| 文件 | 变更类型 |
|------|----------|
| app.js | 修改 |
| index.html | 修改 |
| MainActivity.java | 修改（新增 onShowFileChooser, DownloadListener, JSBridge） |
| AndroidManifest.xml | 修改（新增权限） |

---

### v1.0（2026-08-12）

| 项 | 内容 |
|----|------|
| 版本号 | v1.0 |
| 构建日期 | 2026-08-12 |
| APK 大小 | ~8.0 MB |

**初始功能：**
- 天地图矢量/影像底图
- 建筑物标记（SVG 水滴，按类型着色）
- 筛选（管理所/管理站/类型 + 关键字）
- 添加/修改/删除建筑物
- 多照片管理
- 列表视图 + 定位
- 导入/导出 ovkmz
- 统计
- 定位
- 帮助/关于
- localStorage 持久化
- 恢复初始数据

**初始文件：**
| 文件 | 说明 |
|------|------|
| index.html | HTML + CSS |
| app.js | 应用逻辑 |
| data.js | 建筑物数据（parse_ovkmz.py 生成） |
| leaflet/ | Leaflet 库 |
| jszip.min.js | JSZip 库 |
| images/ | 建筑物照片 |

---

## 2 备份策略

### 2.1 备份规则
- **每次版本发布前**，将当前版本所有文件备份到 `backup/v{X.X}_{YYYY-MM-DD}/`
- **备份内容**：app.js, index.html, MainActivity.java, AndroidManifest.xml
- **每次变更**记录在 `04_软件变更文档.md`
- **每次新增/变更需求**，同步更新所有文档

### 2.2 备份目录结构
```
backup/
├── v2.0_2026-08-13/
│   ├── app.js
│   ├── index.html
│   ├── MainActivity.java
│   └── AndroidManifest.xml
├── v3.0_2026-08-13/
│   ├── app.js
│   ├── index.html
│   ├── MainActivity.java
│   └── AndroidManifest.xml
├── v3.2_2026-08-13/
│   ├── app.js
│   └── index.html
└── ...
```

### 2.3 回退流程
1. 确定要回退到的版本号
2. 从 `backup/` 中找到对应版本的备份目录
3. 复制备份文件覆盖当前文件
4. 重新构建 APK
5. 更新本文档记录回退操作

---

## 3 版本发布检查清单

每次发布新版本时，需完成以下检查：

- [ ] app.js 中 `APP_VERSION` 和 `APP_BUILD_DATE` 已更新
- [ ] index.html 中 CSS 和 HTML 无语法错误
- [ ] MainActivity.java 编译通过
- [ ] APK 构建成功
- [ ] 在手机上测试核心功能（地图/筛选/增删改/导入导出/测距/导航/周边搜索）
- [ ] 备份当前版本到 `backup/`
- [ ] 更新本版本历史文档
- [ ] 更新变更文档
- [ ] APK 复制到 `D:/Users/Claw/`

---

## 4 文档同步规则

当需求变更时，以下文档需同步更新：

| 变更类型 | 需更新文档 |
|----------|-----------|
| 新增功能 | SRS (需求) + DDD (设计) + 变更文档 + 版本历史 |
| 修改功能 | DDD (设计) + 变更文档 + 版本历史 |
| 修复 Bug | 变更文档 + 版本历史 |
| 配置变更 | SRS (约束) + DDD (配置) + 变更文档 |
| UI 变更 | DDD (CSS/HTML) + 变更文档 |


---

## v3.7（2026-08-15）优化迭代

> 本次按用户自查清单对「水利工程基础信息一张图」做体验与能力优化，未删除任何原有功能/菜单。

### 一、菜单图标与筛选图标（Req 1）
- 所有菜单图标统一改为**圆形色块**（主色-浅底白字），醒目且风格一致。
- 筛选图标由 `🔎` 改为 `🎚️`，与查询 `🔍` 明显区分（原两者均为放大镜，易混淆）。

### 二、筛选实时计数 + 结果框选（Req 2）
- 筛选弹层顶部新增**实时计数条**「当前符合条件：N 个建筑」，每勾选/取消一个芯片即时刷新。
- 点击「确认」后：
  - 0 条 → 提示「没有符合条件的选项」；
  - 1 条 → 立即定位该建筑物（沿用原逻辑）；
  - 多条 → 每个命中建筑替换为**倒水滴高亮标记**（琥珀色、尖端朝上），并以**蓝色虚线圈**（`#1e6fbf`，虚线 `7,8`）刚好罩住全部命中建筑，自动 `fitBounds` 适配视图。

### 三、批量导入照片（Req 3）
- 入口图标保持醒目；新增**来源选择**：本地文件 / 百度网盘 / 夸克网盘。
- 压缩包格式支持 **ZIP** 与 **7z/RAR**（原生 `unzipImages` 解压；7z/RAR 若原生不支持会提示改用 ZIP）。
- 网盘导入调用原生 `netdiskPick`（若未连接网盘账号则给出明确提示，请先用本地导入）。

### 四、导出照片（Req 4）
- 导出对话框可选：**范围**（全部合并 / 按管理所单独）、**格式**（ZIP / 7z）、**目标**（本地下载 / 百度网盘 / 夸克网盘 / 分享微信 / 飞书 / QQ）。
- 压缩包按管理所命名与分文件夹；新增原生 `shareFile` + `ShareProvider`，**分享到微信/飞书/QQ 真实可用**（以 content:// 规避 Android 7+ 的 file:// 限制）。
- 7z 格式在当前原生打包未支持时回退为 ZIP 并提示。

### 五、智能匹配确认（Req 4 智能匹配）
- 多匹配确认弹窗**默认勾选最匹配项**，并标注**置信度**（高匹配 / 较可能 / 待确认）与**推断范围**（管理所 / 类型），用户可一键确认或改选。

### 六、风格统一（Req 3）
- 顶栏/弹层头渐变、进度条、悬浮按钮等内联色统一引用 `:root` CSS 变量；刷新《07_APP风格色彩字体搭配》。
- 查询/筛选保持顶栏显眼；每页保留「退出本页」；三击空白处仍可呼出主菜单。
