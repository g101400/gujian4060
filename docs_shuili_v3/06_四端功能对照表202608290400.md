# 四端功能对照表（v3.33 / v1.9 / v2.1，2026-08-27）

> 对应需求：③ 三端四平台同步到小程序、考虑平台差异、给出四端对照表。
> 适用范围：水利工程一张图（V3.33）/ 水利感知项目一张图（V1.9）/ 古建景点打卡（V2.1）。
> 小程序（京引一张图 / jingyin）为独立 WeChat 工程，仅做**版本号 + 风格基线**对齐，不含 ovobj / 管理所等原生能力（本轮已对齐至 v3.33 基线）。

## 一、四端形态与构建对照

| 端 | 形态 | 构建链路 | 本次版本 | 原生桥 | 离线 |
|----|------|----------|----------|--------|------|
| **安卓 Android** | 原生 APK | aapt2 + d8 + zipalign + apksigner（`build_apk.sh`） | 水利 V3.33 / 感知 V1.9 / 古建 V2.1 | `window.Android`（Java 原生） | ✅ 全功能离线 |
| **Windows** | WebView2 原生壳 | dotnet publish → WiX7 MSI + NSIS EXE（`gen_msi_v331.py`） | 同上 | `window.Android`（`window.Desktop` 别名） | ✅ 全功能离线 |
| **统信 UOS** | PyQt6 原生壳 | `make_deb.py` → `.deb`（架构 mips64el，龙芯 3A4000） | 同上 | `window.Android` | ✅ 全功能离线 |
| **iOS** | PWA + 静态 HTTPS | `build_ios_zip.py` → 可托管 ZIP | 同上 | 无（纯静态只读，原生能力降级） | ⚠️ 依赖托管 HTTPS |

## 二、功能实现与显示差异对照（不强行一致）

| 能力 | 安卓 | Win / UOS | iOS(PWA) | 说明 |
|------|------|-----------|-----------|------|
| 地图 /  CRUD / 筛选 / 查询 | ✅ 完整 | ✅ 完整（加载同源 webroot） | ✅ 查看 / 基础交互 | 网页本体四端同源，安卓为超集 |
| 照片落盘 / 网盘导入 / 分享微信飞书 QQ | ✅ 原生 `unzipImages`/`netdiskPick`/`shareFile` | ✅ 经本地 HTTP 服务解析相对路径 `photos/<管理所>/<建筑>/<序号>.jpg` | ⚠️ 仅静态展示，无上传/分享 | iOS 无原生桥，照片为只读展示 |
| ovobj / obj 导入导出（自兼容） | ✅ 原生选择器 + 桥 | ✅ 同源网页逻辑 | ✅ 同源网页逻辑（导出走下载） | 已验证与 `C:/Users/admin/Downloads/*.ovobj` 兼容 |
| 管理所筛选 / 导出（含全部 9 所） | ✅ | ✅ | ✅ | 权威清单 `CANONICAL_OFFICES` 兜底 |
| 退出当前页面按钮 | ✅ `ensureSheetFoot` 每页注入 | ✅ 同源 | ✅ 同源 | 防卡死，严禁删除 |
| 三击空白处呼出主菜单 | ✅ `closeTopLayer` | ✅ 同源 | ✅ 同源 | 快捷操作，与右下角悬浮按钮并存 |
| 风格 / 背景 / 色彩 / 字号 | ✅ `:root` CSS 变量统一 | ✅ 同源 | ✅ 同源 | 四端同一套主题变量 |

## 二·补、本轮修复项（v3.33 / v1.9 / v2.1）

| 问题 | 根因 | 修复 | 覆盖端 |
|------|------|------|--------|
| Bug1：导入建筑物报 `readFileText is not a function` | 网页直接调 `window.Android.readFileText`，但原生桥未暴露该方法（Win 桥曾为返回 null 的桩） | Android 桥补 `readFileText`（UTF-8）；网页端稳健读取 `readFileText→readFileBase64→清晰报错`；导出/导入 CSV 格式完全一致、APP 可导回 | 水利 / 感知（Android + 桌面/统信/浏览器） |
| Bug2：CSV 导出/导入格式兼容 | 导出与导入表头/引号/`\r\n`/BOM 未对齐 | 统一中文表头 + BOM + 引号包裹；已用 `C:/Users/admin/Downloads/水利工程基础信息一张图_建筑物_2026-08-27.csv`（557 行）实证往返无损 | 水利 / 感知 |
| Bug3：感知筛选「后勤」`script error` | `window.appFly(id)` 未先按 id 查回建筑对象即引用 `b` → `ReferenceError` | `appFly` 首行 `var b = BUILDINGS.find(...)`，未命中安全退出 | 感知（古建/水利同源代码本已正确） |
| Bug5：管理所选项全 9 所 | 部分筛选/导出/编辑入口管理所清单不全 | 权威清单 `CANONICAL_OFFICES` 兜底，含全部 9 所（地下水源所、温泉所、龙山所、史山所、埝头所、水库所、北台上所、西田各庄所、潮河所） | 水利（感知/古建各自完整清单） |
| Bug6：统信 deb 安装前「请检查deb文件是否损坏」 | `ar` 成员大小未按偶数补齐 → 部分解包器校验失败 | `make_deb.py` `ar_member` 大小含补齐字节；架构对照表固化（3A3000/3A4000→mips64el 等），架构不符明确提示而非损坏 | 三端 UOS deb |

## 三、版本八处同步清单（本次已对齐）

1. `assets/version.json`（真相源）
2. `assets/app.js`（`APP_VERSION` / `APP_BUILD_DATE`）
3. `AndroidManifest.xml`（`versionCode` / `versionName`）
4. 6 个 webroot（`version.json` / `app.js` / `ovobj_bridge.js` 由 `bump_version.py` 同步）
5. `build_msi.wxs`（`<Product Version>`）
6. `make_deb.py`（`PROJ[].ver`，**dict 形式，需手动同步**）
7. `build_ios_zip.py`（`PROJECTS[].file_ver/new_ver/readme_ver`，**dict 形式，需手动同步**）
8. 文档（`04_软件变更文档` / `05_版本历史与备份`）

## 四、已知差异与边界

- **iOS 无原生桥**：照片上传、网盘、系统分享等依赖 `window.Android` 的能力在 iOS 上降级为只读/不支持；其余查看类功能完整。
- **UOS 架构**：龙芯 3A4000 → `mips64el`；3A5000+ → `loongarch64`；FT2000/鲲鹏 → `arm64`；其余 → `amd64`（均经 `make_deb.py` `ARCH_MAP` 与 `ARCH=` 覆盖）。
- **小程序**：独立工程，仅版本/风格基线对齐，不在此四端构建链路内。
