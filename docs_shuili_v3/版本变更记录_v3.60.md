# 版本变更记录 v3.60（2026-09-07，内部通道）

> 配套：感知 v1.36；古建 v3.7.5（古建不分内外通道）。
> 性质：**v3.59 的内部孪生版**（同源功能 + 真实业务数据），并随版落地 GitHub 升级链路与五仓库改名。

## 一、本次需求

1. 内部通道补发与 v3.59 同源的功能版（公开 v3.59 / 内部 v3.60 奇偶并行）。
2. 内置真实业务数据（内部渠道；公开渠道仍为演示数据）。
3. 建立 GitHub 升级链路：五仓库 Release 按版本发布 + App 内「通过 GitHub 升级」。

## 二、核心改动

### E1 与 v3.59 同源功能（内部通道）

- 知识库管理导出 md/txt/html 自定义文件名（默认 `知识库_YYYYMMDD.md/.txt/.html`）+ 自选保存文件夹（Android 原生目录选择落 Download/指定目录；Win/UOS/iOS 回退系统下载目录并提示），共享模块 kbExport 三应用同步。
- 其余导出（建筑物表格 / 照片 / ovkmz / ovobj / obj / 升级备份）自定义文件夹与文件名逐一核查保持；各子菜单防「运行错误:script error」冒烟回归。
- 信息与帮助（功能介绍 / 版本变更 / 四端功能对照单）更新至 v3.60。

### E2 ovkmz 导入/导出兼容修复（随版）

- 导入兼容奥维原装 ovkmz：BOM / TAB 分隔符差异修复，照片附件解析正常。
- 导出格式与奥维原装保持一致（奥维可直接打开回放）。

### E3 GitHub 升级链路落地（随版）

- 五仓库统一改名（方案C）：古建 `gujian-travel5090`、水利公开 `shuili-yitu-5090pub`、感知公开 `ganzhi-yitu-5090pub`、水利内部 `shuili-yitu5090`、感知内部 `ganzhi-yitu5090`。
- 各仓按版本建 Release/tag（四平台安装包资产）；App 设置菜单新增「通过 GitHub 升级」，按渠道查询对应仓库 Release 并下载。

### E4 iOS PWA 壳 per-product 修复（随版）

- `build_ios_zip.py` PWA 壳基线不再固定取水利 zip，感知/古建 iOS 包 manifest 名称/图标正确。

## 三、四平台同步与验证

- canonical = `android-build/shuili-v329/assets`；`sync_all_mirrors.py` 同步 Win/UOS/iOS 镜像。
- 产物归档：`APK归档/`（APK + deb 四架构 + iOS PWA）。
- 门禁：`node --check` 全部 webroot JS 通过；`verify_myml_ar.py` deb ar 头校验通过。
- 回退：`sync_all_mirrors.py` 自动备份（`*_bak_v3.59_*`）；canonical git 可回退。
