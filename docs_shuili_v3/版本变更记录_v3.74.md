# 版本变更记录 v3.74（水利工程一张图 · 内部版 · 三端同批）

- 版本：**v3.74**（内部偶数通道）｜构建日期：2026-09-13
- 同批版本：感知 **v1.50**／古建 **v3.7.12**
- 归档：`APK归档/四端安装包_20260913_v374_int`
- 回退备份：`backup_2026-09-13_before_v374`
- 提版脚本：`bump_version_v374_int.py`（模 `bump_version_v372_int.py`）
- 公开派生：水利 **v3.75**／感知 **v1.51**（奇数通道，GitHub Pages / Release）

> 本轮为**三端同批重出**，主要承载两项预置改进：**口令硬化（O1）** + **局/管理处数据化（O5 选 B）**，
> 三者代码同源改动，版本号与发行节奏一致；古建无局/管理处字段，仅跟随口令硬化重出。

## 一、本批实际改动

### 1. 口令硬化（O1）
- **去除源码明文「开发者分机号」**：改 AES-GCM(PBKDF2) 凭证校验 + 运行时输入 + 构建期注入密钥
  （本地 `.sec_pass` 不随包，密钥不写进共享源码）。
- `secure_boot.js` 门闸**默认关闭**，仅借用校验能力；公开版（奇数）免密放行，行为不变。
- 全三端源码明文 `3305` 计数 = **0**（已 grep 核验，无回退）。
- `DEV_EXT` 改运行时注入；构建脚本版本串全改（`make_deb` / `gen_win_msi_wix7` / `build_win_exe_nsis` / `make_secure_internal_pwa`）。

### 2. 局/管理处数据化（O5，选 B 数据化）
- 编辑表单 `openEdit` 新增「局」「管理处」下拉（`levelOptions("ju")` / `guanchuOptions()`，默认回退 `getOrgDefaults`）。
- `appSave` 写 `b.ju` / `b.guanchu`。
- ovkmz 导出 `ExtendedData` 补写「局」「管理处」（`dataEl` 自动跳过空值）。
- CSV 导出 `baseCols` 加「局」列（`labelMap` 补 `ju:"局"`；导出行走通用 `b[c]`）。
- CSV / ovkmz 导入与 AI `_orgContextForQuery` 经 `FIELD_ALIAS` 已覆盖局/管理处，无需改。
- 范围：水利 `shuili-v329` + 感知 `perc-v13`（古建无局/管理处，跳过）。
- CRLF 保留：app.js 为 CRLF，改动用 Python 块级精准替换，未破 7 个共享模块 md5 门禁。

## 二、感知 / 古建（本轮改动）

| 产品 | 版本 | 改动 |
|---|---|---|
| 水利感知项目一张图 | v1.48 → **v1.50** | 口令硬化（同水利）＋局/管理处数据化（同水利）；水工建筑物底图种子 `water_data.js`(557 条) 沿用 v1.48 |
| 古建景点打卡 | v3.7.11 → **v3.7.12** | 三端同批重出：口令硬化（去明文分机号，改 AES-GCM 凭证校验，门闸保持关闭）；古建无局/管理处字段，不涉及数据化 |

## 三、门禁与验证（全绿）

- `sync_all_mirrors.py --check`：11 镜像 + 跨产品 7 模块（kb_rag/ai_module/ovobj_bridge/kb_core/kb_vector/photo_gesture/ctx_menu）md5 一致（EXIT=0）
- `sync_weapp_version.py --check`：小程序(jingyin)基线 **3.74**（2026-09-13）
- `verify_pass.py <归档> int`：21/21（版本号 + 数据标记）
- `verify_pkgs_smartquery.py`：pub+int **52/52**（renderQueryFurther / furtherQuery + 数据条数 557/1125/1032）
- `node --check`：canonical + 各镜像 webroot `app.js` 全过
- 口令硬化回归：三 canonical `app.js` 明文 `3305` = 0

## 四、四端产物

见 `APK归档/四端安装包_20260913_v374_int/`：Android APK ×3、UOS deb ×12（4 架构 ×3 产品）、
Windows MSI ×3 + NSIS EXE ×3、iOS 可托管 PWA zip ×3（水利/感知另有内部加密版 + 公开派生 3.75/1.51）。

## 五、发布与分发

- **GitHub Release**（内部三仓 `v3.74`/`v1.50`/`v3.7.12`）：资产下载正常；内部私有仓无 Pages（422 边界），仅走 Release 资产。
- **GitHub Pages 公开版**：`shuili-yitu-5090pub@v3.75`、`ganzhi-yitu-5090pub@v1.51` 已开启可访问（curl 回验 version.json 正确）。
- **百度网盘**：`yitu-release/四端全量_v3.74_1.50_3.7.12_20260913/` 四平台 26 文件全成功。
- **交付须知**：用户须**重新安装新 APK** 才能看到口令硬化 + 局/管理处字段（旧包仍是 3.72/1.48/3.7.11）。

## 六、发布铁律沉淀（本次实操）

- bdpan `upload` 远端路径必须「目录/文件名」两级（单级被当根文件名，生成 `(1)` 副本）。
- gh 发布前必 `gh auth status` 探活 + `gh auth setup-git` 打通 git 推送；本会话后台 shell 的 `*.deb` glob 不可靠，改前台显式绝对路径调 `gh`。
- fine-grained PAT 存 `.gujian_token`（三仓根，已 `.gitignore` 忽略），不入库。

## 七、下一步（未动）

- 公开（奇数）通道下一轮：水利 v3.77／感知 v1.53（待定）。
- 待用户决策：OpenRouter Key 是否轮换（O2，用户 09-13 决定保持现状）。
