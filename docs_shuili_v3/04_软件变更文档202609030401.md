# 软件变更文档

## 水利工程基础信息一张图 APP
## v3.38 / v1.14 / v2.6（2026-08-29）

- 水利 v3.38、感知 v1.14、古建 v2.6 同期发版，版本号八处同步。
- 筛选默认值持久化：首次安装注入非「全部」种子（水利/感知→水库所、古建→北京市），每次筛选操作自动写回 localStorage，启动即套用，避免全量渲染卡顿。
- 新增知识库（Markdown 正文 + JSON 元数据，md+json 混合，导入/导出 zip）：设置菜单「知识库管理」含读取外部文件入库（pdf/xls/doc/csv/txt/md）、导出知识库备份、导入备份。
- Hermes 自我学习循环：智能查询/更新/纠错后异步抽提「关键参数+操作偏好」沉淀 hermes 条目，智能更新写入记 operation 条目，后续查询注入上下文自我提升。
- 筛选结果可视化（浅蓝虚线圈选 + fitBounds 居中）回归保持；UOS deb ar 头铁律 + 四平台同步回归保持。
- 三日问题回归：verify_myml_ar.py 门禁对 shuili 3.37/perc 1.13/gujian 2.5 全过。

## v3.36 / v1.12 / v2.4（2026-08-28）

- 水利 v3.36、感知 v1.12、古建 v2.4 同期发版，版本号八处同步。
- 修复智能AI 联网查询「String contains non ISO-8859-1 code point」：HTTP 头中文 appName 经 toLatin1 转 ASCII。
- 菜单顺序智能调整：关于/帮助移至末尾，删除/恢复初始数据归「设置」子菜单，智能AI 按同名分组自动合并。
- 新增显示界面右键上下文菜单（地图/标注/列表）：获取坐标、周边搜索、导航、智能补全、智能描述此地；调用大模型自动填充简介/特征。
- 更换 OpenRouter 密钥（openclaw626），三个免费模型（MiniMax M2.7 / GLM 5.2 / Nemotron 3 Nano Omni）failover 实测；古建开放联网智能查询/更新/纠错，水利/感知保持「本地辅助」不联网编造。
- 三日问题（CSV 导入 readFileText、管理所全 9 所、UOS deb ar 大小补齐）回归保持。


## v3.35 / v1.11 / v2.3（2026-08-28）

- 水利 v3.35、感知 v1.11、古建 v2.3 同期发版，版本号八处同步。
- 智能AI 增强：内置 OpenRouter 三个免费模型预设（MiniMax M2.7 / GLM 5.2 / Nemotron 3 Nano Omni，均 :free 兼容 OpenAI）；默认自动调用策略改为 failover（免费模型限流自动切换）；本地 ai_seed.js 注入个人 Key（不写进共享源码）；chatOne 增加 OpenRouter 推荐头与 429/401 提示、reasoning 模型兜底。
- 古建开放联网智能查询/智能更新/智能纠错；水利/感知为内部资料，保持「本地辅助」模式不联网编造。
- 三日问题（CSV 导入 readFileText、感知筛选后勤 script error、管理所全 9 所、UOS deb ar 大小补齐）回归保持。


## v3.34 / v1.10 / v2.2（2026-08-28）

- 水利 v3.34、感知 v1.10、古建 v2.2 同期发版，版本号八处同步。
- 新增「智能AI设置 + 智能AI」双菜单：自定义大模型接口（OpenRouter 默认、OpenAI 兼容、可本地模型）、多模型默认选择与自动调用策略（仅默认/失败切换/轮询）、连接测试、配置导入导出。
- 古建开放联网智能查询/智能更新(补丁确认写入)/智能纠错(标注差异)；水利/感知为内部资料切换「本地辅助」模式，不联网编造。
- 三日问题（CSV 导入 readFileText、感知筛选后勤 script error、管理所全 9 所、UOS deb ar 大小补齐）回归保持。


## v3.33 / v1.9 / v2.1（2026-08-27）

- 水利 v3.33、感知 v1.9、古建 v2.1 同期发版，版本号八处同步。
- Bug1 CSV 导入 readFileText 报错修复（原生桥补齐 + 网页稳健读取，导出导入自兼容）。
- Bug3 感知筛选「后勤」script error 修复（appFly 按 id 查回建筑）。
- Bug5 管理所全 9 所；Bug6 统信 deb ar 大小补齐根除「文件损坏」提示。
- 三产品四平台 webroot 全量同步（含本轮回退修复），iOS PWA 同步。



| 项目 | 内容 |
|------|------|
| 文档编号 | CHG-SHUILI-v3.2 |
| 版本 | v3.2 |
| 编写日期 | 2026-08-13 |
| 作者 | 炎冰 |
| 状态 | 发布（热修复） |

---

## 1 变更概览

本次为 **v3.1 紧急热修复（v3.1 → v3.2）**，修复导致启动白屏/假死的致命语法错误，并增加运行期错误兜底。

### 变更统计
| 类型 | 数量 |
|------|------|
| 致命 Bug 修复 | 1 |
| 优化改进（防御） | 1 |
| 文档更新 | 2 |

---

## 2 变更明细

### CHG-HOT-001: 修复 app.js 致命语法错误导致启动假死
| 项 | 内容 |
|----|------|
| 类型 | 致命 Bug 修复 |
| 优先级 | 最高 |
| 状态 | 已完成 |
| 影响文件 | app.js |
| 回归版本 | v3.1（批量导入建筑物功能引入） |

**问题现象：**
- v3.1 安装后打开即"白屏/假死"，无任何交互响应（地图、菜单、按钮全部失效）。

**根因：**
- `doBatchImportBuildings()` 中错误提示字符串 `未找到"名称"列（name/名称）` 内含未转义的双引号，导致 `app.js` 在解析阶段抛出 `SyntaxError: Unexpected identifier`，整个 IIFE 不执行，所有初始化代码（地图、菜单、事件绑定）均未运行。
- 用 `node --check app.js` 已定位并验证。

**变更后：**
- 字符串转义为 `未找到\"名称\"列（name/名称）`，`node --check` 验证通过。
- 版本号升级为 v3.2。

### CHG-HOT-002: 新增全局运行期错误兜底
| 项 | 内容 |
|----|------|
| 类型 | 优化（防御性） |
| 优先级 | 中 |
| 状态 | 已完成 |
| 影响文件 | app.js |

**变更内容：**
- IIFE 顶部新增 `window.onerror`，将运行期异常通过 toast 显示，避免再次出现"无提示假死"，便于现场快速定位问题。

---

### CHG-001: 天地图 Token 规范化
| 项 | 内容 |
|----|------|
| 类型 | 配置变更 |
| 优先级 | 高 |
| 状态 | 已完成 |
| 影响文件 | app.js |

**变更前：**
- 仅有一个 `TOKEN` 变量，无服务器端/浏览器端区分说明

**变更后：**
- 新增 `TOKEN_BROWSER`（浏览器端：`61691764ff68bf341c4c9c4770b24b5f`）
- 新增 `TOKEN_SERVER`（服务器端：`d258e3622cb1755b59a59e4b7ac6d96e`）
- `TOKEN = TOKEN_BROWSER`，并添加注释说明使用场景

### CHG-001: 天地图 Token 规范化
| 项 | 内容 |
|----|------|
| 类型 | 配置变更 |
| 优先级 | 高 |
| 状态 | 已完成 |
| 影响文件 | app.js |

**变更前：**
- 仅有一个 `TOKEN` 变量，无服务器端/浏览器端区分说明

**变更后：**
- 新增 `TOKEN_BROWSER`（浏览器端：`61691764ff68bf341c4c9c4770b24b5f`）
- 新增 `TOKEN_SERVER`（服务器端：`d258e3622cb1755b59a59e4b7ac6d96e`）
- `TOKEN = TOKEN_BROWSER`，并添加注释说明使用场景
- localStorage 键名从 `shuili_map_v1` 改为 `shuili_map_v2`

**回退说明：** 如需回退，将 `TOKEN` 改回直接赋值 `61691764ff68bf341c4c9c4770b24b5f`，`LS_KEY` 改回 `shuili_map_v1`

---

### CHG-002: 建筑物标记样式变更
| 项 | 内容 |
|----|------|
| 类型 | 优化改进 |
| 优先级 | 高 |
| 状态 | 已完成 |
| 影响文件 | app.js |

**变更前：**
- 标记颜色按建筑物类型 hash 着色，尺寸 30x42

**变更后：**
- 所有标记统一为蓝色水滴（`#1e6fbf`），中间小白点
- 尺寸调整为 32x44，描边 2.5px

**回退说明：** 恢复 `makeIcon()` 中 `fill` 为 `color` 参数

---

### CHG-003: 定位权限自动跳转设置
| 项 | 内容 |
|----|------|
| 类型 | 优化改进 |
| 优先级 | 高 |
| 状态 | 已完成 |
| 影响文件 | app.js |

**变更前：**
- 定位失败统一提示"定位失败/未授权"

**变更后：**
- 区分错误类型（PERMISSION_DENIED / TIMEOUT / 其他）
- 权限拒绝时 1.2 秒后自动调用 `Android.openLocationSettings()` 跳转设置
- 定位成功显示坐标信息
- 超时时间从 8 秒增至 10 秒，`maximumAge: 0`

**回退说明：** 恢复原 `locateMe()` 函数

---

### CHG-004: 新增导航功能
| 项 | 内容 |
|----|------|
| 类型 | 新增功能 |
| 优先级 | 高 |
| 状态 | 已完成 |
| 影响文件 | app.js, index.html |

**变更内容：**
- 建筑物弹窗新增「导航」按钮
- `appNav(id)` 函数：构造 `geo:lat,lon?q=lat,lon(name)` URI
- 通过 `Android.openExternal()` 唤起系统导航 App 选择器
- 线要素取中点坐标导航
- 新增 `.pop-nav` CSS 样式（绿色按钮）

**回退说明：** 删除 `appNav` 函数和弹窗中的导航按钮

---

### CHG-005: 新增测距功能
| 项 | 内容 |
|----|------|
| 类型 | 新增功能 |
| 优先级 | 高 |
| 状态 | 已完成 |
| 影响文件 | app.js, index.html |

**变更内容：**
- 菜单新增「测距」项
- `toggleMeasure()` 进入/退出测距模式
- 点击地图记录点，绘制折线 + 各段距离标签 + 总距离标签
- 双击或按钮结束测距
- 测距模式禁用双击缩放
- 测距模式菜单按钮显示「结束测距」
- 新增 `.hint-measure` CSS 和 HTML 元素
- 新增 `.measure-tip` CSS

**回退说明：** 删除测距相关变量、函数、菜单项、CSS、HTML

---

### CHG-006: 新增周边搜索功能
| 项 | 内容 |
|----|------|
| 类型 | 新增功能 |
| 优先级 | 高 |
| 状态 | 已完成 |
| 影响文件 | app.js |

**变更内容：**
- 菜单新增「周边搜索」项
- `nearbySearch()` 显示选择中心建筑物和半径的 UI
- `appDoNearby()` 使用 `latLng.distanceTo()` 计算距离
- 地图上画范围圆（`L.circle`）
- 结果列表按距离排序，可点定位

**回退说明：** 删除 `nearbySearch`、`appDoNearby` 函数和菜单项

---

### CHG-007: 菜单智能排序
| 项 | 内容 |
|----|------|
| 类型 | 优化改进 |
| 优先级 | 中 |
| 状态 | 已完成 |
| 影响文件 | app.js |

**变更前顺序：**
筛选 → 添加 → 导入 → 导出 → 列表 → 统计 → 定位 → 恢复 → 帮助 → 关于

**变更后顺序：**
定位 → 列表 → 测距 → 周边搜索 → 筛选 → 添加 → 导入 → 导出 → 统计 → 恢复 → 帮助 → 关于

**原则：** 高频功能（定位/列表/测距/搜索）靠前，管理功能（导入/导出/恢复）靠后

**回退说明：** 恢复 `buildMenu()` 中 items 数组顺序

---

### CHG-008: 关于页增加建议使用环境和制作环境
| 项 | 内容 |
|----|------|
| 类型 | 优化改进 |
| 优先级 | 中 |
| 状态 | 已完成 |
| 影响文件 | app.js |

**变更前：**
- 仅显示"制作环境"（自动检测的运行环境）

**变更后：**
- 新增"建议使用环境"（Android 8.0+, 5寸+, 2GB+ 等）
- 新增"制作环境"（开发机固定信息：Win10, i7-9700K, GTX 1660 Ti, 32GB 等）
- 保留"当前运行环境"（自动检测）
- 新增版本号和构建日期显示

**回退说明：** 恢复原 `showAbout()` 函数

---

### CHG-009: UI 遮挡修复
| 项 | 内容 |
|----|------|
| 类型 | Bug 修复 |
| 优先级 | 高 |
| 状态 | 已完成 |
| 影响文件 | app.js, index.html |

**修复内容：**
- 测距提示条与底图切换器不重叠（测距提示居中，底图切换器靠右）
- 列表视图切换时隐藏测距提示
- 测距模式点击事件优先于添加模式
- `toggleAdd()` 进入时自动退出测距模式
- 菜单按钮正确处理三种状态（添加/测距/正常）

**回退说明：** 恢复相关事件处理代码

---

### CHG-010: 导航按钮 CSS
| 项 | 内容 |
|----|------|
| 类型 | Bug 修复 |
| 优先级 | 低 |
| 状态 | 已完成 |
| 影响文件 | index.html |

**变更内容：**
- 新增 `.pop-nav { background:#2b8a5d; color:#fff; }` 样式

---

### CHG-011: 版本号和日期管理
| 项 | 内容 |
|----|------|
| 类型 | 配置变更 |
| 优先级 | 低 |
| 状态 | 已完成 |
| 影响文件 | app.js |

**变更内容：**
- 新增 `APP_VERSION = "v3.0"` 和 `APP_BUILD_DATE = "2026-08-13"`
- `detectEnv()` 使用变量而非硬编码

---

### CHG-012: 帮助文档更新
| 项 | 内容 |
|----|------|
| 类型 | 文档建设 |
| 优先级 | 低 |
| 状态 | 已完成 |
| 影响文件 | app.js |

**变更内容：**
- 新增导航、测距、周边搜索、定位的使用说明
- 导出说明增加保存路径

---

### CHG-013: 项目文档建设
| 项 | 内容 |
|----|------|
| 类型 | 文档建设 |
| 优先级 | 高 |
| 状态 | 已完成 |
| 影响文件 | docs/ 目录 |

**新增文档：**
1. `01_软件需求说明书.md` — SRS
2. `02_软件概要设计.md` — HLD
3. `03_软件详细设计.md` — DDD
4. `04_软件变更文档.md` — 本文件
5. `05_版本历史与备份.md` — 版本记录

---

### CHG-014: 备份机制
| 项 | 内容 |
|----|------|
| 类型 | 文档建设 |
| 优先级 | 高 |
| 状态 | 已完成 |
| 影响文件 | backup/ 目录 |

**变更内容：**
- v2.0 版本文件备份至 `backup/v2.0_2026-08-13/`
- 包含 app.js, index.html, MainActivity.java, AndroidManifest.xml

---

### CHG-015: AndroidManifest 权限完善
| 项 | 内容 |
|----|------|
| 类型 | 配置变更 |
| 优先级 | 中 |
| 状态 | 已完成（v2.0 已完成，v3.0 继承） |
| 影响文件 | AndroidManifest.xml |

**权限清单：**
- INTERNET（在线底图）
- ACCESS_FINE_LOCATION / ACCESS_COARSE_LOCATION（定位）
- WRITE_EXTERNAL_STORAGE / READ_EXTERNAL_STORAGE（maxSdk 28，导出/导入）
- requestLegacyExternalStorage: true（Android 10 兼容）

---

## 3 回退方案

### 3.1 整体回退到 v2.0
1. 从 `backup/v2.0_2026-08-13/` 恢复 app.js, index.html
2. 从备份恢复 MainActivity.java, AndroidManifest.xml
3. 重新构建 APK

### 3.2 单项回退
每项变更的回退说明见各 CHG 条目的"回退说明"字段。

### 3.3 数据兼容
- v3.0 使用 `LS_KEY = "shuili_map_v2"`，与 v1.0 的 `shuili_map_v1` 不冲突
- 回退到 v1.0 后旧数据仍在 `shuili_map_v1` 中
- v3.0 首次启动会从嵌入数据初始化 `shuili_map_v2`


---

## v3.7（2026-08-15）优化迭代

> 本次按用户自查清单对「水利工程基础信息一张图」做体验与能力优化，未删除任何原有功能/菜单。

### 一、菜单图标与筛选图标（Req 1）
- 所有菜单图标统一改为**圆形色块**（主色-浅底白字），醒目且风格一致。
- 筛选图标由 `🔎` 改为 `🎚️`，与查询 `🔍` 明显区分（原两者均为放大镜，易混淆）。

### 二、筛选实时计数 + 结果框选（Req 2）
- 筛选弹层顶部新增**实时计数条**「当前符合条件：N 个建筑」，每勾选/取消一个芯片即时刷新。
- 点击「确认」后：
  - 0 条 → 提示「没有符合条件的选项」；
  - 1 条 → 立即定位该建筑物（沿用原逻辑）；
  - 多条 → 每个命中建筑替换为**倒水滴高亮标记**（琥珀色、尖端朝上），并以**蓝色虚线圈**（`#1e6fbf`，虚线 `7,8`）刚好罩住全部命中建筑，自动 `fitBounds` 适配视图。

### 三、批量导入照片（Req 3）
- 入口图标保持醒目；新增**来源选择**：本地文件 / 百度网盘 / 夸克网盘。
- 压缩包格式支持 **ZIP** 与 **7z/RAR**（原生 `unzipImages` 解压；7z/RAR 若原生不支持会提示改用 ZIP）。
- 网盘导入调用原生 `netdiskPick`（若未连接网盘账号则给出明确提示，请先用本地导入）。

### 四、导出照片（Req 4）
- 导出对话框可选：**范围**（全部合并 / 按管理所单独）、**格式**（ZIP / 7z）、**目标**（本地下载 / 百度网盘 / 夸克网盘 / 分享微信 / 飞书 / QQ）。
- 压缩包按管理所命名与分文件夹；新增原生 `shareFile` + `ShareProvider`，**分享到微信/飞书/QQ 真实可用**（以 content:// 规避 Android 7+ 的 file:// 限制）。
- 7z 格式在当前原生打包未支持时回退为 ZIP 并提示。

### 五、智能匹配确认（Req 4 智能匹配）
- 多匹配确认弹窗**默认勾选最匹配项**，并标注**置信度**（高匹配 / 较可能 / 待确认）与**推断范围**（管理所 / 类型），用户可一键确认或改选。

### 六、风格统一（Req 3）
- 顶栏/弹层头渐变、进度条、悬浮按钮等内联色统一引用 `:root` CSS 变量；刷新《07_APP风格色彩字体搭配》。
- 查询/筛选保持顶栏显眼；每页保留「退出本页」；三击空白处仍可呼出主菜单。

---

## v3.23（2026-08-19）真机问题修复

> 本轮按真机测试反馈修复「导入提示不弹」「查询圈选」两类问题，未删除任何原有功能/菜单。

### 一、导入提示真机不弹（根因修复）
- **根因**：kmz/ovkmz 二次导入、以及解析异常等分支误用原生 `confirm()`/`alert()`。离线 Android WebView 中这些原生对话框**不显示且默认返回 false**，导致：重复导入被静默取消、同名建筑物被静默覆盖、异常无提示。
- **修复**：全部改用应用内自定义 `ask()` 对话框（`#askBox`，复用同一套 CSS 变量与主题绿），保证提示必现。
- 三处导入提示现已齐全：
  1. 同名建筑物 → 「覆盖更新 / 保留现有 / 取消导入」三选一；
  2. 同一文件（按内容 SHA-256 比对）二次导入 → 「已导入过，是否覆盖」；
  3. 解析不到任何建筑（Placemark）→ 给出原因提示。

### 二、查询/筛选圈选修复与调整
- 0 个符合 → 弹窗提示「没有符合条件的建筑」并保留筛选面板（便于就地调整）。
- 1 个符合 → 立即 `flyTo` 跳转到该建筑物。
- 多个符合 → 倒水滴高亮标记 + **绿色虚线圈**（主题绿 `#2e8b57`，虚线 `7,8`）刚好罩住全部命中建筑，平滑 `fitBounds` 飞入显示整圈（圈线由蓝改绿，与 App 主题一致）。
- 无结果、导入异常等提示统一改用 `ask()`。

### 三、一致性
- 圈选虚线圈由蓝改绿；所有弹窗走统一 `ask()` 与 CSS 变量，风格、背景、色彩、文字全 App 一致。
- 应用内「版本变更」新增 v3.23 记录。

---

## v3.24（2026-08-19）真机反馈修复（查询圈选 + 类型规范化）

> 按真机测试反馈修复「主界面查询菜单不圈选」「建筑物类型出现乱字段」两类问题，未删除任何原有功能/菜单。

### 一、主界面「查询」菜单圈选补齐
- **原状**：主界面「查询」菜单仅聚焦顶部搜索框，输入关键词后无「确认」动作，不圈选、不定位、不提示。
- **修复**：顶栏「🔍 查询」按钮与搜索框回车键 = 查询确认（`window.doQueryConfirm`），与筛选同一套结果处理：
  1. 0 个符合 → 弹窗提示「没有符合条件的建筑」并保留关键词与搜索框；
  2. 1 个符合 → 立即 `flyTo` 定位到该建筑物；
  3. 多个符合 → 倒水滴高亮标记 + **绿色虚线圈**（主题绿 `#2e8b57`）刚好罩住全部 + 平滑 `fitBounds` 显示整圈 + 居中「命中 N 个」徽标。
- 实时匹配区、防抖渲染等既有机制不变，查询与筛选共用 `passFilter` 条件集。

### 二、筛选圈改浅蓝、查询圈绿色（双圈区分）
- `showFilterResult(matched, color)` 新增颜色参数，`FILTER_CIRCLE_COLORS` 定义两套配色：
  - `green`（查询）：`#2e8b57` 主题绿；
  - `blue`（筛选）：`#4aa3e0` 浅蓝。
- 筛选「确认」多结果 → 浅蓝色虚线圈；查询「确认」多结果 → 绿色虚线圈。动画（由内向外展开 + 行进蚂蚁描边）两色通用。

### 三、建筑物类型规范化
- **乱因**：类型列表此前由数据 `btype` 去重动态生成，导致 `西田各庄段引渠`、`技术指标`、`挖填方统计表`、`进水闸 部分缺少经纬度` 等非类型字段混入「建筑物类型」。
- **修复**：新增规范清单 `CANONICAL_TYPES`（进水闸/节制闸/分水闸/泄洪闸/扬水闸/补水闸/**泵站**/**溢洪道**/倒虹吸/跌水/过渠涵洞/穿渠项目/交通桥/山洪桥/工作桥/清污机/雨水口/道路/变压器）与 `ALWAYS_TYPES`（泵站、溢洪道始终展示）。
- 主界面图例、筛选「建筑物类型」、编辑表单类型下拉统一使用 `canonicalTypes()`；「泵站」「溢洪道」即使当前数据暂无该类建筑也展示。
- **数据安全**：仅清理“类型分类显示”，不删除任何建筑/照片/属性；编辑历史遗留非规范类型建筑时，该类型仍作为可选项保留，避免被静默改写。

### 四、一致性
- 查询/筛选双圈颜色区分且都在主题体系内；所有弹窗仍走统一 `ask()` 与 CSS 变量；退出按钮、三击空白、菜单/功能、风格色彩字体均保持一致。
- 应用内「版本变更」新增 v3.24 记录。

---

## v3.25 变更明细（2026-08-20）

### 一、建筑物双维度区分（颜色 + 形状）
- **需求**：不同建筑物类型应可用「不同颜色或形状」区分，左下角图例需同时展示「名称 + 颜色 + 形状示例」。
- **实现**：
  - 新增 `shapeForType(t)`：取与 `colorForType` 同源的哈希，将 19 类规范类型循环映射到 8 种形状符号 `● ■ ▲ ◆ ★ ⬢ ✚ ✖`，保证类型→(色,形)映射稳定。
  - `makeIcon(color, glyph)` 重构：标记倒水滴填充色 = 类型专属色（`colorForType`），中心白点内嵌该类型专属形状符号（`shapeForType`），颜色仍按类型名哈希唯一、形状按同类循环，二者叠加保证两两易区分。
  - 左下角图例（`buildLegend`）升级：每个类型项由「纯色块」改为「**彩色圆点内嵌形状符号 + 类型名称**」，一眼可辨类型归属。
- **影响文件**：app.js（`colorForType`/`shapeForType`/`makeIcon`/`buildLegend`）、index.html（图例 `.dot` 样式支持符号居中）。

### 二、全量需求回归确认（无回退）
- 经代码逐条核对，此前 v3.16–v3.24 已落地且本轮回归确认无回退的需求：
  1. 导入 kmz/ovkmz/建筑物表格时，同名建筑弹三选一（覆盖更新/保留现有/取消导入）、同文件按 SHA-256 二次导入提示「已导入过一次，是否覆盖」；
  2. 模糊匹配→智能匹配（置信度评分，歧义时在选定范围内弹确认卡片，确认更方便）；
  3. 每个页面具备「✕ 退出本页」按钮 + 三击空白呼主菜单；
  4. 菜单命令执行中弹「请稍后」忙提示浮层（`busy()`）；
  5. 查询/筛选置顶显眼 + 实时计数提示区 + 确定后蓝/绿虚线圈选 + 单/多/零结果跳转；
  6. 信息与帮助新增「四端功能对照单」子菜单（记录每版本四端功能差异，v3.24 起）。
- 全 App 风格、背景、色彩、文字保持一致；未删除任何已有功能与菜单。

### 三、版本与产物
- `APP_VERSION` 升 v3.25，`APP_BUILD_DATE` 2026-08-20；Android `AndroidManifest` versionCode=25 / versionName=3.25；`version.json` 同步。
- 用 `multi-platform-app-pack` 技能一键生成四端部署文件：Android APK、统信 UOS DEB（不含绿版）、Win11 x86_64 安装包、Apple HTTPS PWA 可托管 ZIP，并附分平台校验与四端功能对照单。

## v3.26 变更明细（2026-08-21）

### 一、地图操作控件（需求①）
- 页面右侧中部新增 4 个悬浮按钮：**＋ 放大 / － 缩小 / ☆ 收藏位置 / ⌂ 返回收藏位置**（`#mapCtrl`），风格与 App 主色一致。
- 收藏：将当前地图中心与缩放级别存入 `localStorage`（`shuili_bookmark`）；返回收藏：一键 `setView` 跳回并弹「收藏位置」标记。离线可用。
- 放大/缩小与 Leaflet 内置控件等效，满足「智能调节功能与位置」——控件置于右侧中部，避开顶栏/底图切换器。

### 二、管理所名称统一（需求③）
- 新增 `normOffice(o)` 统一规则：
  1. 去掉「管理」二字：温泉管理所 → 温泉所、埝头管理所 → 埝头所（导入/导出统一处理）；
  2. 潮河特殊：潮河管理所 / 潮河总干渠管理所 → **潮河所**；
  3. 查询/筛选比较忽略「管理」：史山所 与 史山管理所 视为同一管理所；
  4. **管理站(station) 是管理所下一级单位**，独立字段、不参与合并。
- **应用范围**：CSV/XLSX 表格导入（processBuildingRows）、ovkmz/KMZ 导入（office 解析）、建筑物表格导出（doExportTable office 列）、ovkmz 导出分组与 KML `管理单位` 字段、筛选管理所列表去重（openFilter）、查询关键词匹配（passFilter）。

### 三、ZIP 多层目录导入（需求④）
- 导入建筑物表格 ZIP / 照片 ZIP 时，递归搜索全部层级目录（JSZip 全路径查找 `Object.keys(zip.files)`），支持多层子目录结构。

### 四、四端功能对照表升级（需求⑤）
- App 内「信息与帮助 → 四端功能对照单」新增 **v3.26 行**：地图操作控件 / 管理所名称统一 / ZIP 多层目录导入；`gen_compare.py` 同步更新功能矩阵并新增「**国产平台架构对照表**（固化）」：3A3000/3A4000→mips64el、3A5000+→loongarch64、FT2000/鲲鹏→arm64、其余→amd64。

### 五、UOS DEB 龙芯安装失败修复（需求⑥ · 关键）
- **现象（二次）**：龙芯 3A4000 / 内核 4.19 统信 1050 报 `无法创建 /opt/shuili-map/app.js.dpkg-new: 没有那个文件或目录`。
- **根因**：`data.tar.gz` 缺**显式目录条目**（`./opt/`、`./opt/shuili-map/` 及子目录），dpkg 无目录可写。
- **修复**（build_deb.py `make_data_tgz`）：
  1. 显式写入目录条目（typeflag=5，父目录先于子目录：`./opt/` → `./opt/shuili-map/` → 子目录）；
  2. 保持 GNU_FORMAT（无 PAX 扩展头，修复首轮「不支持的 PAX tar 头部类型 'x'」）；
  3. 文件路径统一 `./opt/shuili-map/...` 前缀。
- **验证**：GNU tar 实解成功；`verify_pkg.py` 新增「目录条目 + 前缀」双重校验。

### 六、全量需求回归确认（无回退）
- 需求②（图标颜色+形状双维度 + 左下角图例，v3.25）、需求⑦（查询/筛选置顶 + 实时计数 + 蓝/绿虚线圈选 + 单/多/零结果跳转）、第三~六原则（导入同名三选一/SHA-256 二次导入提示、智能匹配、每页退出 + 三击空白、执行中「请稍后」）经核对确认无回退。

### 七、版本与产物
- `APP_VERSION` 升 v3.26，`APP_BUILD_DATE` 2026-08-21；Android `AndroidManifest` versionCode=26 / versionName=3.26；`version.json` 同步。
- 用 `multi-platform-app-pack` 技能一键生成四端部署文件（DEB 已含目录条目修复），附分平台校验与四端功能对照单。

---

## 2026-08-23 跨项目修复与优化（感知 v1.6 / 水利 v3.30 / 古建 v1.8）

### 一、感知项目（v1.5 → v1.6）
1. **APK 体积优化（需求①）**：删除内置离线瓦片包 `assets/offline_tiles/`（2216 张 PNG）+ `offline_manifest.json`，APK 由 **11.5MB → 308KB**。此前用户怀疑内置照片，经核查照片已于 v3.28 删除，11MB 实为离线瓦片。底图改为统一在线天地图瓦片（矢量/影像），离线地图选项停用。**无网时地图区域空白，但全部业务功能（台账/巡视/筛选/导入导出）不受影响**。
2. **设施录入字段（需求②）**：菜单「添加建筑物」→「添加感知设施设备（点地图定位）」；编辑页新增 **所属系统**（下拉：视频监控/雨水情监测/大坝安全监测/地下水源监测）、**设备型号**、**固定资产编号**；新增 **设备主要参数**（可多行增删）；弹窗同步展示上述字段。
3. **照片目录菜单（需求③）**：信息与帮助重新挂回「照片（目录与命名）」入口（函数 `openPhotoHelp` 一直保留），展示四端照片目录与命名约定。

### 二、水利项目（v3.29 → v3.30）— 修复安装后中间空白崩溃
- **现象**：安装后有菜单，但中间空白，提示「运行错误：Script error. @0:0」，无法操作。
- **根因**：`assets/` 目录**缺失 leaflet 地图库**（`leaflet/leaflet.js` + `leaflet/leaflet.css`），而 `index.html` 引用之；Leaflet 全局 `L` 未定义 → `initMap()` 抛错。
- **修复**：从同源版本 `perc-v13/assets/leaflet/` 补回 leaflet（约 164KB），重打包后地图正常、功能可用。

### 三、古建项目（v1.7 → v1.8）— 修复卡顿
- **弹窗延迟生成**：`bindPopup(popupHtml(b))` → `bindPopup(function(){ return popupHtml(b); })`，仅在点开时拼装 HTML，不再 render 时给全国 1032 个点预生成。
- **图标按色缓存**：`makeIcon` 按颜色缓存 DivIcon（全国仅约 6 类色），render 不再每个点新建 SVG。
- **WebView 硬件加速**：`MainActivity` 启用 `LAYER_TYPE_HARDWARE` + `LOAD_DEFAULT` 缓存复用。

### 四、跨项目修正（正确性）
- 感知/水利原生导出目录名误写为「古建景点打卡」（copy-paste 缺陷），统一改为项目正确名：感知→`水利感知项目一张图`，水利→`水利工程一张图`。不影响任何功能逻辑。

### 五、四端平台架构与差异对照表（约束②）
| 维度 | 安卓 (Android) | Windows 11 | 统信 UOS (1050 专业版) | 苹果 (iOS/macOS) |
|---|---|---|---|---|
| 形态 | 原生 APK（aapt2+d8+zipalign+apksigner） | 原生 NSIS 安装器（本地 HTTP 服务 + WebView2） | 原生 DEB（python3 http.server + WebView，GNU tar 兼容龙芯） | **PWA + 静态 HTTPS**（不打包原生） |
| 芯片/架构 | arm64 / armv7 | amd64 | **龙芯 3A4000 / mips64el**（内核 4.19） | Apple Silicon / Intel |
| 性能策略 | 原生高性能，硬件加速 WebView | 原生高性能，WebView2 | 原生高性能，注意 8G 内存 + 龙芯算力，避免浏览器打包导入 | 静态资源走 CDN/HTTPS，PWA 缓存 |
| 离线能力 | 业务数据本地 + 在线瓦片 | 同安卓 | 同安卓 | 依赖 HTTPS 托管，可 PWA 离线缓存 |
| 照片目录 | `Download/<项目名>/photos/<管理所>/<建筑物>/<序号>.jpg` | `webroot/photos/...` | `webroot/photos/...` | 同 Web，HTTPS 路径 |
| 地图瓦片 | 在线天地图（v1.6 起取消内置离线包） | 同安卓 | 同安卓 | 同安卓 |
| 功能差异 | 全功能；原生桥 storePhoto/readPhoto/exportPhotos | 全功能；照片走文件夹复制 | 全功能；龙芯机勿走浏览器 ZIP 导入 | 全功能；受 PWA 沙箱限制，部分原生桥（如系统定位设置跳转）以 Web API 替代 |

> **原则**：四端风格/背景/色彩/文字统一（CSS 变量驱动）；功能保留原则——只调顺序、不删功能；每页「退出当前页面」按钮 + 三击空白呼主菜单。

### 六、微信集成规划（需求 D，待落地）
**个人微信方案注册开放平台步骤**（用户问询）：
1. 注册微信开放平台账号：https://open.weixin.qq.com → 开发者资质认证（企业/个人均可，个人需实名）。
2. 创建「移动应用」，填包名（`com.shuili.ganzhi` 等）+ 签名 MD5（用 keystore.jks 的 SHA1 填开放平台），获取 **AppID**。
3. 三个 App 各建一个移动应用，分别拿 AppID（iOS 用同一开放平台下的「网站应用」+ Universal Link）。
4. 集成：Android 用 `WechatOpenAPI`（或 `ShareSDK`）实现授权登录（oauth2 code→token→userinfo）；群消息读取/发送需**企业微信**或**微信机器人（企业微信群机器人 webhook）**，个人微信无官方群消息读取 API。
5. **结论**：个人微信方案只能做「授权登录 + 分享到会话/朋友圈」；**读群消息、向群发消息**必须走企业微信（群机器人 webhook 可发消息+图片，但收消息需企业微信会话存档 API，需企业资质）。建议 D 功能先落地「授权登录 + 分享」，群收发单独评估企业微信方案。

### 七、版本与产物
- 感知 `APP_VERSION` v1.6 / 2026-08-23；水利 v3.30；古建 v1.8。
- 归档：`APK归档/感知项目一张图/水利感知项目一张图V1.6_20260823_5090.apk`、`APK归档/水利一张图V3.30_20260823_5090.apk`、`APK归档/古建景点打卡V1.8_20260823_5090.apk`；根目录各留一份。
- 回退备份：`backup_2026-08-23/perc-v13_pre-v1.6-src`、`shuili-v329_pre-v3.30-src`、`gujian-android_pre-v1.8-src`。

---

## 2026-08-23 四端同步（约束②③⑤⑦⑧⑨）

将当日安卓三端新功能（感知 v1.6 / 水利 v3.30 / 古建 v1.8）**全量同步至 Win11 / 统信 UOS / 苹果 PWA** 三端。

### 一、同步策略（四端网页逻辑完全复用）
- 网页 `app.js / data.js / index.html / leaflet` 四端同源；原生壳仅替换承载方式（安卓 APK / Win WebView2 / UOS PyQt6 QWebEngine / iOS PWA ServiceWorker）。
- 安卓版为**已验证超集**（含 Web 照片回退 `new URL(p.file, location.href)`），故采用「安卓素材 → 各 webroot 整体覆盖」方式同步，零丢失 Web 专属逻辑。
- 源→目标映射：
  - 感知 `perc-v13/assets/*` → `win-webview2/webroot` + `uos-pyqt6/webroot`
  - 水利 `shuili-v329/assets/*` → `win-water-webview2/webroot` + `uos-water-pyqt6/webroot`
  - 古建 `travel/android/assets/*` → `travel/webroot`
  - 水利 iOS PWA → 重建 `水利工程一张图_iOS_3.30_20260823_可托管.zip`（manifest version 3.30.20260823）

### 二、同步落地内容
- 感知：APK 瘦身（删离线瓦片）/ 添加感知设施设备 + 新字段 / 照片目录菜单，三端一致。
- 水利：leaflet 补回修复（Web 端本无此缺，随同步确认一致）/ 菜单与页面统一。
- 古建：弹窗延迟生成 + 图标按色缓存（Web 端同样生效，1032 点更流畅）。
- UOS DEB 版本号对齐：感知 `uos-pyqt6/build_deb.sh` VERSION→1.6.20260823；水利 `uos-water-pyqt6/build_deb.sh` VERSION→3.30.20260823。

### 三、平台差异处理（约束③，允许不同）
| 能力 | 安卓 | Win11 | 统信 UOS | 苹果 PWA |
|---|---|---|---|---|
| 照片落盘 | 原生 storePhoto 落 Download | webroot 相对路径（HTTP 读） | 同 Win | 同 Web（HTTPS 托管） |
| 系统定位跳转 | 原生 openLocationSettings | 浏览器 geolocation API | 同 Web | 同 Web |
| 离线能力 | 业务数据本地 | 同安卓 | 同安卓 | ServiceWorker 缓存 App Shell |
| 微信分享/群 | 待企业微信 | 同左 | 同左 | 同左（本次放弃） |

### 四、验证
- 五 webroot `app.js` 全部 `node --check` 通过；版本号/新菜单/图标缓存/Web 照片回退标记均命中。
- iOS PWA zip 重建成功，manifest version 3.30.20260823。
- 功能保留：未删任何菜单/功能；风格 CSS 变量四端统一。

---

## 2026-08-23 四端最终部署文件生成（约束⑧⑨）

将当日新功能全量产出**可部署安装包**，归档于 `APK归档/四端安装包_20260823/`。

### 一、安卓 APK（已构建+签名）
| 项目 | 文件 |
|---|---|
| 感知 v1.6 | 水利感知项目一张图V1.6_20260823_5090.apk |
| 水利 v3.30 | 水利一张图V3.30_20260823_5090.apk |
| 古建 v1.8 | 古建景点打卡V1.8_20260823_5090.apk |

### 二、Win11 原生安装包（WebView2 C# 单文件 exe + 便携安装/卸载）
- 构建：`dotnet publish -c Release -r win-x64 --self-contained false`（需 Win11 .NET 8 运行时）
- 产物：`<项目>_Win11_x86_64_便携版_vX.X.zip`，内含 `*.exe` + webroot + `install.bat`（复制到 Program Files + 开始菜单/桌面快捷方式）+ `uninstall.bat`（删除目录与快捷方式）。
- 三项目均生成：感知/水利/古建。
- 本环境无 NSIS，故为便携版 zip + bat 安装卸载（非向导式 exe 安装器）；如需 NSIS 真安装器，在 Win11 装 NSIS 后跑 `build_all.sh`。

### 三、统信 UOS DEB（PyQt6 + QWebEngine 原生壳）
- 构建：纯 Python `native-shell/build_uos_deb.py`（无需 dpkg-deb），GNU_FORMAT + 显式目录条目，龙芯 3A4000 / 内核 4.19 兼容。
- 依赖：`python3-pyqt6` + `python3-pyqt6.qtwebengine`（UOS 上 apt install）。
- 产物：`<项目>_X.X.0_20260823_uos.deb`，含 main.py + webroot + /usr/bin 启动器 + .desktop。
- 三项目均生成：感知/水利/古建。

### 四、苹果 iOS PWA（静态 HTTPS 托管 zip）
- 水利：`水利工程一张图_iOS_3.30_20260823_可托管.zip`（manifest 3.30.20260823），Safari「添加到主屏幕」即类原生 App。
- 注：感知/古建本次未单独出 iOS zip（历史仅水利有 iOS 包）；如需可复用 `build_apple_zip.py`。

### 五、验证
- 三 APK apksigner verify 通过；五 webroot node --check 通过；三 Win exe 发布成功；三 UOS deb ar 结构校验通过。
- 回退备份：`backup_2026-08-23/`。

---

## 2026-08-24 照片性能与老 WebView 兼容性大迭代（v3.30.1 → v3.30.3）

> 本日完成「照片导入可靠性 + 照片性能 + 老设备兼容」三轮迭代，三产品（水利/感知/古建）四端（APK/UOS/Win/Apple）同步升 .1/.2/.3 系列。下面对水利一张图（母版）记录变更，派生 APP 同源对齐。

### 一、安卓 ZIP 照片导入「显示为 0」根因修复（v3.30.1）
- **现象**：批量导入含中文文件名照片 ZIP（如水印中文名），点确定后导入结果「照片显示为 0」。
- **双因**：① ZIP 条目名中文乱码（主因）——`ZipInputStream` 默认按 UTF-8/平台编码解码，7-Zip/Windows 打的含中文名 ZIP 多用 GBK/GB18030，名被乱码化 → 扩展名 `.jpg` 匹配失败 → 抽到 0 张；② 文件夹层级只取首段（次因）——原 `folder = n.substring(0, n.indexOf("/"))` 丢弃嵌套「建筑物」层 → 智能匹配按文件夹名匹配时丢失关键层。
- **根因修复**：`LargeFileManager.unzipImages` 改 `ZipFile(File, Charset)` 显式指定字符集 + `detectZipCharset()` 依次尝试 UTF-8→GBK→GB18030（按是否出现 Unicode 替换符判定）；`folder` 改取完整相对目录 `n.substring(0, n.lastIndexOf("/"))`。三端（水利/感知/古建）同源同步。
- **验证**：三端 `javac -cp android-34.jar` 编译通过；DEX 确认含 GBK/GB18030 字符串；APK 门禁全过。

### 二、两个真实 bug 修复（v3.30.2）
- **Bug A（对勾丢失）**：添加建筑物选照片后 名称/管理所/管理站 右侧「对勾」丢失。根因：「对勾」是 WebView 自动填充标识（非代码 CSS），`fPhotos.onchange` 内 `openEdit(b)` 整体重建 `editBody.innerHTML` 销毁 input.f → 新 input.value 一致但 WebView 未识别。修复：抽 `renderPhotoGrid/buildPhotosHtml/bindPhotoCardEvents`，`fPhotos.onchange` 改用 `renderPhotoGrid()` 只刷 `phGrid` 子树、**保留 input.f DOM**。三产品源/原生壳/归档 win 共 21 份同步。
- **Bug B（ambiguous 保存 0 张）**：ZIP 导入有 ambiguous 待确认项时，点确认保存 0 张。根因：`finishPhotoMatch` 在把 ambBox/dupBox/unmbBox 写入 DOM **之前**就调用了 `maybeCleanInbox()`（函数上方注释说「此处不立即 cleanInbox」但函数体却调了 → 注释与代码矛盾型 bug），三个 `getElementById("ambBox")` 守卫全 null 失效 → `cleanInbox()` 删光 `inbox/uz_*` 临时目录 → 确认时 `linkPhoto(a.it.path)` 找不到文件返回 "" → 0 张。修复：删除该错误调用，清理交还 `confirmAmbiguous/confirmUnmatched/dupOverwrite` 各自末尾（DOM 写入后才调，时机正确）。有 ambiguous 项时 100% 必现；全 auto 匹配不触发（古建用内存守卫 `__dupBlocks.length`，时序天然正确，此 bug 不存在）。
- **验证**：`node --check` 14 份主文件全过；6 份关键文件确认 `fPhotos.onchange`→`renderPhotoGrid()`、`finishPhotoMatch` 内 `maybeCleanInbox()` 已删。

### 三、照片性能三件套 + 老 WebView 兼容（v3.30.3）
- **性能根因**：500+ 建筑、每张 ~7M 原图。①筛选「有照片/无照片」卡死——`showFilterResult()` 为每个命中建筑**立即** `bindPopup(popupHtml(b))`，`popupHtml` 内 `photoSrc(p)` 同步调原生 `thumbPhoto(720)` 解码 7M 原图 → 同步大 IO 阻塞主线程；②`thumbPhoto` 无持久缓存，每次重新解码；③导入链路全部原样落盘不压缩（500×7M≈3.5GB+）。
- **三件套落地**：①筛选/查询高亮 marker popup 改**函数式懒构造**（卡顿根因消除）；②显示改 `ensureThumb(320)` 持久化缩略图（`.thumbs/<size>/` 缓存，首次生成后永久复用）；③导入压缩：`>1.5M` 大图（zip 批量/ovkmz/单张添加/巡视）弹窗选压缩目标（200K/500K/1M/不压缩，默认 500K），原生 4 并发压缩带进度，只存压缩版（删原图前明确提示）。
- **ovobj / obj 奥维坐标导入导出**：独立 `ovobj_bridge.js`（纯函数，含 parse/build Ovobj·Obj + import/export/merge），三产品 index.html 在 app.js 前引入；菜单「传输与共享」组加 [导入ovobj][导出ovobj·实验][导入obj坐标][导出obj坐标]。安卓端 `MainActivity`+`LargeFileManager` 新增 `readFileBase64(path)` 桥供 JS 解析二进制 ovobj。导入 ovobj/obj + 导出 obj 可靠；导出 ovobj 为实验性（名称不等长会字段错位），建议用 obj 文本导出。
- **老 WebView 兼容**：用户装 3.30.3 仍报 `Script error @0:0` → 根因是系统 WebView 未更新（Chrome 37）缺 ES6 API（`list.find`/`Array.from`/`Object.assign`/`TextDecoder`），崩的功能恰好用了这些。修复：app.js + ovobj_bridge.js 顶部补 `Array.prototype.find/findIndex/Array.from/Object.assign/TextDecoder/TextEncoder` polyfill（含手写 utf8 编解码兜底老 WebKitGTK）；`onPickFiles`/`importOvobj` 套 try/catch + toast 具体错误；onerror 增强暴露真实堆栈。
- **版本升 .3**：水利 3.30.3(code33)/感知 1.6.3(code12)/古建 1.8.3(code12)；4 份 app.js APP_VERSION+buildDate、4 份 version.json 全对齐（build_apk.sh 门禁自动同步 manifest）。

### 四、四端同步与桌面端原生桥补全
- 版本一致性：六 webroot（三产品源 + 感知 win-webview2 + 古建 win-gujian-webview2 + 水利桌面）对齐 .3 系列。
- **深层缺口修复**：Win C# 壳 / UOS Python 壳的 `window.Android` 桥把 `unzipImages/linkPhoto/fileSha256/photoSha256/cleanInbox` 全标 `not supported` → 桌面端照片导入残废。已为两端真实实现这些方法（含 GBK 探测 + 同步返回 JSON 且触发 onUnzipImages 回调，兼容水利/感知异步 + 古建同步两种约定）。
- 小程序：ZIP 批量导入是原生壳专属，菜单「批量导入照片」指向单张拍照 collect 页，架构正确，无需同步原生导入逻辑。
- 已知限制：桌面端（Win/UOS/iOS）无原生压缩桥，照片压缩仅安卓完整；桌面显示走 URL 直读原文件（筛选懒构造已修复，弹窗单张显示 7M 原图仍偏慢）。

### 五、验证
- 三工程 `javac` + `build_apk.sh` 门禁（versionCode/versionName/launchable/签名）全过；DEX 含 GBK/GB18030/compressPhoto/ensureThumb 常量。
- `node --check` 14 份主文件全过；老环境（删 Array.find/from/Object.assign/TextDecoder）模拟 → polyfill 生效、真实样本 24/24 点解析中文名正确。
- 交付：`APK归档/四端安装包_20260824_v33/{水利工程一张图,水利感知项目一张图,古建景点打卡}/` 各含 4 端 + 对照单（V3.30.3/V1.6.3/V1.8.3）。

### 六、待办（人工）
- 真机装 v3.30.3 验证（须**卸载旧 APK** 装新）；用 `C:/Users/admin/Downloads/埝头管理所建筑物照片.zip` 验证应保存 16 张；古建端同 zip 回归。
- 导出 ovobj 兼容性待真机验证（建议改 obj 文本导出）。
- 桌面端照片压缩为后续项（需原生桥扩展）。

### 七、关键教训（沉淀）
1. 「对勾」非代码画，是 WebView 自动填充；修复靠保留 DOM 而非改 CSS。
2. 注释与代码矛盾 = 高危信号，必须检查（Bug B 即此）。
3. DOM 守卫（getElementById）的清理逻辑须在 innerHTML 写入后调用，否则永远踩坑；内存守卫（数组 length）天然安全。
4. 静态分析 + Node 时序模拟可高效定位时序型 bug，无需真机复现。
5. 安卓老 WebView（系统 WebView 未更新）缺 ES6 API 是「部分功能崩+部分正常」经典成因——统一补 polyfill 是最优解。
6. **打包源多副本一致性**：本体 web 源码存在 `webroot_shuili/` 与 `android-build/shuili-v329/assets/` 两份，会分叉；打包前先比对版本号，以 assets 为准并同步 webroot_shuili（2026-08-25 已将 webroot_shuili 从 v3.26 同步到 v3.30.3）。

---

## 2026-08-25 ovobj/obj 全面修复与四平台一致性（v3.30.3 → v3.31 / 感知 v1.6.3 → v1.7 / 古建 v1.8.3 → v1.9）

### 一、用户反馈原文（7 条）
1. 导入导出 ovobj/obj 能否准确导入导出奥维坐标？能否仿照 ovkmz 选择文件夹和文件？
2. 导入导出 ovobj/obj **文本**文件能否准确导入导出文本坐标？同样要文件夹+文件选择。
3. 导出 zip 照片能否先压缩后导出？
4. 筛选建筑物/照片能否也导入到 UOS 平台，或按管理所导入导出？按管理所导出照片时必须包含**全部管理所**。
5. 个别菜单仍出现安卓平台导入 ovobj「运行错误：Script error. @0:0」。
6. 导入 ovobj 菜单点击后**没反应**，不知道是从哪个文件导入（系统 WebView 已是新内核）。
7. deb 平台安装总是提示**架构不匹配**。
附：添加建筑物同时点添加照片 → 建筑物信息从页面丢失；导入 zip 照片，完全匹配能保存、**最佳匹配保存不了**。

### 二、根因与修复（CHG-SHUILI-v3.31）
| 编号 | 变更 | 文件 |
|------|------|------|
| CHG-v331-01 | ovobj/obj 导入增加「单文件 / 整文件夹批量」选择对话框 + 明示可选后缀 | `assets/ovobj_bridge.js`（三产品） |
| CHG-v331-02 | `escapeJson` 补单引号转义，根治 `Script error. @0:0` | `MainActivity.java`（三产品） |
| CHG-v331-03 | 新增 `pickFolder(exts)` 目录桥：`ACTION_OPEN_DOCUMENT_TREE` + 递归枚举(≤6 层) + 后缀过滤 + `takePersistableUriPermission` | `MainActivity.java`（三产品） |
| CHG-v331-04 | `onPickFiles` 增加文件夹批量回调路由 | `assets/app.js`（三产品） |
| CHG-v331-05 | `openEdit` 表单 `oninput → flushEditFields()` 实时回写，建筑+照片同存不丢信息 | `assets/app.js`（三产品） |
| CHG-v331-06 | zip 导出前压缩：`addPhotoToZip` >1.5MB 走 `canvasCompress` | `assets/app.js`（三产品） |
| CHG-v331-07 | zip 最佳匹配保存失败：`finishPhotoMatch` 延迟 `cleanInbox`（canonical 已修，本次随同步落到 UOS/古建陈旧副本） | `assets/app.js` |
| CHG-v331-08 | 按管理所导出照片含「全部管理所」`__ALL__`（canonical 已有，随同步生效） | `assets/app.js` |
| CHG-v331-09 | DEB 架构对照表固化 + `--arch` / `ARCH=` 覆盖，修复架构不匹配 | `native-shell/build_uos_deb.py`、`make_deb.py`、`uos-*/build_deb.sh`×3 |
| CHG-v331-10 | 四平台 webroot 全量同步 canonical；两处 UOS + water-v329 的 `index.html` 补加载 `ovobj_bridge.js` | 6 份 webroot + 3 份 index.html |
| CHG-v331-11 | 全链路版本号 8 处同步（27 条）：3.30.3→3.31 / 1.6.3→1.7 / 1.8.3→1.9 | `bump_version_v331.py` 一键执行 |

### 三、UOS 架构对照表（固化）
| 芯片 | UOS 架构 | deb Architecture |
|------|---------|------------------|
| 龙芯 3A3000 / 3A4000 | mips64el | `mips64el`（默认） |
| 龙芯 3A5000 / 3A6000+ | loongarch64 | `loongarch64` |
| 飞腾 FT2000 / 鲲鹏 | arm64 | `arm64` |
| x86_64 | amd64 | `amd64` |

用户实际环境：统信 UOS 20 专业版 1050 / 内核 4.19.0-loongson-3-desktop / 龙芯 3A400 / 8G / 无 GPU → **mips64el**。

### 四、四端对照表
| 端 | 水利 v3.31 | 感知 v1.7 | 古建 v1.9 | 原生壳 |
|----|-----------|----------|----------|--------|
| Android | `android-build/shuili-v329` | `android-build/perc-v13` | `travel/android` | aapt2+d8+zipalign+apksigner |
| Win11 | `native-shell/win-water-webview2` | `native-shell/win-webview2` | `native-shell/win-gujian-webview2` | .NET8 WebView2 + WiX7 MSI |
| 统信 UOS | `native-shell/uos-water-pyqt6` | `native-shell/uos-pyqt6` | `native-shell/uos-gujian-pyqt6` | PyQt6 + DEB(mips64el) |
| iOS | 由 win-water webroot 级联 | 由 win-webview2 级联 | 由 win-gujian 级联 | PWA 静态 HTTPS 托管 |
| 小程序 | `jingyin/`（独立工程，无 ovobj/照片桥，仅版本风格对齐） | — | — | 微信小程序 |

### 五、未删除任何功能/菜单
本轮全部为「增强 + 修复」，`menuGroups` 条目数与 v3.30.3 一致，仅在 ovobj/obj 导入前插入选择方式对话框。

### 六、验证
- `node --check` 13 份 app.js/ovobj_bridge.js 全过。
- `build_apk.sh` 发布门禁（version.json 真相源校验 → manifest 同步 → aapt2 badging → apksigner verify）三产品全过。
- `make_deb.py` 本机实跑产出 mips64el DEB×3；`build_ios_zip.py` 产出 PWA×3。

### 七、2026-08-26 实际重新生成并交付（关键）

8/25 本轮仅完成**源码修复 + 文档**，但三产品 APK **实际未出包**——交付到用户设备的仍是 v3.30.1（2026-08-24 构建），故用户侧复现了全部 bug（ovobj 文件夹卡死、Script error @0:0、建筑+照片丢信息、"架构不匹配"）。

2026-08-26 通过 `release_v331.sh` 实际重建并交付三产品 × 四平台，统一归档 `APK归档/四端安装包_20260825_v331/`：
- 水利 v3.31 / 感知 v1.7 / 古建 v1.9 各含 **APK**（`aapt2 badging` 校验 versionName + `apksigner verify` 全过）、**统信 UOS DEB**（`Architecture: mips64el`，`tarfile` 解包验证 control 字段）、**Win11 MSI(WiX7)+NSIS EXE**、**iOS PWA ZIP**（`build_ios_zip.py`）。
- DEB 架构对照已生效：龙芯 3A4000→`mips64el`（修复"架构不匹配"）；旧 `_all.deb` 弃用。
- 回退备份：`D:/Users/Claw/backup_2026-08-26/`（含 9 份 canonical webroot + 已交付四平台包）。

### 八、v3.32（2026-08-27）管理所选项补全 + 三端四平台升版
- **需求⑤ 管理所选项补全（水利一张图）**：新增 `CANONICAL_OFFICES` 权威清单（地下水源所、温泉所、龙山所、史山所、埝头所、水库所、北台上所、西田各庄所、潮河所），`officeOptions()` 合并「权威清单 + 数据实际管理所」，保证筛选 / 导出范围 / 编辑表单的「管理所」选项**不遗漏任何一所**。
- `normOffice` 补齐归一化：`潮河总干渠`→`潮河所`、`怀柔水库所`→`水库所`（修复此前 `潮河总干渠` 因无「管理所」后缀未被归一、导致筛选出现 `潮河总干渠` 与 `潮河所` 两份的瑕疵）。
- **版本八处同步升 v3.32 / v1.8 / v2.0**：`bump_version.py` 同步 `version.json` / `app.js` / `AndroidManifest` / 6 webroot / `build_msi.wxs` / `build_deb.sh`；**手动补两处 dict 版本源**：`make_deb.py` 的 `PROJ[].ver`、`build_ios_zip.py` 的 `PROJECTS[].file_ver/new_ver/readme_ver`（这两处为 dict 形式，`bump_version.py` 正则未命中，须手动改，否则 DEB/iOS 仍打旧版本号）。
- **四平台实跑重建并交付**：归档 `APK归档/四端安装包_20260827_v332/`，三产品各含 APK（签名校验通过）+ 统信 UOS DEB（mips64el，ar size 含补齐字节、双解析器一致，已根除「请检查deb是否损坏」）+ Win11 MSI(WiX7)+NSIS EXE + iOS PWA ZIP。
- **验证**：`node ovobj_verify.js` 真实 `.ovobj` round-trip 全过（解析 69 点 → 导出 24→24 → 文本导出 → merge → 无 ReferenceError）；`officeOptions()` 实证命中全部 9 所；交付 DEB 经 deepin 双规则 ar 解析对齐 ✅。
- 回退：旧归档 `四端安装包_20260825_v331/` 保留作 v3.31 回退。

### 九、v3.41 / v1.17 / v2.9（2026-08-29 晚）智能AI×知识库深度融合 + 交互修复
- **发行前预生成知识库**：构建期 `gen_kb_seed*.js` 生成 `kb_building_seed.js`（shuili 557 / perc 1125 / gujian 1032 条）随包内置，`mergeSeed()` init 合并；`buildBuildingBody()` 扩展子系统/属地/朝代/级别/简介/特点通用字段。
- **查询融合**：智能AI 与知识库查询同引擎——本地优先(🔒)→允许联网再在线(🌐)→友好提示；知识库查询加确认按钮；全程 resolve 不 reject，根治「运行错误: script error」。
- **知识库网页入库**（普通/微信公众号/微博）+ **智能更新补丁可编辑**（预览字段改输入框）。
- **水利专项**：筛选照片状态卡顿根治（事件委托，不再 `innerHTML` 重建）；管理所/管理站手动增改（逐个确认同步建筑物）+ 表单下拉化；关键词历史可开收；周边搜索三中心（建筑/当前位置/地图选点）+类型筛选；导出表格/照片全 9 所+按建筑自动匹配；四类导出文件名可编辑（ovkmz/ovobj 新增导出对话框）。
- **四平台重建交付**：`APK归档/四端安装包_20260829_v341/`（APK 门禁 42/22/22 全过；deb mips64el `verify_myml_ar` 3/3；MSI `wix msi validate` 过 + NSIS EXE；iOS PWA×3）；`build_win_exe_nsis.py` 版本表补齐（3.41.0/1.17.0/2.9.0）。
- **小程序补同步**：jingyin config.js(3.38→3.41)/app.js(3.40→3.41)/package.json(3.33→3.41.0) 三处对齐（修复今晨漏更）。
- **回退**：`backup_2026-08-29_before_v341/` + 旧归档 `四端安装包_20260829_fixed/`（v3.40）保留。

### 十、v3.42 / v1.18 / v3.0（2026-08-30）五级组织 + 快捷常用 + 导出组装器 + zip 三级匹配
- **五级组织**：完整字段 `局 → 管理处 → 所 → 站 → 段`，默认 `局=水利工程管理中心 / 管理处=京密引水管理处 / 所=水库所`；`ORG_LEVELS` 持久化到 localStorage `org_levels`，重命名对每个受影响建筑物逐个 confirm 同步；新增菜单入口「设置 → 组织与类型管理」三 Tab。
- **快捷常用（★）**：菜单置顶新组，`toggleFavMenu` 单项 ☆/★ 切换 + 设置里批量勾选；空则隐藏；原子菜单保留。
- **导出组装器**：`assembleExportName` / `assembleExportPath` / `renderExportOptions` / `readExportOptions` / `expDefaultCfg`，四类导出（kmz/ovobj/photos/table）全部支持"文件名组成（默认 所_建筑物名）"+"文件夹层次（默认 所/）"勾选；按导出类型独立持久化 `exp_cfg_<kind>`；`exportKmz` 签名扩展 `(fileName, useFilter, cfg)`。
- **zip 三级匹配钩子**：`__uzAcc.zipQueue` 跟踪当前 zip 名 + `onUnzipImages` 把 zip 的 nameNoExt 作为 `item.zipName` 写入（legacy JSZip 路径同步）；`matchOrgInToken` 在 5 级组织名称中按命中等级最强锁定 + `itemOrgScope` + `passOrgScope` + `finishUzIfDone` 注入 `it.orgScope` 供 `runPhotoMatchNative` 收敛候选。
- **perc 5 类分类方案**：`PERC_TYPE_SCHEMES = [信息系统分类/感知子系统/中类/子类/信息传输方式]`；`orgPanelTypesPerc` 自动按 `APPNAME === "水利感知项目一张图"` 触发；每个 scheme 独立 `org_perc_types_<scheme>`。
- **古建**："参考智能添加"，通用增强同步到 travel/android/app.js（暂无 5 类分类，避免破坏古建字段）。

### 十一、v3.43 / v1.19 / v3.1（2026-08-30 v2）水利 5 项 hotfix + AI placeholder 适配
- **B1 筛选顺序**：历史关键词段上移到「管理所」之上（在查询关键词之后）。
- **B2 默认值**：多管理所（offices[]）+ 段默认可空（`duan: ""`）+ 段/所独立。`getOrgDefaults/setOrgDefaults` 增加 `offices[]`；`defaultOffice()`/`bZhan()`/`bDuan()`/`bJu()`/`bGuanChu()` 辅助函数统一兜底；旧单一 `suo` 自动迁移为 `offices[0]`。UI 上「默认管理所」改为 chip 多选，「默认段」加「（空：无默认段）」选项。
- **B3 建筑物类型**：增/改/删 + 逐个 confirm 同步（已有实现，UI 端启用确认按钮+toast）。
- **B4 周边搜索类型筛选**：nbtype chip className 提升 + stopPropagation + toast 反馈；空结果标注「（已选类型：xxx）」。
- **B5 快捷常用与点击分隔**：星标 stopPropagation + preventDefault + 菜单项 onclick `closest(".menu-fav") return`。
- **B6 zip 匹配 level-aware**：`itemOrgScope` 按 局→处→所→站→段 顺序逐级匹配，命中即返回 `{level, value, parentLevel}`；folder 子匹配链式下钻；示例：`史山.zip` → 所=史山所，folder 段开始匹配 zhan (站)。
- **C AI placeholder**：古建 → "如：颐和园佛香阁的建筑年代与结构特点"；感知 → "如：北台上液位传感器最近一次监测数据"；水利（默认）→ "如：龚庄子进水闸的设计流量与结构特点"。

### 十二、v3.44 / v1.20 / v3.2（2026-08-30）三日智能化更新三端深度同步 + 按核心差异化重构
- **本次修复的 perc 历史 cp 失误**：`LS_KEY` 从 `shuili_map_v2` 改 `perc_map_v2`；`APPNAME` 从「水利工程基础信息一张图」改「水利感知项目一张图」；数据源从 `window.SHUILI_DATA` 改 `window.PERCEPTION_DATA`；`AIModule.init` 的 domain/appName/fieldSchema 全部回归感知域（fieldSchema 增 `subsystem` 设备子系统）；UOS webroot 从 uos-pyqt6 替换为 perc 完整感知 webroot。
- **古建 v3.2 智能化升级**（公共受众向，~13KB 注入到 `app.js` 末尾 IIFE 内）：① 5 级景区组织 `spot/area/sub/point/tag`（仿水利 5 级结构但 key 名调整）= 景区/园区/子景点/打卡位/标签；② 17 类古建专属 `CANNONICAL_TYPES`（古塔/寺庙/城墙/古桥/园林/陵墓/书院/会馆/故居/古街/楼阁/亭台/坛庙/宫殿/石窟/衙署/其他）；③ 快捷常用 ⭐ 菜单组（同款水利 B5 隔离机制）；④ 历史关键词可折叠面板（B1 古建版）；⑤ 周边搜索 nbtype chip（B4 古建版，包装原 nearbySearch）；⑥ ZIP 三级匹配古建版（chain: spot→area→sub→point→tag）；⑦ AI 系统提示词注入古建文物助手分支（联网核实公开文物，优先引用文物局/UNESCO/世界遗产委员会官网）。
- **三图核心差异**：水利核心=水工建筑物（内部）/ 感知核心=感知切切实设备（内部）/ 古建核心=古建（**公共受众**）。AI 联网策略：水利/感知默认关（内部资料不编造），古建默认开（公开文物可核实）。
- **ai_module.js 三态 placeholder 已通用化**：`CFG.appName.indexOf("古建")` → 颐和园佛香阁 / `.indexOf("感知")` → 北台液位计 / 默认 → 龚庄子进水闸。三端 webroot 共用一份 70623 字节 ai_module.js。
- **四端出包**：`APK归档/四端安装包_20260830_v344/`（APK×3 + deb mips64el×3 + MSI×3 + EXE×3 + iOS PWA×3 = 15 件）；小程序 jingyin 三处 baseline 同步 `Android v3.44 (2026-08-30)`。
- **回退**：`backup_2026-08-30_before_v344/`。

### 十三、v3.45 / v1.21 / v3.3（2026-08-30）5 日智能化更新三端深度同步 + 按核心差异化重构

**E1 设置菜单 - 修改/添加天地图密钥**（三端共用）
- 三层回退：localStorage 自配 → window.TIANDITU_TOKENS 环境 → 内置默认
- 32 位 16 进制校验（非 32 位弹确认对话框，仍可强行保存）
- 📋 复制当前密钥（兼容 navigator.clipboard，老 WebView 回退 execCommand）
- 恢复内置默认按钮
- 保存后立即 refreshTdtTileLayer() 重建底图图层（无须重启 App）

**E2 筛选 - 管理处可多选 chip**
- filters.guanchu 新持久化字段，默认值「京密引水管理处」
- passFilter() 加 guanchu 判断（按局→管理处层次，前级筛掉不进入后级）
- openFilter() 在管理所之上加「管理处」chip 行（guanchuOptions = 权威 5 项 + 自定义清单 + 数据实际值）
- filterActive() / filterDesc() / updateFilterCount() 同步加 guanchu 分支
- saveDefaultFilter() 通过 Object.keys(filters) 自动序列化

**E3 导出建筑物表格 - 管理处列**
- baseCols 在「名称」后插入 { k:"guanchu", t:"管理处", checked:false }，**默认不勾选**（兼容老用户）
- labelMap 增加 guanchu 映射；按建筑物读 b.guanchu（缺则回退默认值）

**E5 水利 zip 三级匹配 - longest-match level-aware**
- matchOrgInToken 每层内按 value 长度倒序（长→短），保证「史山管理所」优先于「温泉管理所」
- 跨级按 ju→guanchu→suo→zhan→duan 顺序，命中后 parentLevel = levels[li+1] 供 folder 子匹配链式下钻
- **案例验证**：C:/Users/admin/Downloads/史山.zip 内的 IMG-6857.jpg → 最匹配 史山所 的 三扬分水闸
  - token="史山.zip" 扫各级→最长匹配命中 "史山管理所"（len=4 → level=suo, parentLevel=zhan）
  - z.folder=""（裸放）→ 直接采用 zipName 命中的 scope
  - classifyOne 在所有史山所建筑物里按距离 + 名称匹配挑出"三扬分水闸"

**E6 三态 placeholder 已通用化**
- 古建→颐和园佛香阁 / 感知→北台上液位计 / 水利→龚庄子进水闸
- 三端 ai_module.js 完全一致，古建/感知不再出现"龚庄子"残留

**E7-E8 全量同步**
- 每页退出按钮 + 三击空白呼主菜单（v3.15+v3.6 已固化，三端回归验证）
- 快捷常用星标与点击隔离（v3.43 B5 已固化，三端回归）
- 古建 v3.3 用古建版 5 级组织 key=spot/area/sub/point/tag，感知 v1.21 保留感知独有元数据 subsystem

**四端出包**：APK归档/四端安装包_20260830_v345/（APK×3 + deb mips64el×3 + MSI×3 + EXE×3 + iOS PWA×3 = 15 件）；小程序 jingyin 三处 baseline 同步 Android v3.45 (2026-08-30)

**回退**：backup_2026-08-30_before_v345/ 638 个文件全量备份
