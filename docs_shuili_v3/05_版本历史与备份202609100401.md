# 版本历史与备份

## 水利工程基础信息一张图 APP
## v3.40 / v1.16 / v2.8（2026-08-29）

- 水利 v3.40、感知 v1.16、古建 v2.8 同期发版，版本号八处同步（version.json 真相源 + APP_VERSION + 6 webroot + make_deb/gen_win_msi/build_ios_zip 硬编码串，由 bump_version.py 一次对齐）。
- 智能AI 能力增强（Feature④⑤⑥⑦①，ai_module.js 一份代码九处加载）：①水利/感知新增「联网在线查询」开关（默认仅本地辅助，可逐次开启联网；古建默认联网）；②查询历史按时间保存（关键词/模式/时间），点击复用缓存省词元，可「强制重新查询」重新联网；③查询结果美观排版（标题/列表/重点加粗）＋保存知识库提供「精简 / 不保存」选项；④建筑骨干自动播种（水利 557 / 感知 1125 / 古建 1032 条，含参数/照片/坐标，知识库一键重新播种）；⑤知识库（md+json 混合，导入/导出 zip）三子菜单：读取外部文件入库 / 导出 zip 备份 / 导入备份；Hermes 自我学习循环（智能操作异步沉淀 hermes/operation 条目，后续查询注入上下文自我提升）。
- 四平台出包：Android APK（build_apk.sh 版本门禁 + badging + apksigner 全过）×3；UOS deb（make_deb.py，ar 头铁律空格填充 + 100644 + tar.xz，verify_myml_ar.py 门禁全过）×3 mips64el；Win WiX7 MSI + NSIS EXE（ProgramFiles64Folder 提权安装 + app.ico 图标 + 开始菜单/桌面快捷）×3；iOS PWA 可托管 zip×3。
- 回归：20 个 app.js/ai_module.js 全过 node --check；筛选默认值持久化、筛选/查询蓝绿虚线圈、每页退出按钮 + 三击空白进主菜单、知识库三子菜单、联网开关/查询历史/保存知识库/建筑骨干种子等标记静态核验齐备。

## v3.38 / v1.14 / v2.6（2026-08-29）

- 水利 v3.38、感知 v1.14、古建 v2.6 同期发版，版本号八处同步。
- 筛选默认值持久化 + 知识库（md+json 混合，导入/导出 zip）+ Hermes 自我学习循环；筛选结果可视化回归保持。
- 三日问题回归：verify_myml_ar.py 门禁对 shuili 3.37/perc 1.13/gujian 2.5 全过。

## v3.36 / v1.12 / v2.4（2026-08-28）

- 水利 v3.36、感知 v1.12、古建 v2.4 同期发版，版本号八处同步。
- 修复智能AI 联网查询「String contains non ISO-8859-1 code point」：HTTP 头中文 appName 经 toLatin1 转 ASCII。
- 菜单顺序智能调整：关于/帮助移至末尾，删除/恢复初始数据归「设置」子菜单，智能AI 按同名分组自动合并。
- 新增显示界面右键上下文菜单（地图/标注/列表）：获取坐标、周边搜索、导航、智能补全、智能描述此地；调用大模型自动填充简介/特征。
- 更换 OpenRouter 密钥（openclaw626），三个免费模型（MiniMax M2.7 / GLM 5.2 / Nemotron 3 Nano Omni）failover 实测；古建开放联网智能查询/更新/纠错，水利/感知保持「本地辅助」不联网编造。
- 三日问题（CSV 导入 readFileText、管理所全 9 所、UOS deb ar 大小补齐）回归保持。


## v3.35 / v1.11 / v2.3（2026-08-28）

- 水利 v3.35、感知 v1.11、古建 v2.3 同期发版，版本号八处同步。
- 智能AI 增强：内置 OpenRouter 三个免费模型预设（MiniMax M2.7 / GLM 5.2 / Nemotron 3 Nano Omni，均 :free 兼容 OpenAI）；默认自动调用策略改为 failover（免费模型限流自动切换）；本地 ai_seed.js 注入个人 Key（不写进共享源码）；chatOne 增加 OpenRouter 推荐头与 429/401 提示、reasoning 模型兜底。
- 古建开放联网智能查询/智能更新/智能纠错；水利/感知为内部资料，保持「本地辅助」模式不联网编造。
- 三日问题（CSV 导入 readFileText、感知筛选后勤 script error、管理所全 9 所、UOS deb ar 大小补齐）回归保持。


## v3.34 / v1.10 / v2.2（2026-08-28）

- 水利 v3.34、感知 v1.10、古建 v2.2 同期发版，版本号八处同步。
- 新增「智能AI设置 + 智能AI」双菜单：自定义大模型接口（OpenRouter 默认、OpenAI 兼容、可本地模型）、多模型默认选择与自动调用策略（仅默认/失败切换/轮询）、连接测试、配置导入导出。
- 古建开放联网智能查询/智能更新(补丁确认写入)/智能纠错(标注差异)；水利/感知为内部资料切换「本地辅助」模式，不联网编造。
- 三日问题（CSV 导入 readFileText、感知筛选后勤 script error、管理所全 9 所、UOS deb ar 大小补齐）回归保持。


## v3.33 / v1.9 / v2.1（2026-08-27）

- 水利 v3.33、感知 v1.9、古建 v2.1 同期发版，版本号八处同步。
- Bug1 CSV 导入 readFileText 报错修复（原生桥补齐 + 网页稳健读取，导出导入自兼容）。
- Bug3 感知筛选「后勤」script error 修复（appFly 按 id 查回建筑）。
- Bug5 管理所全 9 所；Bug6 统信 deb ar 大小补齐根除「文件损坏」提示。
- 三产品四平台 webroot 全量同步（含本轮回退修复），iOS PWA 同步。



| 项目 | 内容 |
|------|------|
| 文档编号 | VER-SHUILI |
| 更新日期 | 2026-08-25 |
| 维护人 | 炎冰 / 小七 |

---

## 1 版本历史

### v3.38（2026-08-29）

| 项 | 内容 |
|----|------|
| 版本号 | v3.38 |
| versionCode / versionName | 40 / 3.38 |
| 构建日期 | 2026-08-29 |
| 源码快照 | `D:/Users/Claw/android-build/shuili-v329/assets/`（canonical，四平台 webroot 由此同步） |
| 回退备份 | `D:/Users/aowwei_app/webroot_shuili_bak_v3.36_20260829_042448/`、`D:/Users/Claw/android-build/water-v329/assets_bak_v3.32_20260829_042449/`（镜像对齐前自动备份） |
| 交付文件 | `D:/Users/Claw/APK归档/四端安装包_20260829_fixed/` |

**迭代主线（v3.36 → v3.38）：** 筛选默认值持久化（首次注入非「全部」种子、每次筛选操作写回 localStorage、启动即套用避免全量渲染卡顿）+ 新增知识库（Markdown 正文 + JSON 元数据，md+json 混合，导入/导出 zip）+ Hermes 自我学习循环（智能操作异步沉淀 hermes/operation 条目，后续查询注入上下文自我提升）+ 筛选结果可视化（浅蓝虚线圈选 + fitBounds）回归 + UOS deb ar 头铁律 / 四平台同步。

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | 导入 ovobj/obj 点了没反应、不知从哪个文件导入 | 仅 `pickFiles("*/*")` 直调系统选择器，无引导、无文件夹入口 | `ovobj_bridge.js` 增「选单个文件 / 选整个文件夹批量」对话框，明示可选后缀（.ovobj/.obj/.txt/.csv） |
| 2 | 运行错误：Script error. @0:0 | Java `escapeJson()` 未转义单引号，而 `evaluateJavascript("onPickFiles('"+json+"')")` 用单引号包裹 → 文件名含 `'` 直接崩 | 三产品 `MainActivity.java` 的 `escapeJson` 增 `.replace("'","\\'")` |
| 3 | 无法像 ovkmz 那样选文件夹 | 全端只有文件选择桥，无目录桥 | 新增 `pickFolder(exts)`：`ACTION_OPEN_DOCUMENT_TREE` + `listTreeFiles` 递归（≤6 层）+ 后缀过滤 + 复用 `onPickFiles` 路由 |
| 4 | 建筑物与照片同时添加 → 已填信息丢失 | 表单文字只在 `appSave` 时回写 `editing`，任何表单重建（+增加参数 / 原生选图返回）都丢 | `openEdit` 绑定 `oninput → flushEditFields()` 实时回写 |
| 5 | 导出 zip 照片体积过大 | 导出直接塞原图 | `addPhotoToZip` >1.5MB 走 `canvasCompress`；原生导出读压缩后文件 |
| 6 | DEB 安装提示架构不匹配 | `build_uos_deb.py` 硬编码 `Architecture: all`；`build_deb.sh` 取构建主机架构 | 固化架构对照表（3A3000/3A4000→mips64el、3A5000+→loongarch64、FT2000/鲲鹏→arm64、其余→amd64），新增 `--arch` / `ARCH=` 覆盖 |
| 7 | 四平台源码分叉 | UOS webroot 落后到 v3.30.1，`index.html` 未加载 `ovobj_bridge.js` | 三产品 × Win/UOS webroot 全量同步 canonical，补 `<script src="ovobj_bridge.js">` |

**版本号全链路 8 处（本次同步 27 条）：** `assets/version.json`（真相源）→ `assets/app.js`(APP_VERSION+APP_BUILD_DATE) → `AndroidManifest.xml`(versionCode/versionName) → 6 份 webroot(version.json/app.js/ovobj_bridge.js) → `build_msi.wxs`×3 → `build_deb.sh`×3 → `make_deb.py`/`make_msi.py`/`gen_win_msi.py`/`build_ios_zip.py` → `build_four_ends.sh` → 本文档 + `04_软件变更文档.md`。

**交付状态（2026-08-26 实际重新生成）：** 8/25 仅落地源码与文档，APK 未实际出包（停留在 v3.30.1），用户设备跑旧包复现全部 bug。2026-08-26 已用 `release_v331.sh` 实际重建三产品 × 四平台并归档 `APK归档/四端安装包_20260825_v331/`：水利 v3.31 / 感知 v1.7 / 古建 v1.9 各含 APK（`aapt2 badging` + `apksigner verify` 全过）+ 统信 UOS DEB（`Architecture: mips64el`，已验证 control）+ Win11 MSI+NSIS EXE + iOS PWA ZIP。回退备份见 `backup_2026-08-26/`。

### v3.30.3（2026-08-24）

| 项 | 内容 |
|----|------|
| 版本号 | v3.30.3 |
| versionCode / versionName | 33 / 3.30.3 |
| 构建日期 | 2026-08-24 |
| 源码快照 | `D:/Users/aowwei_app/webroot_shuili/`（2026-08-25 由 android-build/shuili-v329/assets 同步至 v3.30.3，消除多副本分叉） |
| 交付文件 | `D:/Users/Claw/APK归档/四端安装包_20260824_v33/水利工程一张图/`（4 端 + 对照单） |

**迭代主线（v3.30.1→v3.30.3）：** 照片导入/性能/老 WebView 兼容三大修复。
- v3.30.1：安卓 ZIP 照片导入「显示为 0」根因修复（中文条目名 GBK/GB18030 乱码 + 嵌套目录层级丢失，改用 `ZipFile(File,Charset)` + detectZipCharset + 完整相对目录）。
- v3.30.2：Bug A（选照片后对勾丢失，保留 DOM 而非重建 innerHTML）；Bug B（ambiguous 确认保存 0 张，finishPhotoMatch 早于 DOM 写入误调 maybeCleanInbox 删光临时目录，删除该调用）。
- v3.30.3：照片性能三件套（筛选弹窗懒构造 / ensureThumb 持久缩略图 / >1.5M 导入压缩默认 500K）+ ovobj·obj 奥维坐标导入导出 + 老 WebView ES6 polyfill（Array.find/from/Object.assign/TextDecoder 兜底，修复 Script error @0:0）。

**回归确认：** 全 App 风格一致，未删除任何已有功能与菜单；六 webroot（三产品源 + 三桌面 webroot）版本对齐；桌面端原生桥真实实现 unzipImages/linkPhoto/fileSha256/photoSha256/cleanInbox（此前 not supported 属残废）。详见 `04_软件变更文档.md` 的「2026-08-24」章节。

### v3.26（2026-08-21）— 历史版本

| 项 | 内容 |
|----|------|
| 版本号 | v3.26 |
| versionCode / versionName | 26 / 3.26 |
| 构建日期 | 2026-08-21 |
| APK 大小 | ~8.2 MB |
| 交付文件 | `D:/Users/Claw/APK归档/四端安装包_20260821/水利工程一张图V3.26_20260821_5090.apk`、统信 UOS DEB、Win11 x86_64 安装包、Apple HTTPS PWA 可托管 ZIP（同目录） |
| 源码快照 | `D:/Users/aowwei_app/webroot_shuili/`（app.js / index.html） |
| 备份路径 | `D:/Users/aowwei_app/backup/before_v326_20260821/`（回滚点，含 v3.25 旧版） |

**功能（地图操作控件 · 需求①）：** 页面右侧中部新增「＋ 放大 / － 缩小 / ☆ 收藏位置 / ⌂ 返回收藏位置」四个悬浮按钮，收藏存本地、一键跳回，离线可用。

**功能（管理所名称统一 · 需求③）：** 新增 `normOffice()` 统一处理——导入/导出去「管理」二字（温泉管理所→温泉所、埝头管理所→埝头所）、潮河管理所/潮河总干渠管理所→潮河所；查询/筛选忽略「管理」（史山所=史山管理所）；管理站为管理所下一级单位、独立字段不合并。覆盖 CSV/XLSX 导入、ovkmz 导入、表格导出、ovkmz 导出、筛选去重、查询匹配。

**功能（ZIP 多层目录导入 · 需求④）：** 导入表格/照片 ZIP 递归搜索全部层级目录。

**功能（四端功能对照表升级 · 需求⑤ + 架构对照表固化 · 需求7）：** App 内对照单新增 v3.26 行；`gen_compare.py` 新增国产平台架构对照表：3A3000/4000→mips64el、3A5000+→loongarch64、FT2000/鲲鹏→arm64、其余→amd64。

**修复（UOS DEB 龙芯安装失败 · 需求⑥）：** data/control.tar.gz 改用 GNU_FORMAT（无 PAX 头）+ data.tar.gz 显式写入目录条目（./opt/、./opt/shuili-map/ 及子目录）+ 路径前缀修复，解决龙芯 3A4000/内核 4.19 统信 1050 安装报「无法创建 /opt/shuili-map/app.js.dpkg-new: 没有那个文件或目录」及首轮「不支持的 PAX tar 头部类型 'x'」。

**回归确认：** 需求②（图标颜色+形状双维度 + 图例）、需求⑦（查询/筛选置顶 + 计数 + 圈选跳转）、导入同名三选一/SHA-256、智能匹配、每页退出 + 三击空白、执行中「请稍后」等已实现项无回退；全 App 风格一致，未删除任何已有功能与菜单。

**变更文件清单：**
| 文件 | 变更类型 |
|------|----------|
| app.js | 修改（新增 normOffice / appBookmark / appGotoBookmark / 地图操作控件绑定；导入导出查询筛选应用统一命名；CHANGES/PLATFORM_COMPARE 加 v3.26；版本 v3.26） |
| index.html | 修改（新增 `#mapCtrl` 控件组与 CSS） |
| AndroidManifest.xml | 版本号 26 / 3.26 |
| assets/version.json | 更新至 v3.26 |
| build_deb.py | 修改（GNU_FORMAT + 目录条目 + opt/shuili-map 前缀） |
| verify_pkg.py | 修改（新增目录条目 + 前缀校验） |
| gen_compare.py | 修改（v3.26 功能行 + 架构对照表 + NSIS 描述） |
| SKILL.md | 修改（DEB 三坑完整记录） |

### v3.25（2026-08-20）— 历史版本

| 项 | 内容 |
|----|------|
| 版本号 | v3.25 |
| versionCode / versionName | 25 / 3.25 |
| 构建日期 | 2026-08-20 |
| APK 大小 | ~8.2 MB |
| 交付文件 | `D:/Users/Claw/APK归档/四端安装包_20260820/水利工程一张图V3.25_20260820_5090.apk`、统信 UOS DEB、Win11 x86_64 安装包、Apple HTTPS PWA 可托管 ZIP（同目录） |
| 源码快照 | `D:/Users/aowwei_app/webroot_shuili/`（app.js / index.html） |
| 备份路径 | `D:/Users/aowwei_app/backup/before_v325_20260820/`（回滚点，含 v3.24 旧版） |

**功能（建筑物双维度区分 · 需求②）：** 不同建筑物类型用「颜色 + 形状」双重区分——标记倒水滴填充色取类型专属色（`colorForType`），中心白点内嵌该类型专属形状符号（`shapeForType`：● ■ ▲ ◆ ★ ⬢ ✚ ✖ 循环对应）。左下角图例同步升级：每个类型项展示「彩色形状符号 + 类型名称」，一眼可辨类型归属。

**回归确认（需求③④⑤⑥⑦及四端对照单）：** 经代码逐条核对，此前 v3.16–v3.24 已落地的导入同名建筑三选一 / 同文件 SHA-256 二次导入提示、智能模糊匹配、每页退出按钮 + 三击空白主菜单、执行中「请稍后」忙提示、查询/筛选双圈计数跳转、「四端功能对照单」子菜单等本轮回归确认无回退；全 App 风格、背景、色彩、文字一致，未删除任何已有功能与菜单。

**变更文件清单：**
| 文件 | 变更类型 |
|------|----------|
| app.js | 修改（新增 shapeForType / 重构 makeIcon(color, glyph) / 图例 buildLegend 双维度；CHANGES 加 v3.25；版本 v3.25） |
| index.html | 修改（新增 `#legend .dot` 样式支持形状符号居中） |
| AndroidManifest.xml | 版本号 25 / 3.25 |
| assets/version.json | 更新至 v3.25 |

### v3.24（2026-08-19）— 历史版本

| 项 | 内容 |
|----|------|
| 版本号 | v3.24 |
| versionCode / versionName | 24 / 3.24 |
| 构建日期 | 2026-08-19 |
| APK 大小 | ~8.2 MB |
| 交付文件 | `D:/Users/Claw/水利一张图V3.24_20260819_5090.apk`、`D:/Users/Claw/APK归档/水利一张图V3.24_20260819_5090.apk` |
| 源码快照 | `D:/Users/aowwei_app/webroot_shuili/`（app.js / index.html）、`versions/V1.9.0/` |
| 备份路径 | `D:/Users/aowwei_app/backup/v3.24_2026-08-19/` |

**功能（主界面查询圈选）：** 顶栏「🔍 查询」按钮与搜索框回车键 = 查询确认，与筛选同一套结果处理——0 个符合：弹窗提示并保留关键词；1 个符合：立即 flyTo 定位；多个符合：倒水滴高亮标记 + 绿色虚线圈（#2e8b57）刚好罩住全部 + 平滑飞入显示整圈。此前「查询」菜单只是聚焦搜索框、无圈选动作，本次补齐。

**功能（筛选圈改浅蓝）：** 筛选「确认」的多结果圈选改为浅蓝色虚线圈（#4aa3e0），与主界面查询的绿色圈区分开——查询=绿、筛选=浅蓝。

**功能（建筑物类型规范化）：** 主界面图例、筛选「建筑物类型」、编辑表单类型下拉统一改为规范类型清单（`CANONICAL_TYPES` + `ALWAYS_TYPES`，见 app.js）。移除被误当类型的字段：西田各庄段引渠 / 技术指标 / 挖填方统计表 / “进水闸 部分缺少经纬度” 等（仅影响分类显示，不删除任何建筑），新增「泵站」「溢洪道」。历史遗留的非规范类型在编辑该建筑时仍保留可选项，避免静默改写数据。

**变更文件清单：**
| 文件 | 变更类型 |
|------|----------|
| app.js | 修改（新增 CANONICAL_TYPES/canonicalTypes/doQueryConfirm/FILTER_CIRCLE_COLORS；图例/筛选/编辑类型改用规范清单；查询圈选=绿、筛选圈=浅蓝；版本 v3.24） |
| index.html | 无改动（动画 CSS 于 v3.22 已就绪） |
| AndroidManifest.xml | 版本号 24 / 3.24 |
| assets/version.json | 更新至 v3.24 |

### v3.23（2026-08-19）

| 项 | 内容 |
|----|------|
| 版本号 | v3.23 |
| versionCode / versionName | 23 / 3.23 |
| 构建日期 | 2026-08-19 |
| APK 大小 | ~8.2 MB |
| 交付文件 | `D:/Users/Claw/水利一张图V3.23_20260819_5090.apk`、`D:/Users/Claw/APK归档/水利一张图V3.23_20260819_5090.apk` |
| 源码快照 | `D:/Users/aowwei_app/webroot_shuili/`（app.js / index.html） |
| 备份路径 | `D:/Users/aowwei_app/backup/v3.23_2026-08-19/` |

**修复（导入提示真机不弹 · 根因）：** kmz/ovkmz/建筑物表格导入时的重复文件与同名建筑物提示此前误用原生 `confirm()`/`alert()`，离线 Android WebView 不显示且默认返回 false，导致静默取消或静默覆盖。已全部改为自定义 `ask()` 对话框，提示必现。

**修复（导入覆盖逻辑）：** 三处均弹窗——① 同名建筑物「覆盖更新 / 保留现有 / 取消导入」三选一；② 同一文件按 SHA-256 内容比对二次导入提示「已导入过，是否覆盖」；③ 解析不到任何建筑时给出原因提示。

**修复并调整（查询圈选）：** 筛选/查询「确定」后——0 个符合弹窗提示并保留面板；1 个符合立即跳转到该建筑物；多个符合倒水滴高亮 + 绿色虚线圈（accent #2e8b57）刚好罩住全部 + 平滑飞入显示整圈；无结果/导入异常提示统一改用 `ask()`。

**一致性：** 圈选虚线圈由蓝改绿，与 App 主题绿一致；所有弹窗走统一 `ask()` 与 CSS 变量，风格、背景、色彩、文字全 App 一致。

**变更文件清单：**
| 文件 | 变更类型 |
|------|----------|
| app.js | 修改（导入提示改用 ask()、查询绿圈、无结果 ask()、应用内版本变更加 v3.23） |
| index.html | 无改动（v3.22 已加动画 CSS） |
| AndroidManifest.xml | 版本号 23 / 3.23 |
| version.json | 版本号 v3.23 / 23 |

### v3.2（2026-08-13）— 历史版本（热修复）

| 项 | 内容 |
|----|------|
| 版本号 | v3.2 |
| 构建日期 | 2026-08-13 |
| APK 大小 | ~8.1 MB |
| 备份路径 | `backup/v3.2_2026-08-13/` |

**紧急修复（致命）：**
- **修复 v3.1 启动白屏/假死（app.js 语法错误）**：批量导入建筑物功能中错误提示字符串 `未找到"名称"列` 内含未转义的双引号，导致整个 `app.js` 解析失败、所有功能（地图/菜单/按钮）均不初始化。已转义为 `\"名称\"`，并用 `node --check` 验证通过。
- **新增全局错误兜底**：`window.onerror` 将运行期错误通过 toast 提示，避免再次出现"无提示白屏/假死"。

### v3.1（2026-08-13）— 已回退（存在致命语法错误，请勿安装）

| 项 | 内容 |
|----|------|
| 版本号 | v3.1 |
| 构建日期 | 2026-08-13 |
| APK 大小 | ~8.1 MB |
| 状态 | ⚠️ 含致命语法错误，启动即白屏假死 |

**新增功能：**
- 获取坐标：支持GPS定位、点选地图、搜索建筑物三种方式获取经纬度，可一键复制
- 批量导入照片：选择多张照片或ZIP压缩包，按文件名自动匹配建筑物
- 批量导入建筑物：支持CSV/Excel/ZIP格式，按列名解析，有经纬度自动定位，无经纬度按名称匹配
- 悬浮菜单按钮（FAB）：右下角始终可见，快速打开菜单
- 三击标题打开菜单：快捷操作，防止列表视图中找不到菜单

**优化改进：**
- 列表卡片"定位"改为"快速定位"，点击后立即跳转地图
- 建筑物弹窗增加"关闭"按钮，防止找不到主界面
- 测距提示条增加×关闭按钮
- Leaflet弹窗关闭按钮放大，更易点击
- 菜单新增"获取坐标"、"批量导入照片"、"批量导入建筑物"三项
- 帮助文档更新，涵盖所有新功能

**Bug 修复：**
- 列表视图中主菜单不可见问题（通过FAB+三击解决）
- 各模式互斥逻辑增强（坐标拾取/测距/添加三选一）

**变更文件清单：**
| 文件 | 变更类型 |
|------|----------|
| app.js | 修改（新增坐标获取、批量导入、FAB、三击菜单等） |
| index.html | 修改（新增CSS、测距关闭按钮） |
| MainActivity.java | 无变更 |
| AndroidManifest.xml | 无变更 |
| docs/ | 更新（变更文档+版本历史） |
| backup/v3.0_2026-08-13/ | 新增（v3.0 备份） |

---

### v3.0（2026-08-13）

| 项 | 内容 |
|----|------|
| 版本号 | v3.0 |
| 构建日期 | 2026-08-13 |
| APK 大小 | ~8.1 MB |
| 备份路径 | `backup/v2.0_2026-08-13/` (v2.0 快照) |

**新增功能：**
- 导航：建筑物弹窗「导航」按钮，唤起第三方导航 App
- 测距：菜单「测距」，多点折线测距，显示各段+总距离
- 周边搜索：菜单「周边搜索」，选中心建筑物+半径，列出范围内建筑物

**优化改进：**
- 标记图标改为蓝色水滴+白点（统一风格）
- 定位失败自动跳转系统设置（权限拒绝时）
- 菜单智能排序（高频功能靠前）
- 关于页增加建议使用环境+制作环境+当前运行环境
- 天地图 Token 规范化（区分浏览器端/服务器端）

**Bug 修复：**
- 测距模式与其他模式互斥
- UI 遮挡问题修复

**文档建设：**
- 新增软件需求说明书、概要设计、详细设计、变更文档、版本历史

**变更文件清单：**
| 文件 | 变更类型 |
|------|----------|
| app.js | 修改（核心逻辑） |
| index.html | 修改（CSS + HTML） |
| MainActivity.java | 无变更（v2.0 已完成） |
| AndroidManifest.xml | 无变更（v2.0 已完成） |
| docs/ | 新增（5 份文档） |
| backup/v2.0_2026-08-13/ | 新增（v2.0 备份） |

---

### v2.0（2026-08-13）

| 项 | 内容 |
|----|------|
| 版本号 | v2.0 |
| 构建日期 | 2026-08-13 |
| APK 大小 | ~8.1 MB |
| 备份路径 | `backup/v2.0_2026-08-13/` |

**新增功能：**
- 自定义底图切换器（替代 Leaflet 内置 layers control）
- 照片上传（WebChromeClient.onShowFileChooser）
- 导出文件保存到 Download 目录（DownloadListener + saveBlob）
- 关于页显示运行环境信息（OS/CPU/GPU/屏幕等）

**优化改进：**
- 列表视图「定位」支持线要素（取中点坐标）
- 切换视图后调用 map.invalidateSize()
- 顶栏 nowrap + 横向滚动
- 缩放控件移到右下角
- 侧滑面板 overflow:hidden

**Bug 修复：**
- 底图切换 radio 状态不同步
- 照片上传"选择文件"无反应
- 列表定位不跳转

**变更文件清单：**
| 文件 | 变更类型 |
|------|----------|
| app.js | 修改 |
| index.html | 修改 |
| MainActivity.java | 修改（新增 onShowFileChooser, DownloadListener, JSBridge） |
| AndroidManifest.xml | 修改（新增权限） |

---

### v1.0（2026-08-12）

| 项 | 内容 |
|----|------|
| 版本号 | v1.0 |
| 构建日期 | 2026-08-12 |
| APK 大小 | ~8.0 MB |

**初始功能：**
- 天地图矢量/影像底图
- 建筑物标记（SVG 水滴，按类型着色）
- 筛选（管理所/管理站/类型 + 关键字）
- 添加/修改/删除建筑物
- 多照片管理
- 列表视图 + 定位
- 导入/导出 ovkmz
- 统计
- 定位
- 帮助/关于
- localStorage 持久化
- 恢复初始数据

**初始文件：**
| 文件 | 说明 |
|------|------|
| index.html | HTML + CSS |
| app.js | 应用逻辑 |
| data.js | 建筑物数据（parse_ovkmz.py 生成） |
| leaflet/ | Leaflet 库 |
| jszip.min.js | JSZip 库 |
| images/ | 建筑物照片 |

---

## 2 备份策略

### 2.1 备份规则
- **每次版本发布前**，将当前版本所有文件备份到 `backup/v{X.X}_{YYYY-MM-DD}/`
- **备份内容**：app.js, index.html, MainActivity.java, AndroidManifest.xml
- **每次变更**记录在 `04_软件变更文档.md`
- **每次新增/变更需求**，同步更新所有文档

### 2.2 备份目录结构
```
backup/
├── v2.0_2026-08-13/
│   ├── app.js
│   ├── index.html
│   ├── MainActivity.java
│   └── AndroidManifest.xml
├── v3.0_2026-08-13/
│   ├── app.js
│   ├── index.html
│   ├── MainActivity.java
│   └── AndroidManifest.xml
├── v3.2_2026-08-13/
│   ├── app.js
│   └── index.html
└── ...
```

### 2.3 回退流程
1. 确定要回退到的版本号
2. 从 `backup/` 中找到对应版本的备份目录
3. 复制备份文件覆盖当前文件
4. 重新构建 APK
5. 更新本文档记录回退操作

---

## 3 版本发布检查清单

每次发布新版本时，需完成以下检查：

- [ ] app.js 中 `APP_VERSION` 和 `APP_BUILD_DATE` 已更新
- [ ] index.html 中 CSS 和 HTML 无语法错误
- [ ] MainActivity.java 编译通过
- [ ] APK 构建成功
- [ ] 在手机上测试核心功能（地图/筛选/增删改/导入导出/测距/导航/周边搜索）
- [ ] 备份当前版本到 `backup/`
- [ ] 更新本版本历史文档
- [ ] 更新变更文档
- [ ] APK 复制到 `D:/Users/Claw/`

---

## 4 文档同步规则

当需求变更时，以下文档需同步更新：

| 变更类型 | 需更新文档 |
|----------|-----------|
| 新增功能 | SRS (需求) + DDD (设计) + 变更文档 + 版本历史 |
| 修改功能 | DDD (设计) + 变更文档 + 版本历史 |
| 修复 Bug | 变更文档 + 版本历史 |
| 配置变更 | SRS (约束) + DDD (配置) + 变更文档 |
| UI 变更 | DDD (CSS/HTML) + 变更文档 |


---

## v3.7（2026-08-15）优化迭代

> 本次按用户自查清单对「水利工程基础信息一张图」做体验与能力优化，未删除任何原有功能/菜单。

### 一、菜单图标与筛选图标（Req 1）
- 所有菜单图标统一改为**圆形色块**（主色-浅底白字），醒目且风格一致。
- 筛选图标由 `🔎` 改为 `🎚️`，与查询 `🔍` 明显区分（原两者均为放大镜，易混淆）。

### 二、筛选实时计数 + 结果框选（Req 2）
- 筛选弹层顶部新增**实时计数条**「当前符合条件：N 个建筑」，每勾选/取消一个芯片即时刷新。
- 点击「确认」后：
  - 0 条 → 提示「没有符合条件的选项」；
  - 1 条 → 立即定位该建筑物（沿用原逻辑）；
  - 多条 → 每个命中建筑替换为**倒水滴高亮标记**（琥珀色、尖端朝上），并以**蓝色虚线圈**（`#1e6fbf`，虚线 `7,8`）刚好罩住全部命中建筑，自动 `fitBounds` 适配视图。

### 三、批量导入照片（Req 3）
- 入口图标保持醒目；新增**来源选择**：本地文件 / 百度网盘 / 夸克网盘。
- 压缩包格式支持 **ZIP** 与 **7z/RAR**（原生 `unzipImages` 解压；7z/RAR 若原生不支持会提示改用 ZIP）。
- 网盘导入调用原生 `netdiskPick`（若未连接网盘账号则给出明确提示，请先用本地导入）。

### 四、导出照片（Req 4）
- 导出对话框可选：**范围**（全部合并 / 按管理所单独）、**格式**（ZIP / 7z）、**目标**（本地下载 / 百度网盘 / 夸克网盘 / 分享微信 / 飞书 / QQ）。
- 压缩包按管理所命名与分文件夹；新增原生 `shareFile` + `ShareProvider`，**分享到微信/飞书/QQ 真实可用**（以 content:// 规避 Android 7+ 的 file:// 限制）。
- 7z 格式在当前原生打包未支持时回退为 ZIP 并提示。

### 五、智能匹配确认（Req 4 智能匹配）
- 多匹配确认弹窗**默认勾选最匹配项**，并标注**置信度**（高匹配 / 较可能 / 待确认）与**推断范围**（管理所 / 类型），用户可一键确认或改选。

### 六、风格统一（Req 3）
- 顶栏/弹层头渐变、进度条、悬浮按钮等内联色统一引用 `:root` CSS 变量；刷新《07_APP风格色彩字体搭配》。
- 查询/筛选保持顶栏显眼；每页保留「退出本页」；三击空白处仍可呼出主菜单。

### 七、v3.32 / v1.8 / v2.0（2026-08-27）
- **管理所选项补全**：水利一张图筛选 / 导出 / 编辑表单「管理所」统一含全部 9 所（地下水源所、温泉所、龙山所、史山所、埝头所、水库所、北台上所、西田各庄所、潮河所）；`normOffice` 补 `潮河总干渠→潮河所`、`怀柔水库所→水库所`。
- **三端四平台升版并交付**：水利 V3.32 / 感知 V1.8 / 古建 V2.0；八处版本同步（含手动补 `make_deb.py`、`build_ios_zip.py` 两处 dict 版本源）。
- **交付物**：`APK归档/四端安装包_20260827_v332/`（APK + mips64el DEB + Win MSI/EXE + iOS PWA ZIP，三产品齐全）。
- **验证**：ovobj 导入导出 round-trip 全过；管理所 9 所齐全；UOS DEB ar 双解析器对齐（根除「deb 损坏」）。
- **回退**：`四端安装包_20260825_v331/`（v3.31）保留作旧版回退。

### 八、v3.41 / v1.17 / v2.9（2026-08-29 晚）— 当前版本
- **智能AI×知识库融合**：KB 发行前预生成种子随包内置（557/1125/1032 条）；查询本地优先(🔒)→在线兜底(🌐)→友好提示，根治 script error；网页读取入库；补丁可编辑。
- **水利专项修复**：筛选照片状态卡顿（事件委托）；管理所/管理站手动增改+表单下拉；关键词历史；周边搜索三中心+类型筛选；导出全 9 所+自动匹配+四类导出自定义文件名。
- **交付物**：`APK归档/四端安装包_20260829_v341/` 四平台×三产品 15 件；小程序三处版本对齐 3.41。
- **验证**：APK aapt2+apksigner 门禁全过；UOS deb verify_myml_ar 3/3；MSI wix validate 过；iOS README 版本 3.41/1.17/2.9。
- **回退**：`backup_2026-08-29_before_v341/`；旧包 `四端安装包_20260829_fixed/`（v3.40）保留。

### 九、v3.42 / v1.18 / v3.0（2026-08-30）
- **五级组织**：局/管理处/所/站/段，默认局=水利工程管理中心/管理处=京密引水/所=水库所；设置→组织与类型管理三 Tab（名称增删改/默认修改/建筑物类型）。
- **快捷常用（⭐）**：菜单置顶新组，☆/★ 切换 + 设置里批量勾选；空隐藏。
- **导出组装器**：四类导出统一"文件名组成 + 文件夹层次"勾选（局/管理处/所/站/段，`_` 与 `/` 分隔），按类型独立持久化偏好。
- **zip 三级匹配**：zipName→zipFolder→fname，命中组织五级强锁定 scope（如 `史山.zip` 自动锁定史山所）。
- **perc 专项**：5 类分类方案（信息系统分类/感知子系统/中类/子类/信息传输方式）独立增删改 UI 字眼统一为"信息系统分类"。
- **四端出包**：`APK归档/四端安装包_20260830_v342/`（APK×3+deb mips64el×3+MSI×3+EXE×3+iOS PWA×3）+ 小程序三处同步 3.42。
- **回退**：`backup_2026-08-30_before_v342/`。

### 十、v3.43 / v1.19 / v3.1（2026-08-30 v2）
- **水利 hotfix**：筛选历史关键词上移 / 多管理所默认 / 段可空 / 周边搜索类型 bug / 快捷常用星标与点击分隔 / zip 匹配 level-aware。
- **AI placeholder 适配**：古建/感知/水利按 APPNAME 切换默认查询示例。
- **四端出包**：`APK归档/四端安装包_20260830_v343/` 15 件；小程序三处同步 baseline v3.43。
- **回退**：`backup_2026-08-30_before_v343/`。

### 十一、v3.44 / v1.20 / v3.2（2026-08-30）
- **perc 历史 cp 失误修复**：LS_KEY/APPNAME/AIModule init/data.js 全部回归感知语义。
- **古建 v3.2 智能化升级**：5 级景区组织 + 17 类古建类型 + 快捷常用 + 历史关键词 + 周边搜索 nbtype + ZIP 三级 + AI 联网文物助手。
- **三图核心差异**：水利/感知=内部应用；古建=公共受众。
- **四端出包**：`APK归档/四端安装包_20260830_v344/` 15 件；小程序三处同步 baseline v3.44。
- **回退**：`backup_2026-08-30_before_v344/`。

### 十二、v3.45 / v1.21 / v3.3（2026-08-30）
- **E1 设置菜单加 🗝️ 修改/添加天地图密钥**（localStorage→TIANDITU_TOKENS→内置默认三层回退，refreshTdtTileLayer 立即生效）
- **E2 筛选加管理处可多选 chip**（默认京密引水管理处，按局→管理处→所层次）
- **E3 导出建筑物表格 guanchu 列**（默认不勾选，老用户兼容）
- **E5 zip longest-match**（按局/处/所/站/段顺序+token.indexOf 长度倒序；案例：史山.zip → 三扬分水闸）
- **E6 placeholder 文雅化**：古建/感知三态已通用
- **E8 古建 v3.3 / 感知 v1.21 同步水利 v3.45 智能化升级**
- **四端出包**：`APK归档/四端安装包_20260830_v345/` 15 件；小程序 jingyin 三处 baseline v3.45
- **回退**：`backup_2026-08-30_before_v345/` 638 个文件全量备份

### 十三、v3.46 / v1.22 / v3.4（2026-08-31）

- **F1 导出建筑物表格 script error 修复**：`doExportTable` 补 guanchu 兜底 + 单行 try/catch。
- **F2 导出照片 script error 修复**：导出文件名组装走统一入口 + 空值保护。
- **F3 全菜单回归验证**：全部菜单点击无 script error。
- **F4 导入压缩包照片退回 v3.42 三级匹配**：`zipName→zipFolder→fname` 收敛 scope；案例 `史山.zip → 三扬分水闸`。
- **F5 筛选自定义组织/类型「添加成功」真正生效**：增改回写持久化并同步建筑物（`getCustomOffices` ×6）。
- **F6 历史关键词上移**：查询关键词紧下方。
- **F7 智能 AI 无 KB 条目注入本域组织/类型数据增强**。
- **F8 智能 AI 结果页「进一步操作」+ 关键词复制（单击复制 / 双击复制并查询）**。
- **F9 设置密钥隐藏明文 + 复制需密码 3305**（水利/古建/感知）。
- **四端出包**：`APK归档/四端安装包_20260831_v346/` 15 件；小程序 jingyin 三处 baseline v3.46（perc v1.22 / gujian v3.4）。
- **回退**：`backup_2026-08-31_before_v346/`（109 文件版本升级前快照）；`backup_2026-08-30_before_v345/`（638 文件全量）可作全量回退。

### 十四、v3.46 二次回归彻底解决（2026-08-31 补发，对应本轮 9 条复检）

- **G1 天地图密钥输入框泄露真实密钥（#386）**：「新密钥」placeholder 由真实密钥改为 `32位十六进制密钥（如 a1b2c3…，留空则恢复内置默认）`；10 份 app.js 掩码 grep 核验一致，真实默认仅存 `TOKEN_BROWSER_DEFAULT` 不展示。
- **G2 知识库导出 zip 文件名与导入一致（#387）**：`kbExport` 导出名统一 `kb_<domain>_<YYYYMMDD>.zip`，导入互认；10 份 ai_module.js 一致。
- **G3 知识库读取外部文件 script error（#388）**：`kbImportExternal` 全程 try/catch + 友好 toast + 粘贴兜底；10 份 ai_module.js 一致。
- **G4/G5 水利 APK 导出建筑物表格 / 导出照片 script error 回归（#389/#390）**：根因是交付 APK 未跟随源码重建；已重构建三产品 APK（shuili-v329 / perc-v13 / gujian-v31+travel/android），node --check 全过。
- **G6 全子菜单回归验证无 script error（#391）**：全部 11 活跃 webroot 植入全局 `window.onerror` + `unhandledrejection` 兜底，捕获写 `localStorage("script_errors")` + 友好 toast；标记 6+5 处。
- **G7 UOS deb 安装失败（#392，铁律级）**：根因 `make_deb.py` 纯 Python tar 未声明父目录条目 → dpkg 暂存 `.dpkg-new` ENOENT；修复 `collect_dirs` 显式写 DIRTYPE 条目。验证：3 个重建 deb 均含 9 目录条目 / 0 缺失父目录 / 启动器 0o755 → ALL DEBS HAVE PARENT DIRS ✅。三 deb：`shuili-map_3.46.20260831_mips64el.deb` / `shuili-ganzhi_1.22.20260831_mips64el.deb` / `gujian-map_3.4.20260831_mips64el.deb`。铁律：`verify_myml_ar.py` 3/3 通过，目录条目声明已写入 make_deb.py。
- **G8 知识库读取公众号链接 failed to fetch（#393）**：`kbImportUrl` 加 CORS 代理回退 `https://api.allorigins.win/raw?url=<encodeURIComponent(url)>` + 手动粘贴兜底 `openKbPasteFallback`，fetch 永不裸抛；10 份 ai_module.js 一致。
- **二次回归出包**：`APK归档/四端安装包_20260831_v346/` 15 件；小程序 jingyin 三处 baseline v3.46 / v1.22 / v3.4。

### 十五、v3.46 三次回归彻底解决（2026-08-31 终次，对应本轮 6 条复检真因）

> 用户再次复检指出「导出建筑物表格 / 导出照片 / 知识库读取外部文件 / 知识库公众号链接 / 知识库 zip 导入导出」仍报 script error 或 `q is not a function`，强调「老问题未彻底解决」。本轮抛开上一轮 G3/G4/G5 表象归因，读源码定位两个长期遗漏的真源 bug，源码修复 + 全端重建 + 执行级验证（Node 沙盒实际运行函数）根治。

- **G9 知识库「读取外部文件」q is not a function（#388 真因修正，覆盖原 G3）**：`ai_module.js` `function renderKBList(q)` 形参 `q` 遮蔽同文件 DOM 助手 `q(id)`，`openKB()`→`renderKBList("")` 调 `q("kbList")` 实为 `字符串.kbList` → `q is not a function`；崩溃在对话框构建阶段、早于 `kbImportExternal` 的 try/catch，故 G3「补 try/catch」兜不住，整个知识库菜单点不进。修复：形参改名 `function renderKBList(kw)`（11 份 ai_module.js 同步）。连带使 G2（zip 名 `kb_<domain>_<date>.zip`）、G8（CORS 代理）此前不可达者随菜单打通生效，公众号链接读取 / zip 导入导出互认恢复，导出名不再为 UUID。验证：Node 沙盒 `renderKBList('温泉所')` 干净执行。
- **G10 导出建筑物表格 / 导出照片 打开即 script error（#389/#390 真因修正，覆盖原 G4/G5）**：`app.js` `orgValOrDefault(b,k)` 调用 `defaultOffice()` 但该函数从未定义，打开导出菜单即抛 `ReferenceError` → 顶层「Script error」；是存在于源码数月、每次点击导出都崩的真源 bug。G4/G5 仅「重构建 APK」未修源码，重装仍崩。修复：补定义 `function defaultOffice(){ return getOrgDefaults().suo || ORG_DEFAULT_NAMES.suo || "水库所"; }`（7 份 app.js 同步）。验证：Node `vm` 沙盒加载 app.js 调用 `exportTable`/`openExportPhotos`/`doExportTable`/`doExportPhotos`/`defaultOffice`/`orgValOrDefault`/`assembleExportName` 全部无抛错（修复前均崩）；交付 APK 解包核验含该函数。
- **三次回归出包与验证**：`APK归档/四端安装包_20260831_v346/` 15 件；小程序 jingyin 三处 baseline v3.46 / v1.22 / v3.4。`verify_myml_ar.py` 对 3 个重建 deb 复验 3/3 PASS（无「没权限 / deb 损坏」回归）。导出/知识库函数在 Node 沙盒实际运行通过 = 执行级验证，区别于既往仅语法检查。
- **回退**：`backup_2026-08-31_before_v346/`（109 文件）；`backup_2026-08-30_before_v345/`（638 文件）可作全量回退。

### 十六、v3.48 data.js 缺逗号致命修复（2026-09-01）

> 发版 v3.47 后真机复现：**水利一张图全平台打开即崩，所有菜单点击报「运行错误：Script error. @0:0」**。

- **真因**：`data.js` 中 `window.SHUILI_DATA` 数组 `b103`→`b104` 之间缺失逗号（09-01 坐标更新脚本替换时丢失），整个 JSON 字面量解析失败（`Unexpected token '{'` @43923）→ `BUILDINGS` 未定义 → App 初始化即崩 → 所有依赖 `BUILDINGS` 的菜单统一 script error。这是「编辑 data.js 即可能整 App 崩」的隐性雷。
- **修复**：`fix_data_comma.py` 对全部活动水利 webroot 补回逗号；新增 `node --check` + `JSON.parse(SHUILI_DATA)` 门禁（557 条，改 data.js 必过）。
- **验证盲区补强**：旧 `verify_threemap_harness.js` 沙盒对未定义全局返回 noop，捕获不到运行时 `ReferenceError`；新增 `verify_export_runtime.js` 忠实沙盒（实际加载 data.js+bridge+app.js 并调用 `doExportTable`/`doExportPhotos`，不掩码未定义全局），三端 PASS。
- **全平台重建**：水利包从修复后源码重建（APK + mips64el/loongarch64/arm64/amd64 四架构 deb + MSI + EXE + iOS PWA），逐一抽取 `data.js` 核验 `b103_BAD=False`/557 条；感知/古建同源重建至 v1.24/v3.6（数据未受影响，仅对齐）。
- **约束⑤核查**：古建 `openSheet` 已自动注入「✕ 退出本页」foot（与水利同源），三击空白 handler 已落地；旧审计按字面「退出」计数误判缺口，本版**不改动** gujian app.js（避免引入新 script error）。
- **门禁全绿**：`verify_myml_ar.py` 12/12；MSI/EXE `GetWebView2DataFolder` 标记 + webroot 版本门禁；`sync_all_mirrors.py --check` EXIT=0；`sync_weapp_version.py --check` 基线 v3.48。
- **交付物**：`APK归档/四端安装包_20260901_v348/`（APK×3 + deb 四架构×3=12 + MSI×3 + EXE×3 + iOS PWA×3 = 24 件）；小程序 jingyin 三处 baseline v3.48 / v1.24 / v3.6。
- **回退**：`backup_2026-09-01_before_v348/` + `_backup_live/Claw_backup_20260901_174432/`。

### 十七、v3.49 / v1.25 / v3.7（2026-09-02）— 文档与诊断增强

> 用户真机确认 v3.48「安装正常、能正常打开、日志也正常出现」（`launch_err.log` 含 `serving version= 3.48` + `no-store=on` + `HTTP=200`，user=guest）。本版在稳定 v3.48 基线之上，做「启动诊断全平台化 + 版本号自动跟随 + 菜单内容更新」。

- **F1 全平台启动诊断 logRuntime（版本号自动跟随）**：三端 canonical app.js 新增 `logRuntime()`，启动时把「产品名 + 版本号(读 `APP_VERSION`) + 构建日期 + 平台(UA 判定 iOS/Android/Win/UOS/Web)」写 `console.log` 与 `localStorage["yitu_runtime_log"]`（最多 20 条）；版本一升、日志里的版本号自动跟着变。UOS 端既有 `server.py._startup_log()`（写 `<pkg>/launch_err.log`+`server.log`+`$HOME/.cache`）兜底证明浏览器加载的是当前版本；铁律-UOS5~9（no-store / 每次写日志 / 日志权限不被递归 chmod 破坏）继续保留。
- **F2 子菜单「帮助」→「功能介绍」改名 + 正文补全（三端）**：`showHelp()` 标题改「功能介绍」，正文扩充 16 项历次新增功能（筛选持久化/快捷常用长按/智能AI/知识库/运行维护/设置-天地图密钥/错误日志排查等）；主菜单与子菜单两处同步改名，旧「帮助」残留 grep 清零。
- **F3 四端功能对照单 + 版本变更补到最新**：`PLATFORM_COMPARE`/`CHANGES`（水利、感知）与 `openChangelog()`（古建）插入 v3.49/v1.25/v3.7（2026-09-02）条目；古建、感知参照同样同步改名与内容。
- **F4 版本统一提升 + 三图同源同步**：水利 3.48→3.49、感知 1.24→1.25、古建 3.6→3.7；`version.json`(真相源)+app.js(`APP_VERSION`/`APP_BUILD_DATE`)+全部 webroot(`sync_all_mirrors.py --real`)+构建脚本硬编码串(`make_deb.py`/`gen_win_msi_wix7.py`/`build_win_exe_nsis.py`/`build_ios_zip.py`)+小程序基线(`sync_weapp_version.py`，`DEFAULTS` 对齐 3.49/1.25/3.7/2026-09-02) 全量提升。
- **门禁全绿**：`node --check`（三端 app.js 含 logRuntime / 功能介绍）；`verify_myml_ar.py` 12/12；`sync_all_mirrors.py --check` EXIT=0；`verify_fav_longpress.js`（长按收藏回归）；`sync_weapp_version.py --check` 基线 3.49。
- **交付物（本环境可产出）**：`APK归档/四端安装包_20260902_v349/`（UOS deb 四架构×3=12）+ `native-shell/water-ios/apple-package/`（iOS PWA zip×3：3.49/1.25/3.7）。Win MSI/EXE 与安卓 APK 版本串已 bump，需在构建机（WiX7/.NET SDK、Android SDK）跑 `gen_win_msi_wix7.py`/`build_win_exe_nsis.py`/`build_apk.sh` 产出。
- **回退**：`backup_2026-09-02_before_v349/`；旧 `四端安装包_20260901_v348/` 保留可作回退。
