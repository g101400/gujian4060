# 2026-09-07 工作日志

## 19:53 自动化：同步古建源码到 GitHub g101400/gujian4060

- 工作树改动 4 文件（index.html / js/app.js / js/io.js / 自动化 memory.md）→ `git commit`（09dfac1）。
- 首次 `python build/push.py` exit=1：遇 >1MB 大文件走 `git/blobs` 比对时网络 `IncompleteRead`（读到 2.34MB 断流），原脚本无重试直接崩。
- 修复 `build/push.py` 两处健壮性缺陷：
  1. `api()` 加瞬时错误重试（IncompleteRead / 5xx / URLError / socket 超时，最多 3 次，退避 2/4s，超时 120→180s）；
  2. **改用 git blob SHA 比对替代大文件字节下载**：远端用 contents 接口返回的 blob `sha`，本地用 `git hash-object`，匹配即跳过，彻底绕开大文件下载。
- 二次运行 exit=0：新增 1 / 更新 10 / 跳过 103 / 失败 0（共 114）。
- 发现并清理误推的 `build/__pycache__/push.cpython-313.pyc`：经 REST API 删除远端（DELETE 200）+ 本地 `rm -rf`，并把 `__pycache__/`、`.pyc` 加入 `.gitignore`。
- 提交 `.gitignore` + `push.py`（d001c0a），重跑 exit=0：新增 0 / 更新 10 / 跳过 103 / 失败 0（共 113）。
- 安全：推送列表仅含 `secrets/config.js` 占位模板，`secrets/config.local.js` 与 `build/targets/keystore/` 始终被 .gitignore 排除，密钥零外泄。
- 教训：本机 api.github.com 对 >1MB blob 的 GET 不稳定，REST 推送比对逻辑必须走 SHA 而非下载字节；Python `__pycache__` 需主动 gitignore。
